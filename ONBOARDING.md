# جو — ما هو، وكيف يعمل

> **اقرأ هذا أولاً.** صفحة واحدة تشرح الفكرة، ثم التفاصيل.

---

## الفكرة في سطرين

**جو ليس خدمة سحابية. جو برنامج يعمل على جهاز المستخدم.**

يفتح المستخدم `http://localhost:5002` في متصفحه، يكتب طلباً بالعربية، فيبني له جو نظاماً حقيقياً — ملفات على قرصه هو، وخادماً يعمل على منفذه هو، وقاعدة بيانات في مجلده هو. لا حساب، ولا اشتراك، ولا خادم بعيد يملك شيئاً من عمله.

هذه ليست تفصيلة نشر. **هذه هي المعمارية**، وكل قرار في المستودع مبنيّ عليها.

---

## ١. لماذا محلّي؟ ولماذا يهمّك هذا كبنّاء

| لو كان جو خدمة سحابية | جو كما هو |
|---|---|
| كود المستخدم على خوادم غيره | كوده على قرصه، يملكه ويراه |
| «شغّل المشروع» يعني حاوية بعيدة | يعني عملية على جهازه، تستمع على منفذه |
| الفحص يجري في مكان لا يراه | يجري أمامه في لوحتين مفتوحتين |
| يحتاج إنترنت ليعمل | يعمل بلا إنترنت — البناء والفحص والتحسين كلها محلية |
| بياناته عندك | بياناته عنده |

**النتيجة العملية عليك كبنّاء:** لا تكتب شيئاً يفترض وجود شبكة، ولا مساراً مطلقاً على خادمك، ولا حالة تعيش في الذاكرة بين الطلبات. كل ما يبنيه جو يجب أن يعمل على جهاز فيه Node وحده.

---

## ٢. ما الذي يعمل فعلاً حين يقلع

سكربت واحد: `.\update-joe.ps1` — يسحب، يبني، **يوقف النسخة القديمة على 5002**، ويشغّل الجديدة.

ثم **عملية Node واحدة** تخدم كل شيء:

```
http://localhost:5002
├── /            →  واجهة React (مبنية مسبقاً من web/dist)
├── /api/*       →  الـAPI
└── ws://…       →  بثّ حيّ للوحات (طرفية، متصفح، تفكير، معاينة)
```

**خادم واحد، منفذ واحد، أصل واحد.** لا proxy، ولا CORS، ولا خادمان يتسابقان.

---

## ٣. أين يعيش كل شيء على قرصه

```
C:\Users\home\Documents\xelitesolutions\
├── api\                 ← الخادم + كل ذكاء جو (TypeScript)
├── web\                 ← الواجهة (React + Vite)
├── services\            ← عامل المتصفح (Playwright) — اختياري ومعزول
├── infra\               ← Docker/Nginx — للنشر على خادم، لا للتشغيل المحلي
└── data\
    ├── projects\
    │   └── my-workspace\      ← ★ كل ما يبنيه جو للمستخدم يهبط هنا
    │       └── react-متجري\
    │           └── .joe-versions\   ← نسخ تلقائية قبل كل تعديل
    └── …
```

**نقطتان مهمّتان:**
- `data/projects/my-workspace` هو **جذر مساحة العمل**. يمكن تثبيته بمتغيّر `JOE_WORKSPACE_ROOT` — استخدمه في أي اختبار معزول.
- `.joe-versions` تُكتب **قبل** أي تعديل. لهذا «تراجع» فوري ومضمون.

---

## ٤. ما يحدث حين يكتب المستخدم طلباً

```
كلامه
  ↓
المخطِّط (PlanningEngine)  ← حتمي، بلا نموذج، يقرر: صفحة؟ تطبيق؟ نظام؟
  ↓
أداة واحدة أو أكثر (156 أداة مسجّلة)
  ↓
اللوحات تُفتح أمامه: طرفية · متصفح · تفكير · معاينة
  ↓
رسالة تسليم تقول ما بُني وما لم يُبنَ
```

**المخطِّط لا يستدعي نموذجاً لغوياً للقرارات الشائعة.** «شغّل المشروع» و«تراجع» و«عدّل الاسم» لها مسارات سريعة حتمية — تعمل حين تكون الشبكة مقطوعة والمفاتيح غائبة. النموذج للأشياء الغامضة فقط.

---

## ٥. مسار البناء — قلب النظام

هذا المسار هو **٩٠٪ من قيمة جو**. افهمه قبل أن تلمس شيئاً.

```
١. الطرفية تُفتح          ← قبل أول ملف. تطبع node -v و npm -v فعلياً.
٢. تصميم البيانات        ← جداوله المصرَّحة هي الجداول. لا يُخترع شيء.
٣. كتابة المشروع         ← React/Vite حقيقي، أو Express + SQLite للأنظمة.
٤. npm install + build   ← عمليات حقيقية، كل أمر بمِحَثّه ورمز خروجه.
٥. التحزيم              ← الواجهة تُنسخ داخل الخادم → أصل واحد.
٦. القياس بأداتين:
     المتصفح  ← Chromium حقيقي يضغط الأزرار ويملأ النماذج ويقيس التباين والتصميم
     الطرفية  ← ٩ فحوص حقيقية على الخادم والواجهة (عمليات، لا ادّعاءات)
٧. حلقة التحسين         ← أصلح → أعد البناء → قِس ثانية.
                            أي جولة لا ترفع الرقم → يُتراجَع عنها.
٨. حكم القبول           ← يقرأ بريفه بنداً بنداً: ماذا أثبتُّ؟ ماذا لم أُثبت؟
٩. التسليم              ← النظام يبقى يعمل، والرابط يُعطى له.
```

---

## ٦. القوانين — اقرأها مرّتين

هذه ليست أسلوباً. **مخالفتها تُرفض في المراجعة.**

### ١) لا كود وهمي
لا `Math.random()` كنتيجة، ولا نجاح مُفترض، ولا رسالة تصف عملاً لم يحدث. إن لم تستطع القياس، **قُل إنك لم تقس**.

### ٢) الميزة لا تكتمل حتى **يصلها** النظام
أداة مسجّلة لا يصل إليها المخطِّط = ميزة غير موجودة. يُفرَض هذا في `api/src/__tests__/wiring-policy.test.ts`.

### ٣) الاختبارات هي العقد
**2790 اختباراً في 163 ملفاً.** كل ضمان في النظام مكتوب كاختبار يسقط حين يسقط الضمان. تغيّر آلية؟ **وجّه الاختبار إلى الآلية الجديدة، ولا تحذف الضمان.**

### ٤) الأشكال لا القوائم
لا نهاية لما قد يطلبه إنسان، لكن **قائمة الأشكال محدودة**. لا تكتب قائمة مجالات (مطاعم، عيادات، متاجر…) — اكتب شكلاً («… اسمها X»، «لا ت…»، «المطلوب:»). القائمة تنمو مع العالم؛ الشكل ينمو مع اللغة.

### ٥) التحقّق حيّ لا نظري
كل ميزة تُختبر بتشغيل حقيقي. انظر `api/src/tests/manual/` — **178 برنامج تحقّق** يُشغّل النظام فعلاً. أهمّها:

```bash
cd api
npx tsx src/tests/manual/verify_the_whole_chain.ts   # السلسلة كاملة  (37 فحصاً)
npx tsx src/tests/manual/verify_every_loop.ts        # ١٤ حلقة النظام (54 فحصاً)
```

---

## ٧. البوابات قبل أي دفع

```bash
cd api
npx jest              # 2790 اختباراً
npx tsc --noEmit      # صفر أخطاء أنواع
npm run build         # esbuild
cd ../web && npm run build && npm run type-check
```

**الأربعة خضراء أو لا تدفع.** والـCI يعيدها كلها ويوقف النشر إن سقطت واحدة.

---

## ٨. الشبكة: ما يحتاجها وما لا يحتاجها

| محلّي بالكامل | يحتاج شبكة |
|---|---|
| البناء، التحزيم، الفحصان، الحلقة، حكم القبول | النماذج اللغوية (مجانية، أو Groq بمفتاح) |
| قراءة النص من الصور (OCR عربي+إنجليزي) | صور المواقع الحقيقية |
| الخطوط العربية (مرفقة في المستودع) | النشر على GitHub |
| قاعدة البيانات (SQLite مدمجة في Node) | تنزيل Chromium أول مرة |

وحين يقول المستخدم **«لا تستخدم الشبكة»**، يطيع: لا تثبيت حزم، ولا تنزيل متصفح — **ويقول ماذا تخطّى ولماذا**.

---

## ٩. مصائد يقع فيها كل بنّاء جديد

| المصيدة | الصواب |
|---|---|
| افتراض وجود شبكة | كل مسار له طريق يعمل بلا إنترنت |
| كتابة مسار مطلق | كل شيء تحت جذر مساحة العمل |
| «نجح» لأن الدالة لم ترمِ خطأ | النجاح = **قياس**، لا غياب استثناء |
| حذف اختبار بدل توجيهه | الضمان يبقى، الآلية تتغيّر |
| قائمة كلمات مفتاحية جديدة | اسأل: هل هذا **شكل** أم **مجال**؟ |
| `process.exit()` داخل فحص | `process.exitCode` — الأول يُسقط libuv على ويندوز |
| إصلاح بلا إعادة قياس | كل إصلاح يُثبت بالرقم بعده |

---

## ١٠. من أين تبدأ القراءة

| تريد أن تفهم | افتح |
|---|---|
| كيف يُقرَّر أي أداة تُستدعى | `api/src/core/orchestrator/PlanningEngine.ts` |
| مسار البناء كاملاً | `api/src/modules/tools/definitions/ReactProjectTool.ts` |
| الخوادم الحقيقية | `api/src/modules/tools/definitions/ApiProjectTool.ts` |
| عين المتصفح | `api/src/core/quality/app-audit.ts` |
| عين الطرفية | `api/src/core/quality/terminal-audit.ts` |
| حلقة التحسين | `api/src/core/quality/improve-loop.ts` |
| حكم القبول | `api/src/core/quality/acceptance.ts` |
| ما يجب ألّا ينكسر أبداً | `api/src/__tests__/wiring-policy.test.ts` |

> **كل ملف في `core/` يبدأ بتعليق يشرح المشكلة الحقيقية التي وُجد لأجلها** — غالباً بكلمات المستخدم نفسه. اقرأ التعليق قبل الكود؛ هو نصف السبب.

---

## الخلاصة

جو **يكتب** النظام، ثم **يشغّله**، ثم **يقيسه بعينين حقيقيتين**، ثم **يصلح ويُثبت أنّ الإصلاح نفع بالرقم** — ثم يقول للمستخدم بالضبط ما أثبته **وما لم يُثبته**.

كل ذلك على جهازه. بلا خادم بعيد.
