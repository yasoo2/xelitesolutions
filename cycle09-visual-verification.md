# دورة 09 — سجل التحقق المرئي

## فحص الطرفية الصريح

أعيد تشغيل Joe من الحزمة المبنية حديثاً على `http://127.0.0.1:5002/joe` ثم أُنشئت جلسة جديدة وأُرسل طلب القبول العربي التالي:

> نفّذ فحصاً محلياً باستخدام `shell_execute` فقط: اعرض `pwd`، ثم نفّذ `node --version`، ثم `echo TERMINAL_VISIBILITY_TEST`. لا تبنِ تطبيقاً ولا تعدّل ملفات ولا تستخدم المتصفح أو Git. يجب عرض الأوامر ومخرجاتها الحقيقية في شاشة Terminal المرئية.

**النتيجة:** اكتمل التنفيذ في عشر خطوات خلال ست ثوان. عرضت المحادثة نواتج الأوامر الحقيقية، كما فتح Joe مساحة العمل وتبويب **Terminal** تلقائياً. ظهر في الطرفية المرئية، بالترتيب:

1. `$ pwd` ثم `/home/ubuntu/xelitesolutions/data/projects/my-workspace` ثم نجاح.
2. `$ node --version` ثم `v22.13.0` ثم نجاح.
3. `$ echo TERMINAL_VISIBILITY_TEST` ثم `TERMINAL_VISIBILITY_TEST` ثم نجاح.

لكل أمر ظهر أيضاً سجل `exec=<command> ... exit=0`. يثبت ذلك صحة سلسلة التخطيط والتنفيذ والبث والاستعادة المرئية بعد إصلاح توجيه الطرفية وتصنيف أوامر التشخيص المقروءة فقط.

## سيناريو البناء التالي

المشروع المناسب لاختبار بناء React حقيقياً هو:

`/home/ubuntu/xelitesolutions/data/projects/my-workspace/react-مدار-الحجوزات`

ويعرّف `package.json` فيه سكربت البناء `vite build` من خلال `npm run build`.

## فحص بناء مشروع قائم عبر الطرفية

أُرسل طلب مقيّد صراحةً لتنفيذ `npm run build` فقط داخل المشروع الموجود في `react-مدار-الحجوزات`، مع منع تعديل الملفات أو التثبيت أو المتصفح أو Git أو النشر.

**النتيجة بعد إصلاح مسار التخطيط:** في جلسة نظيفة، اختار Joe `shell_execute` بدلاً من `react_project`، وفتح تبويب **Terminal** تلقائياً. ظهرت فيه معلومات التنفيذ الحقيقية التالية:

```text
$ npm run build
> مدار-الحجوزات@0.1.0 build
> vite build
sh: 1: vite: not found
✗ exit 1
exec: npm run build (cwd=/home/ubuntu/xelitesolutions/data/projects/my-workspace/react-مدار-الحجوزات)
```

هذه نتيجة صحيحة ومرئية لفحص البناء المقيد: نُفّذ الأمر في المجلد المطلوب، لم يُنشأ مشروع بديل ولم تُعدّل ملفات، وعُرض فشل الاعتماد الحقيقي (`vite` غير مثبت محلياً) مع رمز الخروج `1`. سيُجرى بعد ذلك فحص بناء ناجح على مشروع محلي تتوافر له تبعياته لإثبات خرج النجاح أيضاً.

## إعداد اختبار البناء الناجح

بعد إثبات ظهور فشل الاعتماد الحقيقي في الطرفية، غُيّرت مساحة العمل **محلياً وبشكل مؤقت** من مجلد مشاريع الاختبار إلى `/home/ubuntu/xelitesolutions` عبر واجهة Joe. هذا الجذر يضم مجلد `web`، وهو واجهة React الخاصة بالمستودع وتوجد فيها تبعيات `vite` المثبتة مسبقاً. لا يتضمن هذا التغيير تثبيت حزم أو نشر أو تعديل مصدر المشروع؛ هدفه الوحيد هو تشغيل فحص بناء ناجح مقابل الفحص الفاشل السابق، ضمن حدود مساحة العمل التي تعرضها الواجهة.

## عيب مكتشف في احترام مساحة العمل المختارة

أظهر اختبار `web` خللاً حقيقياً في تمرير مساحة العمل من الواجهة إلى تنفيذ الطرفية: كانت الواجهة تعرض الجذر `/home/ubuntu/xelitesolutions` وتعرض مجلد `web`، لكن سطر الطرفية الفعلي سجل `cwd=/home/ubuntu/xelitesolutions/data/projects/my-workspace/web` وانتهى بـ`spawn /bin/sh ENOENT` لأن المجلد غير موجود هناك. مسار التخطيط اختار `shell_execute` بصورة صحيحة، لكن طبقة التنفيذ لم تحترم مساحة العمل المحددة للجلسة.

لذلك سيُعالج ربط معرف/مسار مساحة العمل بسياق تنفيذ `shell_execute` قبل استنتاج نتيجة اختبار البناء الناجح.

## التحقق المرئي النهائي بعد إصلاح مزامنة مساحة العمل

أُعيد تشغيل Joe من الحزمة المبنية، ثم استُخدمت جلسة متصفح جديدة ومساحة العمل المحلية `/home/ubuntu/xelitesolutions`. أُرسل طلب مقيد لفحص بناء مشروع React الموجود في `web` باستخدام `shell_execute` فقط، مع منع التعديل والتثبيت وGit والنشر.

أظهر تبويب **Terminal** تلقائياً الأمر `npm run build` ومخرجات Vite الحقيقية، وانتهى التنفيذ بالدلائل التالية:

```text
✓ 4268 modules transformed.
✓ built in 18.43s
✓
exec: npm run build (cwd=/home/ubuntu/xelitesolutions/web)
exec=npm run build server=local exit=0
```

وهذا يثبت أن Joe استخدم جذر مساحة العمل المختار، مرّر `cwd=web` بصورة صحيحة، وعرض الأمر والخرج النهائي ورمز النجاح في شاشة Terminal المرئية. لم يُنشئ مشروعاً جديداً، ولم يثبّت حزمًا، ولم ينفذ أي عملية Git أو نشر.
