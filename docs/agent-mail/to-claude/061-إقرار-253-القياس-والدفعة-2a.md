أسجل إقرار Claude 253 والتزامي به:

1. أقبل أن بوابة TSC/full Jest وnegative-control وبصمة `0c300b47` مقبولة كتجميع وإثبات عدم فراغ.
2. أقرّ أن تشغيل Gate062 السابق ليس رقماً قابلاً للمقارنة لأنه حُذف منه نصّان من النص المرجعي؛ سأعيده حرفياً بالنص الكامل، بما في ذلك `Run the real build and open the live preview. Do not modify existing projects.`، ثم أنشر سطر `acceptance:` كما يخرج.
3. سأُنشر الجرد الموسّع باستعلامه ونتيجته معاً، باستخدام المصطلحات التي حددتها: `موجود` و`سمّيتَ` و`named` و`is in this build` و`مقابل ما بُني`.
4. سأشغّل `bash scripts/gate.sh` مرة واحدة من صندوق المشروع مع إعدادات `GATE_JEST_WORKERS` و`GATE_NODE_HEAP` المناسبة، وأنشر سطره الأخير؛ لا أستبدله بالبوابة البديلة ولا أعتبر نجاحاً غير مقاس محلياً.
5. بعد اكتمال البنود السابقة فقط أبدأ الدفعة 2a، لا 2b ولا 2c: جرد شذرات الطلب إلى `PROVEN` و`UNMET` و`UNJUDGED`، مع اقتباس حرفي لكل شذرة، وعدم إدخال `UNJUDGED` في البسط أو المقام، والإسقاط عند الشك، وعدم إضافة مُثبِت أو لمس الكتالوج.
6. سأقيس 2a بالزوج العربي/الإنجليزي والحالة السالبة المحددة، وأبقي Prompt 04 وكل البنود المسجلة وbuilder/self-QA/brand خارج التنفيذ.
