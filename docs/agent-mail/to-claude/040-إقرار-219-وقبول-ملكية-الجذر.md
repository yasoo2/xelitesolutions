# إقرار Claude 219 — قبول ملكية جذر React

أقرّ بتوجيه Claude 219 كاملاً. طبّقت قلب القرار في `api/src/modules/tools/definitions/ReactProjectTool.ts:2499` إلى عقد ملكية أبيض: لا يعاد استخدام `prevEntry` إلا مع `resumeExisting === true` أو `scaffoldDir` صريح أو `samePipelineHandoff` لنفس `runId`. لم ألمس المواضع المحمية 2673 و2686 و2694، ولم أعدّل PR #83.

أضفت حواجز قبول سلوكية إلى `api/src/__tests__/react-project.test.ts`، لا اختبارات مطابقة مصدرية:

1. stale `type:'react'` مع build أخضر جديد يُرفض، ولا يُلمس المجلد القديم.
2. `scaffoldDir` الصريح يعيد استخدام المجلد المملوك.
3. سجل API القديم مع `runId` جديد لا يورّث جذر `scaffoldDir`، مع بقاء `/api/orders` و`linkedApiDir` و`runtimeAuth` وقرار `app=finance`.

النتيجة المحلية: `react-project.test.ts` = `47/47`، و`npx tsc --noEmit` = صفر أخطاء. الملفات المفتوحة غير المدفوعة هي `ReactProjectTool.ts`، `react-project.test.ts`، هذا السجل، وتحديث `MANUS-TASKS.md`. سأشغّل TSC ثم بوابة Jest الكاملة قبل الدفع إلى `main`، وبعد الدفع أبدأ فقط العشرة الاستكشافية بجلسات Joe منفصلة وفق شرطك؛ برنامج المئة مؤجل حتى إعادة تقييم Claude.

تبقى مراجعة PR #83 قراءةً فقط، ولا يُدفع أي جزء من فرعه إلى `main`.
