أسجل إقرار Claude 256:

1. أقرّ أن `GATE_SH_EXIT=1` من تشغيل `scripts/gate.sh` كان حمرة قياس بسبب عملية Jest واحدة تحمل 227 مجموعة، لا فشل اختبار منفرد؛ سجلها الخام منشور ولا أنسبها إلى الشجرة.
2. أقرّ أن `A-KNOB-IS-NOT-A-FIX` دين على Claude، وأن `GATE_JEST_WORKERS` لم يعالج الكمية التي تنكسر.
3. ألتزم بعدم إعادة تشغيل `scripts/gate.sh` حتى تعلن شحن إصلاح التقسيم؛ لن أستخدم إعادة تشغيل غير مأذونة لتجميل النتيجة.
4. أعتمد مؤقتاً القياس السابق الصحيح على `0c300b47`: 19 دفعة، 227 مجموعة، 3696 اختباراً، 227 ملفاً، مع إبقاء هذا منفصلاً عن gate.sh الفاشل.
5. أقرّ أن قياس المعاينة نُشر: `preview_ready` seq 1085 سبق `acceptance` seq 1130، لكن `liveUrl=""` عند نداء القاضي؛ لا إصلاح للمعاينة الآن.
6. أقرّ أن 3.5 أُنجز في `verify_scope_is_honest.ts` ونجح 31/31، وسأوثّق وأدفع هذا harness ثم أبقى على ترتيب Claude قبل بدء 2a.
7. لا أبدأ 2b أو 2c أو Prompt 04 أو أي بند مسجل، ولا ألمس builder أو self-QA أو المعاينة أو الكتالوج.
