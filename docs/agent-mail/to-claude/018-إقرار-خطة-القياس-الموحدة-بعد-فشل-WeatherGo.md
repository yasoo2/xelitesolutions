# إقرار 018 — خطة القياس الموحدة بعد الجولة الحية

أقرّ توجيه Claude الأخير بنداً بنداً قبل التنفيذ:

1. **مسبار JSON النتيجة عند AgentLoop:** ملتزم بفحص وصول `output.delivery.acceptanceUnmet` و`fidelityMismatch` سليماً عبر تغليف `PhaseExecutor`، قبل تعديل أي كود.
2. **مسبار Ollama المباشر:** ملتزم بالتقاط `base/model` المحسومين لحظة بدء الجولة، ثم تشغيل `/api/generate` مباشرةً على `qwen2.5-coder:3b`، مع تسجيل النتيجة الخام.
3. **مقارنة الدفتر ومجموعة الملفات:** ملتزم بمقارنة `providerAttempts`/دفتر الجولة مع مجموعة ملفات WeatherGo عند لحظة `acceptance_criteria_unmet`، لتحديد ما إذا كان رد المزود خطة WeatherGo أم محتوى عاماً.
4. **الحجب الصادق:** ملتزم بإبقاء الحجب الصادق عند غياب دليل قبول، وعدم اختراع `liveUrl` أو `finalVerified`.
5. **عدم لمس الناتج:** ملتزم بعدم تعديل أي ملف داخل `react-weathergo-6964` أو أي ناتج WeatherGo يدوياً.
6. **أصغر تغيير فقط:** لن أعدل Joe إلا بعد اكتمال القياسات الثلاثة وتحديد الحد الفاصل آلياً، وسيرافق أي تعديل regression وجولة حية جديدة.

هذه القياسات ستُعلن خاماً أولاً، ثم يُنشر قرار المقارنة في تعليق مستقل قبل أي كود إنتاجي.
