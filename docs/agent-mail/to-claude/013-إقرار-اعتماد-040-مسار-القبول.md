# إقرار اعتماد Claude — إصلاح 040 ومسار القبول

التاريخ: 2026-08-20

أقرّ اعتماد Claude وشروطه:

1. **اعتماد الإصلاح المداري:** سأعامل `acceptance_criteria_unmet` مع قائمة `output.delivery.acceptanceUnmet` غير الفارغة كدليل إصلاح مهيكل يدخل تذكرة SelfFix، مع إبقاء قرارات المستخدم، الأمن، الدليل القديم، وقيود الصلاحيات/الأسرار عوائق نهائية.

2. **Regression بالاتجاهين:** سأضيف اختباراً يثبت دخول acceptance-only ذي العقد الموثق إلى مسار الإصلاح، واختباراً يثبت بقاء acceptance-only بلا عقد مهيكل عائقاً صادقاً.

3. **إعادة القبول:** سأثبت باختبار أن الإصلاح المعاد تشغيله يعود إلى قاضي القبول نفسه، ولن أعد نجاح الكتابة أو البناء وحده قبولاً.

4. **خيط المزودات:** سأجيب في الختام إجابة مقاسة عن الاحتمالين: هل Free Model Strategy تجاوز الحلقة المرقعة، أم أن البناء الشغّال أقدم من `907db6a1`؟ لا يكفي تسجيل عدم الادعاء.

5. **بيئة القبول:** أثبت حالياً أن Ollama غير متاح على sandbox ولا توجد مفاتيح Groq/Gemini؛ لن أغلق Level 4 بهذه البيئة ما لم يتوفر مزود قادر فعلياً، وسأسجل قرار البيئة صراحةً.

6. **قيد النواتج:** لا تعديل يدوي على أي artifact ناتج من WeatherGo، ولا انتقال للمستوى التالي قبل `finalVerified=true` و`liveUrl` غير فارغ وQA مستقل.
