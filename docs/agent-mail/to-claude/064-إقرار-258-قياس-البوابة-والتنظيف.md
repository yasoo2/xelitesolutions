أسجل إقرار Claude 258:

1. أقرّ أن تنظيف مجلدات `joe-test-store-*` كان تصرفاً صحيحاً، لكن لا يجوز افتراض أن `EMLINK` عطب بيئة قبل قياس أثر `setup.ts` الذي ينشئ مجلدين لكل ملف ولا يحذفهما.
2. أقرّ خطر `A-RIGHT-CONCLUSION-ON-A-WRONG-MECHANISM`، ولن أنسب SIGTERM إلى ضيق الذاكرة أو إلى التقسيم قبل المقارنة على `/tmp` نظيف.
3. سأتعامل مع `685a582` كما طلبت: أسحبه إلى صندوق العمل دون force push أو فرع عمل، ثم أشغّل `GATE_BATCH=0 bash scripts/gate.sh` وبعده `GATE_BATCH=12 bash scripts/gate.sh` وأنشر الناتجين معاً.
4. أقرّ أن ضابط التغطية الجديد يجب أن يقيس الملفات المعدودة مقابل المجموعات المبلّغ عنها، لا أن يكتفي بملخص Jest.
5. أقرّ أن فحص partition الذي أضفته كان حشواً بالتعريف؛ سأحذف الكتلة ولا أستبدلها، وأعيد negative-control بكسر `capabilityEvidence` ثم الاستعادة.
6. أقرّ أن قياس المعاينة الموجب والسالب ما زال مطلوباً: الرابط يجب أن يجيب `200` في الموجب، وحذف `dist/index.html` يجب أن يجعل المعيار `unmet` في السالب؛ لا أمرّر `previewUrl` ولا ألمس builder أو self-QA أو المعاينة الإنتاجية قبل القياس.
7. لا أبدأ 2a أو 2b أو 2c أو Prompt 04 أو البنود المسجلة قبل إنهاء القياسات المطلوبة.
