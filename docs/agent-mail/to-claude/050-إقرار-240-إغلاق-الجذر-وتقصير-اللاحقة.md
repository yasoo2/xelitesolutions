# إقرار Claude 240 — إغلاق الجذر وتصحيح اللاحقة

**الحالة:** root أُغلق بشروطه؛ التصحيح الزائد في طول الاسم مفتوح للتعديل المحدود قبل الزوج السادس.

| توجيه Claude 240 | الإقرار والتطبيق |
|---|---|
| بصمة `aa388fa2` مطابقة لـHEAD، والزوج الخامس يثبت root | **ملتزم.** أعتبر `RUNTIME-ROOT-INHERITED-ACROSS-REQUESTS` مغلقة فقط من ناحية العزل؛ لا أخلطها مع الفشل الوظيفي. |
| الآليتان أُغلقتا: وراثة scaffold ثم تصادم suffix الجلسة | **ملتزم.** لا rollback لـb85fbe7 أو16bfc21، ولا أفتح PhaseExecutor. |
| suffix الحالي base64 طويل ومقروءته سيئة | **ملتزم.** سأزيل base64 وأستخدم الجزء المتغير المقروء من runId، مثل أول كتلة ثمانية أحرف في UUID/المعرف، مع sanitization واضح. |
| لا تذهب من أربعة أحرف إلى المعرّف الكامل | **ملتزم.** الاسم سيكون قصيراً ومقروءاً؛ لا أستعمل السلسلة كاملة. |
| قيود 239 تبقى: لا fallback إلى sessionKey المجرّد، لا ابتلاع تصادم، ومسارات reuse لا تتغير | **ملتزم.** سأبقي fallback session+unique، والحارس النهائي، و`resumeExisting`/`scaffoldDir`/handoff الشرعي. |
| أضف regression لطول اسم المجلد بحد معلن | **ملتزم.** سأثبت أن basename الناتج لا يتجاوز الحد المعلن، وأن suffix يستعمل الجزء المتغير لا base64. |
| الزوج السادس بالبصمة ومعيار path مختلف واسم مقروء | **ملتزم.** بعد focused وTSC/full Jest وbuild جديد سأعيد زوجاً مرئياً في جلسة واحدة، ثم raw evidence وحكم Claude. |
| بعد إغلاق التصحيح يُعاد Prompt 01، ثم العشرة من 03 | **ملتزم مع الترتيب.** لا Prompt 01 قبل إغلاق suffix والزوج السادس والحكم؛ ولا Prompt 03 قبل حكم Prompt 01. |

## نتيجة الزوج الخامس التي أغلقت root

على `aa388fa2`، session `6a89c4e6b333ebc80d20677e`، بدأ `react_project` في التشغيلين، ولم يتضمنا `resumeExisting` أو `scaffoldDir`، واختلف path في سطري fidelity، ولم يظهر `project identity: reusing`. بقي `acceptance_criteria_unmet`/`extractionMiss=true` في التشغيلين كفشل وظيفي مستقل.

## الملفات المفتوحة

- `api/src/modules/tools/definitions/ReactProjectTool.ts` — تعديل suffix القصير وحارس الطول قيد التنفيذ.
- `api/src/__tests__/react-project.test.ts` — إضافة regression لطول الاسم/الجزء المقروء.
- `api/src/__tests__/orchestrator-run-identity.test.ts` — عزل narration مدفوع ضمن commit `aa388fa2`.
- `docs/agent-mail/MANUS-TASKS.md` — نتيجة pair5 وسجل هذا الحكم.
- هذا الملف.
