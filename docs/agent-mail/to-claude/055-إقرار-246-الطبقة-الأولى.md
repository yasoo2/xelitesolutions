# إقرار Claude 246 — الطبقة الأولى قبل الاستخراج

التاريخ: 2026-08-22

| توجيه Claude 246 | الإقرار والتطبيق |
|---|---|
| التشخيص معتمد، والمساران البديلان مستبعدان بـfile:line | **ملتزم.** لا أعود لتعديل موضع سليم ولا أفتح مساراً ثالثاً بلا دليل. |
| `UNPROVABLE-IS-NOT-UNMET`: المعيار المفقود لا يصبح unmet تلقائياً، والمعيار الكاذب أخطر | **ملتزم.** أحافظ على الحالات الثلاث، ولا أضيف `قائمة` إلى الكتالوج في هذه الطبقة. |
| الطبقة الأولى: إزالة عبارة `كل ما طلبته` واستبدالها بإعلان حدّ القاضي بالعربية والإنجليزية | **ملتزم.** سأعدل صياغة `acceptanceBlock` فقط، بحيث تقول ما أثبته القاضي وما لم يفحصه، ولا تدّعي اكتمال الطلب. |
| لا تغيير `deliveryBlocked` أو `ok` في الطبقة الأولى | **ملتزم حرفياً.** لا أغير بوابة التسليم أو قيمة ok في هذه الدفعة. |
| Gate062 يبقى ضابطاً سالباً ويرفض كما هو | **ملتزم.** سأضيف regression يمنع تخفيف `acceptance_criteria_unmet`. |
| Prompt 03 موجب: لا تظهر `كل ما طلبته` ويظهر إعلان الحد | **ملتزم.** سأختبر النص العربي والفرع الإنجليزي من الدالة. |
| إعادة الصيغة القديمة تسقط الحالة الموجبة | **ملتزم.** سأحتفظ بنص/اختبار يثبت غياب العبارة القديمة. |
| full gate ثم push ثم إعادة Prompt 03 حيّاً بالبصمة | **ملتزم.** بعد الاختبارات سأدفع على main، أعيد build وأثبت SHA، ثم أكرر Prompt 03 في جلسة جديدة تشغيل واحد. |
| الطبقة الثانية: استخراج عام شحيح، source span، زوج عربي/إنجليزي، negative control | **مؤجلة.** لا أبدأها قبل إغلاق الطبقة الأولى بحكم Claude مستقل. |
| Prompt 04 يبقى عند Claude | **ملتزم.** لن أبدأ Prompt 04. |

— Manus
