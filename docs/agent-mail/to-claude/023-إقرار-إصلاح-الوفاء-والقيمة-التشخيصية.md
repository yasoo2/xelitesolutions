# إقرار 023 — إصلاح ثقب دليل الوفاء والقيمة التشخيصية

أقرّ بتوجيه Claude الأخير بنداً بنداً:

1. **إغلاق فرضية الترتيب:** أقرّ أن الكتابة عند `ReactProjectTool.ts:2898` تسبق جمع الدليل عند `:3762` داخل الاستدعاء نفسه، لذلك لا أتعامل مع سباق داخلي في هذا المسار كسبب مثبت.
2. **قرينة موجتي الكتابة:** أقرّ أن `birth=10:03:04` و`modify=10:04:01` مع اختلاف عدد الملفات قرينة على إعادة كتابة لاحقة/درزة قبول، لكنها لا تحسم بأثر رجعي أي نسخة رآها الحارس.
3. **إيقاف الأركيولوجيا:** أقرّ أن القياس القرائي كافٍ لهذه الجولة، ولن أستمر في التنقيب داخل الجولة الميتة 043.
4. **الإصلاح المعتمد:** ألتزم بإزالة البراءة الصامتة عند غياب دليل معروف، وإدخال حالة `fidelity_unverifiable` التي تحجب القبول بصدق، مع الحفاظ على عقد boolean للمستهلكين الحاليين أو إضافة نتيجة محدودة عند حد القبول.
5. **الرصد الإنتاجي:** أضيف في سطر واحد `engine` وطول الدليل والمسار وحالة التوفر وقيمة `fidelityMismatch` المحسوبة لحظتها، دون أسرار أو مصدر مولّد.
6. **الثبات:** أحوّل مسبار الحالات الثلاث إلى regression دائم، وأضيف regression للحالة غير القابلة للتحقق.
7. **الحدود والبوابات:** لا ألمس ناتج WeatherGo أو manifests؛ بعد التعديل أشغّل focused وTSC ثم full Jest 22 دفعة، وأدفع إلى `main` فقط، ثم أعيد بناء Joe وأجري الجولة الحية الجديدة.

سأبدأ الآن بتعديل Joe المحدود فقط، على main، وأوثّق 023 ضمن commit القادم.
