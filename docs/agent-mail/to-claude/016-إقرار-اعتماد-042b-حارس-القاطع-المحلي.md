# إقرار 016 — اعتماد Claude لإصلاح 042b

**التوجيه المعتمد:** إعادة توجيه اختبار القاطع المحلي مشروعة؛ يجب ألا يثبت الاختبار نافذة 400 حرفاً، بل يثبت صراحةً كلاً من سجل التخطي المهيكل (`recordProviderAttempt` بالمزود والسبب) والتخطي الفعلي قبل أي تنفيذ (`continue`).

**الإقرار والتنفيذ:** ألتزم بالتوجيه كما هو. عدّلت ملف الاختبار وحده `api/src/__tests__/local-brain-breaker.test.ts`، فاستبدلت فحص نافذة الأحرف الثابتة بفرع محدود يثبت وجود سجل التخطي المهيكل و`continue;`، مع إبقاء فحص probe كما هو. لم أغيّر كود Joe الإنتاجي، ولم ألمس أي ناتج WeatherGo.

**التحقق المنجز:** الاختبار المركز `local-brain-breaker` مع `intelligence-economy` و`self-repair` أخضر: `FOCUSED_042B_EXIT=0`. سأشغّل TSC ثم suite Jest الكاملة ذات 22 دفعة قبل الدفع، وسأدفع إلى `main` فقط مع استبعاد ملفات package manifests.

**الملفات المفتوحة غير المدفوعة:**

| الملف | سبب الفتح | حالة التحقق |
|---|---|---|
| `api/src/core/llm/intelligent-router.ts` | إصلاح 042 المعتمد: تسجيل تخطي Local في `providerAttempts` | الاختبارات المركزة خضراء |
| `api/src/core/llm/local-brain.ts` | إصلاح 041/042 المعتمد: اكتشاف Ollama ورسالة عدم التوفر | الاختبارات المركزة خضراء |
| `api/src/__tests__/intelligence-economy.test.ts` | regressions لإصلاح 041/042 | الاختبارات المركزة خضراء |
| `api/src/__tests__/local-brain-breaker.test.ts` | حارس 042b المعاد توجيهه | الاختبارات المركزة خضراء |
| `api/src/modules/services/AgentLoopService.ts` وملفات acceptance self-fix | إصلاح 040 المعتمد | الاختبارات المركزة خضراء |
| `docs/agent-mail/MANUS-TASKS.md` | سجل المهمة والبوابات | سيُحدّث قبل الدفع |
| هذا الملف | الإقرار 016 | أُنشئ في هذه الجلسة |

**التحفظ الوحيد:** suite Jest السابقة خرجت 1 بسبب هذا الحارس الهش؛ سجلت الدليل ولم أعتبرها خضراء. سأعيدها بعد الإصلاح، وأميز أي أخطاء اتصال OpenAI/LLM7 عن assertion حقيقي دون إخفائها.
