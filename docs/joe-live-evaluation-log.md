# Joe Live Evaluation Log

The entries below are executed through the Joe UI at `http://localhost:5002/joe`, one prompt at a time. The log distinguishes the first observed failure from the repaired rerun.

| Prompt | Task | First result | General repair | Rerun evidence |
| --- | --- | --- | --- | --- |
| P01 | List top-level workspace files and summarize README without changes | Passed | None | Chat gave a concise summary; discovery details stayed in Logs |
| P02 | Create `joe-prompt-02.txt` with three exact lines, read it back, and report it | Failed: negated “do not change other files” was treated as a whole-request read-only guard; a retry also exposed slow generic planning | Context-aware mutation detection plus explicit file-contract planning | File exists with exact three lines; chat reported content; no project scaffold was created by the passing run |
| P03 | Create a folder and a file inside it, then read it back | Failed: the file name was written at workspace root and the folder relationship was dropped | Preserve explicit folder + “inside it” context in the reusable file-path parser | `joe-prompt-03/README.txt` contains the exact two lines; root `README.txt` is absent |
| P04 | Run a read-only local diagnostic and report Node.js version plus workspace path | Failed: the read-only classifier missed the terminal-diagnostic route and entered slow model analysis; run was stopped before any action | Add a generic bounded terminal-diagnostic classifier, preserve read-only safety, and map semantic version/path requests to an allowlisted command plan | Rerun through Joe UI displayed `v24.18.0` and the real workspace path in chat; Terminal received `node --version` and `pwd`; run finished with no writes, installs, or server start |
| P05 | Read two named files, compare their exact content and line counts, and report marker status | Failed: first rerun stopped because multi-project discovery demanded an explicit project; second rerun read both files but chat omitted line counts and marker limits | Add explicit read-file targeting in the active workspace and a structured multi-file report that never invents an unspecified marker | Final rerun read both files through `read_file`; chat showed `3` and `2` lines, exact content, and clearly stated that no marker value was supplied; no writes occurred |
| P06 | Read two named files, report exact content and line counts, and verify two supplied markers | Failed: the report initially treated explicit markers as unspecified and displayed “the selected file” instead of real file names | Extract marker contracts, pass them with each read step, calculate PASS/FAIL from content, and use the trusted input path in the report | Final Joe UI rerun showed both real filenames, `3`/`2` lines, both markers as `PASS`, exact content, and no modifications |
| P07 | Read one named file and verify a marker that is intentionally absent | Passed | None | Joe UI reported the exact `3`-line content and `Marker: FAIL` for the absent marker; no writes occurred |
| P08 | Inspect a named directory, list entries, and confirm `README.txt` exists inside it | Failed: the file extractor treated `README.txt` as a root file, producing `File not found` and an unnecessary search/model detour | Add a generic explicit-directory contract before file extraction, preserve the expected entry, execute `inspect_directory`, and compose a directory evidence report | Rerun through Joe UI completed in 5 steps; Logs showed the real directory path, chat listed `[file] README.txt`, confirmed it exists in `joe-prompt-03`, and stated no files were modified |
| P09 | Build a polished request-derived repair-shop React app with forms, filters, cart-like interactions, responsive layout, and browser QA | Blocked: the first two runs stopped on provider/invalid-artifact conditions; the repaired run produced a valid request-derived project but stopped during `npm install --offline` before Joe delivered verified browser evidence | Permit the bounded request-derived engine to recover an incomplete provider artifact, propagate Stop into the child process tree, keep the delivery gate honest, and avoid a second long provider wait when a verified request-derived engine is available | The fresh rerun created `react-customer-6ce4f1b0`, served `http://localhost:4300/`, compiled the project, and completed real Browser QA at `89/100` with 7 measured interactions and 0 blocking findings. It found two unresponsive generated controls (`Export CSV`, `Undo`) and did not independently prove empty-name validation or status filtering. P09 is therefore still not a clean pass; the defects and evidence gap remain open |
| P10 | Build a request-derived personal reading log with title, author, pages, dates, rating, status, filters, and progress metric | First rerun exposed a false duplicate acceptance criterion, a noisy concatenated row label, and a narrow-screen overflow finding | Deduplicate acceptance against the request, prefer semantic control names, recover stale modal state before measuring reachability, and harden the generated app shell against horizontal spill; replace the stacked neural card with a quiet live line and optional details | Final rerun created `react-status-and-1442bc34`, built `dist`, and completed real Browser QA at `100/100`: 1 measured runtime/network check, 3 control/form checks, and 3 visual/accessibility/responsive checks across 3 viewports, with 11/11 acceptance criteria proven. The run honestly recorded that offline npm installation timed out, then reused a verified local React toolchain and completed the production build. The remaining declared capability was `reviews and ratings`, which was not part of the request. |
| P11 | Build a small event-planning dashboard with event title, date, location, attendee capacity, RSVP status, search, status filter, capacity summary, responsive layout, and browser QA | First rerun exposed a false scope report for the responsive requirement and a real narrow-screen overflow finding | Propagate the complete user request into the builder and scope audit, add responsive evidence detection, and harden the generated mobile shell against long labels and horizontal spill | Fresh rerun through the Joe UI created a request-derived dashboard, served `http://localhost:4301/`, and completed real Browser QA at `97/100`: runtime/network passed, controls/forms passed with 8 measurements, and visual/accessibility/responsive passed across 3 viewports. It recorded 6 controls pressed and one form filled/submitted with 5 fields. The only remaining finding was non-blocking: one dynamic control disappeared before it could be pressed. |
| P12 | Build a school equipment checkout system with required student, equipment, checkout date, return date, condition, submission prevention, and a clear error summary | First reruns exposed false payment classification, missing form-validation delivery mapping, and a contradiction where acceptance was `7/7` but the broader gap reporter still blocked on “clear error summary” | Separate equipment checkout from payments, add source-backed form-validation acceptance, register the delivery topic, and reconcile proven acceptance gaps before the hard delivery gate | Fresh rerun through the Joe UI created `react-myapp-49b2ddb9`, served `http://localhost:4300/`, and completed real Browser QA at `97/100` with 0 blocking findings. The final delivery was accepted with `7/7` acceptance criteria proven; the remaining low finding was the known dynamic-control reachability issue. The run also exposed the environment’s slow offline/provider fallback path, which Joe reported instead of hiding. |
| P13 | Create a four-page science museum website: Home, Exhibits, Visit, and Education, with shared header, active-page indicator, internal links, and a Visit contact form | First rerun exposed a false textarea semantic match (`message` matched `age`), a divergent repair build path, intermittent viewport instrumentation, a fragmented mobile header, and a session refresh that could leave the completed chat visually empty | Use word-boundary field semantics, reuse the proven build helper for repair/rollback, wait and retry viewport application, detect/repair fragmented mobile headers, preserve API project identity over generic `custom`, and restore persisted chat history on session refresh without clearing the active conversation | Final rerun through the Joe UI built `react-science-museum-00f00d5b`, verified all four routes, exercised 23 controls and a 3-field form, challenged one semantic field, measured desktop/tablet/phone, and returned `100/100` with runtime/network, controls/forms, and visual/accessibility/responsive checks passing. Acceptance was independently proven at `5/5`; the Visit contact form was present in generated source and actually submitted during browser QA. |
| P14 | Build a request-derived Arabic personal-expense application with numeric/date validation, filtering, totals, edit/delete confirmation, persistence, responsive UI, and exploratory Browser QA | The real Joe UI accepted the prompt, but the provider mesh exhausted its available providers during the long planning/build path; the API became unresponsive to `/api/health` while the run remained marked `running`, so no completion or QA pass was claimed | Recovered the service, preserved the honest failure, and added a general reserved time slice so semantic form QA cannot be starved by control exploration | Not passed. The run was terminated after verified API unresponsiveness; provider outages/timeouts were recorded in `joe-5002-live.err`. The same task must be rerun after a provider-timeout/recovery improvement. |
| P15 | Open `http://127.0.0.1:5000/api/health` in Joe's Browser, report the displayed status, and make no file changes | Failed: the URL was parsed as the local filename `127.0`; Joe entered a read/search detour and Browser showed `No page loaded` | Remove complete URLs before local-file extraction, prioritize explicit URL observation ahead of the broad read-only workspace route, and let `browser_launch` return bounded visible page text | Passing rerun through Joe's real command box opened the exact health URL in the visible Browser panel and reported `status: OK`, `database: LOCAL`, and `singleUser: true` in chat. The focused routing suites passed `25/25`, the API production build passed, and all ten permanent architecture/self-healing commands passed. No file tool was selected by the repaired run. |
| P16 | Append one exact fourth line to an existing three-line file, preserve prior content, read it back, and report the final line count | The first attempt targeted a file that belonged to an older isolated workspace. The read correctly failed, but Joe hid that precondition behind a provider-backed `central_answer` detour and ultimately reported provider failure | Add a deterministic append-to-existing contract, workspace-contained/idempotent append mode, line-count preconditions, non-recoverable precondition handling, and a verified write/read delivery report | In the current guest workspace Joe first created `joe-append-proof.txt` with three exact lines, then accepted the short follow-up in the same conversation. The UI visibly reported `Final line count: 4` and showed all four lines; direct disk inspection matched. A fresh missing-file rerun stopped in 15s with an English, target-specific message, did not call a provider, and created nothing. Focused tests passed `7/7`; API/web builds and all ten permanent gates passed. |
| P17 | Continue the current project: rename the brand from `مشروعي` to `بصيرة`, add a Services navigation link and a three-service section before Contact, change the primary buttons to emerald, keep the same project, and verify the link in Browser QA | Early runs exposed three general defects: the clause reader rendered the quoted brand instruction as content, hidden reveal sections could pass source-only acceptance, and the live JPEG capture loop raced viewport changes so Joe sometimes reported requested phone/tablet sizes while the page remained desktop-sized | Make edit parsing clause-local, require visible anchor-target evidence, preserve the current project for short compound follow-ups, pause live capture across each viewport transition, and publish only dimensions read back from the page after verification | The exact prompt was replayed through Joe's real command box. The final run changed no further files because the repaired project already satisfied the request, built successfully, proved `8/8` acceptance criteria, and returned Browser QA `100/100` across 3 measured viewports, 27 discovered controls, a submitted 3-field form, and one semantic-field challenge. An independent visible preview followed `#services`, showed exactly three service cards before Contact, and at `390px` measured no horizontal overflow. An isolated manager test with live streaming active also proved the sequence `1280→390→820→390→1280` with `window.innerWidth` matching every transition. |
| P18 | Continue the same project without scaffolding: add a text mark beside `بصيرة`, add a hero `احجز استشارة` CTA to the contact form, add a required telephone field that rejects letters, and test the link plus valid/invalid values in Browser QA | The first run was misclassified as read-only because `دون إنشاء` negated the whole Arabic brief; later reruns built and measured `100/100` but acceptance remained `5/6` because in-page anchors were checked then discarded from structured control evidence, while Arabic-only field labels were not recognized by semantic QA | Scope negation to its verb, add deterministic compound presentation edits, preserve verified in-page anchor targets as structured evidence, recognize Arabic email/telephone/date/time/number labels, and log the exact acceptance evidence used | The exact Arabic prompt was replayed through Joe's real command box on Auto. Run `run-1788846533077` kept the same project, built successfully, returned Browser QA `100/100`, and proved `6/6` acceptance. Evidence records `احجز استشارة` resolving to `#contact`, four filled fields, `رقم الهاتف` as native `tel`, invalid-value rejection with `[0-9+ ]{7,20}`, and zero semantic validation failures. Focused suites passed `57/57`; all ten permanent architecture/self-healing gates passed. |
| P19 | Continue the same project without scaffolding: add a three-question FAQ and navigation link, keep only one answer open at a time with keyboard closing, and test the link and disclosure behavior on phone and desktop | Early runs exposed Arabic mutation text being treated as read-only, a provider-recovery path that could rebuild instead of edit, source-only disclosure acceptance, absolute Vite asset paths that left static preview blank, loss of the FAQ anchor from phone evidence, and a duplicate responsive pass that could exhaust the shared QA budget after phone coverage had already completed | Normalize Arabic mutation intent, keep project-edit verification final, add stateful disclosure evidence with real Enter-key closing, build portable relative-asset previews, preserve anchors in the dedicated phone pass, and make each completed route/viewport own its evidence so duplicate sweeps are skipped | The exact prompt was replayed through Joe's command box. The final run kept `react-مشروعي-48eeedb0`, needed no additional source changes, built successfully, returned Browser QA `100/100`, and proved `5/5` acceptance. The audit exercised 11 controls, filled/submitted a four-field form, challenged two semantic fields, discovered 48 controls, performed 23 exploratory actions, visited 14 states, and measured three viewports. Independent visible checks opened all three FAQ items one at a time, closed the last with Enter, and showed the generated page inside Joe's fixed phone viewport without horizontal clipping or overlap. Focused suites passed `206/206`; the API build and all ten permanent architecture/self-healing gates passed. |
| P20 | Continue the same project without scaffolding: add a consultation-cost calculator with service choice, hours, live price plus 16% tax, Reset, and Browser QA for every choice, zero, a large value, negative rejection, phone, and desktop | The first run misrouted the word `السعر` to the named-row price editor; the next model-backed attempt changed nothing. After deterministic generation, Browser QA proved every requested behavior but a generic total criterion could not recognize arithmetic outside a row reduction, while a blind click on an already-reset Reset button created a false dead-control finding | Require an actual row-edit verb before named-row routing, add a provider-independent calculator edit, add state-aware arithmetic and boundary evidence, recognize a rendered calculator total as computed evidence, and exclude calculator controls from the blind baseline walk when the dedicated probe owns stronger evidence | The exact prompt was replayed through Joe's real command box. Final run `run-1788856943589` kept `react-مشروعي-48eeedb0`, built successfully, returned Browser QA `100/100`, and proved `6/6` acceptance. Evidence covered all 3 service choices, `0`, `9999`, `-1`, exact subtotal/tax/total arithmetic, Reset, and `1280x900`, `390x844`, and `820x1180`. Independent visible interaction changed service 3 to two hours and observed `500 + 80 = 580`, then Reset restored service 1, one hour, and `139.2`. Focused suites passed `132/132`; full permanent gates are recorded with the batch commit. |
| P14 retry preparation | Re-run the same expense prompt with `OFFLINE_MODE=true` after bounding the Local (Auto) provider deadline | Prior run could wait up to ten minutes inside local planning even when network providers were intentionally disabled | Added guarded offline local deadline and verified `52/52` focused tests plus production build; the live 5002 process still needs a controlled restart before this retest can be authoritative | No pass claimed until the restarted 5002 UI completes or fails fast with a clear local-provider result |
| P14 Browser-eye retest | Re-run the same expense prompt with Browser visibly open and keep the live QA stream in front of the user | The UI switched from Browser to Preview/Terminal during QA, disconnected the watcher, and falsely made visual QA impossible | Added a session-scoped Browser visibility lock during `self_qa`; web production build passed, commit `e1c27fbf` was pushed and verified on GitHub main, and the real rerun logged `watching=true` | Still not passed: the same run measured only 1 page/1 viewport and remained at `86/100` with `qa_budget_exhausted`, missing `h1`, and weak heading hierarchy; the next fix must improve generated-app visual repair and coverage without claiming a pass |

## Session-isolation finding

During the second prompt, selecting a GitHub repository and pressing `Done` while the prompt was running exposed a session-boundary defect: the manual repository sync inherited the active prompt's `sessionId`, so its operational line (`المستودع … موجود محلياً — أجلب آخر التحديثات…`) could be attributed to that conversation. The UI now performs GitHub-panel sync without the active prompt id; the line remains a Logs/Terminal concern when explicitly addressed and cannot become a conversational answer. The regression is covered by `github-connect.test.ts`.

The live retest reproduced the user action while another prompt was executing: open GitHub, select `yasoo2/kkkk`, and press `Done`. The request contained no prompt session id, and the chat remained free of the repository-sync line. When the local API had no persisted token after restart, the physical sync returned an error; the UI cleared the stale repository state and reopened GitHub with the recovery message instead of silently claiming success.

## Latest P09 Retest Notes

- The browser-visible neural surface is now a single live sentence with the stage names `Understand · Plan · Build · Test · Repair · Verify`; the long trace is opt-in and the technical stream remains in Logs.
- The run exercised the embedded Browser panel and the generated preview at `http://localhost:4300/`. The preview was reachable and the QA report was evidence-backed rather than a fixed remembered click list.
- Local providers were unavailable during the run. Joe recorded the failures and used the request-derived bounded engine once; the authoring path skipped an unproductive second provider retry, while the browser QA and delivery gates still ran.
- The run exposed a remaining operational cost: independent QA analysis can still wait on the provider mesh when every provider is down. This is a performance/recovery gap, not evidence that the task succeeded without QA.

## Current Gaps

- This log contains P01-P20 attempts with mixed outcomes; P09 and P14 retain open failures. It does not establish twenty clean sequential passes. The current objective is 500 sequential UI prompts; security regression checks do not increment that count.
- GitHub disconnect is implemented and statically covered, but the connected-state click still needs a live session with a valid user token.
- Browser QA and existing-project continuation require the next progressively harder prompts and independent artifact checks.
- P10 passed its requested acceptance and browser gates, but the environment still needs a dedicated offline-install regression test so a cached toolchain is reused before npm waits on the registry.
- P04 proves the bounded terminal diagnostic path for version/path requests; arbitrary shell text is still intentionally rejected.
- P08 proves explicit directory targeting for a named folder and nested entry; recursive depth and more ambiguous directory language still need coverage.
- P11 passes the responsive browser gate at `97/100`; the remaining low-severity reachability finding is a dynamic-control timing issue and needs broader state-rediscovery coverage in later prompts.
- P12 passes its requested acceptance and browser gates at `97/100`; the remaining low-severity reachability finding is a dynamic-control timing issue. The delivery reconciliation fix is now covered by a regression test.
- The neural activity surface is being upgraded from a one-line status card to a compact four-event live stream, with the newest real WebSocket event marked as the current action and full history still available in details/Logs.
- P13 exposed and fixed a session-refresh recovery defect: title refresh now updates the visible chip immediately and rehydrates the active conversation from persisted history without clearing it. It also added general mobile-header fragmentation detection and repair, and promoted contact forms to explicit acceptance criteria.
- P13 follow-up fixed a contradictory scope warning: contact-form evidence is now part of the capability audit, and explicitly listed page names are excluded from unchecked-clause warnings.

## Short Follow-up Continuation Evaluation

This is a continuation capability check and does not increment the numbered P01-P14 sequence.

- Entered through Joe's real command box in the existing `عدسة` conversation: `غيّر نص زر «احجز موعدك» إلى «احجز جلستك»، ثم اختبر التعديل في المتصفح.`
- Joe kept the same project, changed only the requested source label first, completed the production build, and opened the Browser panel. Visual inspection confirmed the new button text.
- The first browser pass measured `85/100` with `low_contrast`. Joe did not deliver at that score: it announced a measured repair, rebuilt, reran the browser audit, and reported `85 → 100/100` with the finding removed.
- Source inspection then found that the repair block had been appended to both `src/styles/base.css` and `src/styles/tokens.css`, together with unrelated responsive rules. This was rejected as an over-broad repair even though the score was clean.
- `repairProjectFiles` now selects one primary CSS target for evidence-driven repairs and gates responsive CSS on an actual responsive finding. A new regression proves a low-contrast finding changes only `base.css` and does not invent responsive work.
- Verification after the narrower fix: focused project-edit/UI-repair/self-repair suites `57/57`; improvement-loop suites `114/114`; production API build; and all ten permanent architecture/self-healing commands passed.
- Restarting the API initially invalidated the visible guest identity because the WebSocket client deleted a valid token immediately on a policy close. The client now asks the authenticated HTTP boundary first and clears the token only on an explicit `401`.
- Live continuity proof: a fresh guest created `SESSION_RESTART_PROBE` and reached `Sessions · 1`; after a forced API restart the same tab retained the completed message, and a newly opened Joe tab rehydrated the same conversation and `Sessions · 1`. The ownership/restart regression suite passed `26/26`, and the web production build completed with 4,281 transformed modules.
- The interrupted old guest conversation cannot be recovered after its token was already deleted, so the narrower one-file CSS repair remains regression-proven but still needs a fresh measured app rerun.

## 2026-09-07 Account-Isolation Follow-up

The user reported seeing the same project from an authenticated account and a guest view. This batch extends the earlier session/workspace fixes to browser ownership. It is a security regression batch, not a new numbered prompt.

- Confirmed implementation defect: unknown `browser:<chatId>` streams were assigned to the first requesting account. HTTP and WebSocket paths now check the persisted chat owner in JSON or Mongo mode. Unknown streams are denied; the legacy exact `browser:<userId>` namespace is account-bound.
- The saved-browser-login status route now checks ownership before reading metadata.
- Live chat subscriptions now authorize persisted sessions even before a run registers an in-memory owner. Anonymous live sockets and supplied invalid tokens are rejected. A subscription revision prevents a slow old request from replacing a newer session selection.
- Focused verification: `npm test -- --runInBand --silent src/__tests__/live-subscription-ownership.test.ts src/__tests__/session-isolation.test.ts src/__tests__/browser-session-ownership.test.ts src/__tests__/browser-session-status-isolation.test.ts src/__tests__/session-controller-isolation.test.ts src/__tests__/local-guest-browser-panel.test.ts src/__tests__/browser-session-wiring.test.ts` passed: 7 suites, 25 tests.
- API `npm run build` passed after retrying outside the filesystem sandbox (the initial esbuild invocation was denied directory access). The local API was restarted with the new bundle.
- `node scripts/verify-browser-account-isolation.cjs` passed against the running API: owner metadata access 200; foreign metadata 403; foreign history/workspace 404; foreign browser socket closed 1008; foreign live subscriptions empty; owner live subscription accepted; invalid HTTP and live tokens rejected. The synthetic session was deleted by the test.
- UI check in the isolated in-app Guest tab: selecting Browser still connects the owner's panel. Screenshot shows an empty canvas labelled `No page loaded`, as expected for the read-only session. This is connection/access evidence, not a rendered-page QA pass.
- Remaining security review: `projectPreview.ts` explicitly serves project builds without authentication. This source-level finding needs a scoped preview authorization design and isolated runtime reproduction. No full-account-isolation completion claim is made.
- Required gates completed with exit code 0: `guard:architecture`, `guard:package-scripts`, `test:joe:engineer-flow`, `test:self-fix:build-context`, `test:self-fix:execution-safety`, `test:self-fix:typescript-repair`, `test:self-fix:typescript-missing-name`, `test:self-fix:typescript-number-to-string`, `test:self-healing:failure`, `test:self-healing:success`. The engineer-flow plan is controlled/mocked, as documented in AGENTS.md; it does not prove unrestricted planning.
- UI follow-up: at a narrow viewport the workspace tabs and New Chat control lost their accessible names when their labels were hidden. Added explicit accessible labels/tooltips in `WorkspacePanel.tsx` and `SessionsBar.tsx`; the live accessibility tree now retains those names. New Chat was clicked in the real UI and opened directly, with empty Logs and an independently connected Browser panel. Frontend `npm run build` passed TypeScript and Vite (4,281 modules; Vite completed in 2m 15s).
- GitHub synchronization is not yet verified for this batch. No new numbered prompt is claimed.

### Browser Prompt Regression: Repaired And Live-Proven

Entered and submitted through Joe's command box in the in-app browser, with Auto selected and Browser visible:

> Open http://127.0.0.1:5000/api/health in your browser and report the status shown. Do not modify any files.

Session: `6a9de7c44402a4c682685f88` (API Health Check). Joe initially stopped after 8 steps / 3:10 with `Read the file '127.0' if found` / `File not found`; Browser remained `No page loaded`. The live UI showed a repair/search detour for the invented local file.

Root cause confirmed in source and `joe-isolation-verified.out`: `parseExplicitReadFilesRequest` extracts filename-looking fragments directly from the entire prompt, including URL hosts and paths. PlanningEngine selected `read_file_0` with path `127.0`. A second boundary also needs repair: after correcting file extraction, the broad read-only audit route currently precedes browser routing and would still reinterpret the file-mutation prohibition as a workspace inspection task.

The general repair now keeps URL targets out of local-file extraction, preserves legitimate local filenames elsewhere in mixed requests, and prioritizes browser observation when the user forbids file modifications. `browser_launch` optionally reads bounded visible text when the request asks to read, show, or report content.

The exact prompt was then replayed through Joe's real command box in a clean session. The Browser panel visibly navigated to the complete URL and rendered the health JSON. Joe's chat reported the same observed values: `status: OK`, `database: LOCAL`, and `singleUser: true`. This closes P15; it does not claim broader interactive Browser QA beyond this bounded navigation-and-observation contract.

### Browser Frame Isolation And Existing-File Follow-up

- Opening a new chat initially reset the Browser URL to `No page loaded` but left the previous chat's `/api/health` pixels on the canvas. `EmbeddedBrowser` is now keyed by the owning browser/chat session, so session changes remount the frame and cannot visually inherit stale evidence. The live retest showed an empty canvas in the new chat, and `browser-session-frame-isolation.test.ts` protects the contract.
- P16's original target existed only under the older `my-workspace`. Joe correctly did not cross into that workspace. The repair preserves this security boundary and improves the failure message instead of weakening path resolution.
- The repaired append route uses no model call: it recognises a literal line and safe relative path, requires the file to exist in the active workspace, verifies any stated final-line ordinal, appends once, and reads the file back.
- Replaying the same append safely is idempotent when the expected final count and suffix are already satisfied. A mismatched initial count is a terminal precondition failure and leaves the file unchanged.
- The live same-conversation sequence proved continuation: create three lines, then issue a short follow-up to append the fourth. Joe's chat and the persisted file independently showed `alpha=1`, `beta=2`, `gamma=3`, `delta=4` and a final count of four.

## 2026-09-08 Arabic Reader Wiring Follow-up

The Arabic package the user remembered is `snowball-stemmers`, used as the Snowball Arabic stemmer in `core/language/arabic.ts` together with the platform's `Intl.Segmenter('ar')`. It is declared in `api/package.json`, locked, installed locally, and directly imported by five production readers: application blueprints, acceptance, content contracts, PlanningEngine, and ReactProjectTool. A separate `promptNormalizer.ts` layer owns dialect, spelling, and action-phrase normalization.

The package was therefore present, but the overall language path was only partially connected. A direct probe showed why both layers are needed: the reader tokenizes `تصميم موقع حديث` correctly and recognizes `تصميم`, but stemming does not turn the verbal noun `تصميم` into the imperative `صمم`. The normalizer already knew that semantic mapping, while the front-door `looksLikeBuild()` classifier was still testing the unnormalized prompt.

The classifier now evaluates the original and normalized readings while preserving question intent. `تصميم موقع حديث لشركة برمجيات` and the polite `هل يمكنك تصميم صفحة...؟` route as build requests; `ما رأيك في تصميم موقع؟`, `كيف أصمم موقعاً؟`, and `هل تصميم موقع مكلف؟` remain conversations. An initial broad question guard broke a separate clinic brief containing a later `كم`; the focused regression caught it, and the guard was narrowed to question openings before proceeding.

Live proof used Joe's real command box with Auto:

> تصميم موقع حديث لشركة برمجيات بعنوان مدار، بواجهة عربية متجاوبة وقسم خدمات وزر تواصل واضح.

The running API logged direct routing to the evidence-first `project_pipeline` and deterministic React builder without depending on Ollama. The first run exposed a second general defect: `بعنوان` was not an explicit brand marker. After adding Arabic and English title markers and replaying the exact prompt, Joe generated `react-مدار`; direct artifact inspection found `مدار` in the HTML title, social metadata, package name, and content contract. Focused routing/title verification passed 5 suites and 111 tests, and the ten permanent architecture/self-healing gates passed.

This is not a claim that every Arabic expression is solved. Snowball remains directly wired into only five production areas, and semantic normalization must continue to replace scattered literal readers as failures are observed. The rerun also left a separate open delivery defect: after completion, the embedded Browser canvas appeared blank and the final response called verification incomplete despite a reported `100/100`; that inconsistency is recorded for repair and is not counted as a clean prompt pass.

### Malformed Arabic Brief Retest

The language stack was audited end to end. `snowball-stemmers@0.6.0` remains installed and is used by `core/language/arabic.ts`; `promptNormalizer.ts` is the complementary intent layer for dialect, spelling variants, and multilingual action phrases. They remain separate deliberately because applying a stemmer to an entire instruction can collapse distinct Arabic meanings.

The audit found three general defects. The normalizer removed an initial Arabic waw before attempting the intact token, two explicit-URL browser routes omitted the canonical `describe` and `translate` actions, and `/api/runs/start` selected Mongo from environment flags even when Mongoose was disconnected. These were repaired with regression coverage. The local session store now remains owner-checked while Mongo is unavailable.

The exact malformed prompt was entered through Joe's real command box in a fresh guest session:

> بدي تصيم واجهة مودرن لموقع شركة استشارات، فيها عنوان واضح وزر تواصل. اختبر التصميم في المتصفح.

The first live attempt exposed the disconnected-Mongo crash. After repair and restart, the same prompt routed to the project pipeline, built and compiled a React project, opened the visible Browser panel, challenged the form's native field contracts, explored the submitted state, and remeasured desktop, tablet, and phone layouts. The final rerun reported `100/100`, one page, and three measured interactions. Independent preview and source inspection confirmed the visible headline `شركة استشارات متكاملة` and no occurrence of the raw `بدي تصيم` instruction in generated copy. The generated brand remained the generic `مشروعي` because no explicit product name was supplied; name invention remains a separate quality opportunity, not a language-routing failure.

Focused verification passed 4 suites and 59 tests. API and web type checks passed, and both production builds completed. The delivery-summary regression now keeps a clean Browser mechanics score distinct from incomplete request acceptance, so a `100/100` browser score cannot hide unproven requested criteria.

## P21 Same-Project Routing And Stateful Calculator Extension

The exact Arabic continuation was entered through Joe's real command box after selecting the existing `react-مشروعي-48eeedb0` project. Earlier attempts exposed two competing general edit routes: the older branch trusted stale in-memory project history and could preempt the later workspace-aware branch, while the selected Explorer root was lost when the API restarted. The result was either a new sibling calculator project or a repair plan against an invented `src/components/Calculator.js` path.

The older general edit branch was deleted. The remaining canonical edit route now treats the request-bound Explorer root as authoritative when it contains a project manifest, passes that exact directory to `project_edit`, and still leaves genuinely specialized import, undo, and image-edit routes intact. Workspace selections are persisted per workspace id and restored on startup. A live restart/reload retained the selected project without another picker action.

The same prompt also expanded the provider-independent calculator editor and Browser QA. Joe now renders JOD/USD selection, preserves service and hours across currency changes, validates `BASIRA10`, rejects a wrong code visibly, applies the 10% discount before the requested 16% tax, and resets all state. The first clean browser pass reached `100/100` but an older generic click-counter acceptance rule still rejected the proven calculated total. Calculator acceptance now consumes the stronger calculator evidence instead of that unrelated legacy detector.

The exact prompt was replayed after every repair. The final UI run used `project_edit`, created no project, reported `Delivery summary`, Browser QA `100/100`, one page, 13 interactions, and no critical findings. Evidence recorded all three viewports (`1280x900`, `390x844`, `820x1180`) and every requested state. Independent browser interaction reproduced the invalid and valid promotion messages, a USD subtotal of `1057.50`, discount `105.75`, tax `152.28`, total `1104.03`, and reset to service 0, one hour, JOD, empty code, and zero discount.

Focused verification passed 6 suites and 159 tests before the acceptance consolidation, then 4 suites and 71 tests afterward. The API production build passed. All ten permanent architecture and self-healing commands passed, including the full engineer flow and both success/failure stop conditions.

## 2026-09-22 Capability Decision Profile UAT

- The first visible request, `Choose the least-setup safe route for offline speech transcription with local-only privacy`, reached `decide_capability_route` but spent 51 seconds in IntentParser's provider analysis first. The result was not accepted as efficient: logs showed the local route with no user setup action, but the delay was treated as a capability-routing defect.
- `IntentParser` now recognizes explicit route-choice requests for supported capability families before deep analysis, while build requests remain outside this fast path. The decision tool also derives clear offline, local-only privacy, deployment, proven-reliability, and rate-limit constraints from ordinary request text; explicit structured constraints remain authoritative.
- Retesting the same request in the isolated local Joe UI (`127.0.0.1:5004`, API bound only to `127.0.0.1:5003`) completed in 7 seconds. The visible Logs panel recorded `CAPABILITY_DECISION selected local with no user setup action`.
- A second unseen request, `Choose the least-setup safe route for deployed file storage. Show the required user action, but do not connect any account.`, also completed in 7 seconds. The visible Logs panel recorded `CAPABILITY_DECISION selected account_connection with CONNECT_ACCOUNT user setup action`. No account was connected, no key was used, and no payment or deployment action occurred.
- The first receipt delivery echoed the request rather than its evidence; after adding a receipt composer, bounded runtime memory exposed two display defects (`item.reasons` compacted to a scalar, then nested alternatives truncated). Both were repaired with focused regressions. The final clean UI replay showed the complete `Capability route decision`: `account_connection (CONNECT_ACCOUNT)`, `UNKNOWN; evidence is stale or incomplete`, rejected `storage-local: does not support the deployment target`, and an explicit statement that no account, key, or payment was used. There was no invented viable alternative.
- This UAT proves bounded decision selection and its UI execution path, not live provider availability or successful external integration.
## 2026-09-11 Issue 89 Pre-UAT Review Corrections

Status: local changes only, not a new passed prompt and not verified on GitHub main.

Review found four defects: different run ids at tool/firewall boundaries; product constraints discarded because their quote begins with an imperative; incomplete dependency graphs allowing narrowed reuse; and linked/unreadable filesystem inputs silently omitted from fingerprints. These were corrected with focused regression coverage. Follow-up review found UI action verbs still too broad; external-link, availability, keyboard-submit, and refresh requirements now have verbatim/paraphrase regressions.

The UAT runner incorrectly expected the run receipt status `completed`, while `AgentLoopService` and the receipt endpoint expose `done`. The runner now uses `done`; successful terminal events, verification evidence, and independent preview interactions remain separate required conditions.

Verification executed from `.issue89-verification/api`:

- `npm test -- src/__tests__/orchestrator-run-identity.test.ts --runInBand --silent`: passed, one test covering three executions with distinct identities.
- `npm test -- src/__tests__/verification-ledger.test.ts src/__tests__/change-aware-phase-verification.test.ts --runInBand --silent`: passed, 30 tests.
- `npm test -- src/__tests__/an-instruction-to-joe-is-not-a-requirement.test.ts src/__tests__/records-requirements-without-a-model.test.ts --runInBand --silent`: passed, 19 tests after the follow-up correction.
- `npx tsc --noEmit`: exit 0; ran before the final action-verb regex/test-only correction.
- `npm run guard:architecture`: passed.
- `npm run guard:package-scripts`: passed.
- `npm run test:joe:engineer-flow`: passed, including unchanged-check reuse, one repair, and a separate final gate. Its planner is controlled.
- `npm run test:self-fix:build-context`: passed after supplying a process-local test JWT value; initial invocation stopped before testing because JWT_SECRET was absent.
- `npm run test:self-fix:execution-safety`: passed.
- `npm run test:self-fix:typescript-repair`: passed.
- `npm run test:self-fix:typescript-missing-name`: passed.
- `npm run test:self-fix:typescript-number-to-string`: passed.
- `npm run test:self-healing:failure`: passed, stopped after failed repair.
- `npm run test:self-healing:success`: passed, repaired and completed.

Browser status: `/api/health` responded OK. Opening the internal browser via CUA failed with `failed to write kernel assets` (missing path); `open_in_codex` accepted a queued request for `http://127.0.0.1:5002/joe`. Neither is proof of visual UAT. No fresh live prompt was executed in this verification batch. Next: load the corrected local runtime, replay the same library-board prompt, inspect its real output, and resolve any remaining failures before advancement or GitHub completion claims.

### Same-Prompt UI Replay: Failed, Not Advanced

After loading the corrected local API, the first browser runner attempt exposed a startup race: session hydration cleared the composer after it was filled. The screenshot showed an empty input and the authenticated active-run endpoint returned no runs. That idle runner was stopped, not counted as an executed prompt. The runner now waits for session identity, checks that the full prompt survives rendering before sending, and fails early if no run begins.

The unchanged library-board prompt then started successfully from Joe's actual composer. Run: `run-1789092927930`. Evidence directory: `api/data/tests/joe-ui-acceptance/2026-09-11T02-14-50-975Z`, including `report.json`, submitted-prompt, desktop, and mobile screenshots. The run finished in 475,468 ms with `terminalOk=false`, receipt `failed`, and runner exit 1.

- Independently passed in the generated preview: invalid-form rejection, record creation, returned switch, date input, persistence after reload, filtering, and mobile horizontal fit. No page exceptions were captured.
- Joe's initial builder self-QA reported 100/100; the final visible delivery reported 92/100 and incomplete verification. API logs recorded a browser navigation 502 and viewport requests for 390/820 remaining at 1280. These are unresolved internal browser-verification defects, not disproved by the independent preview working.
- Verification summaries now reach the canonical run id, but the ledger has zero selected/reused/passed/failed checks. An empty event must not be promoted to useful verification coverage. The runner currently records event presence separately; a substantive final-check evidence gate still needs strengthening.
- The builder acceptance receipt collapsed the multi-part request into one compound criterion and called it 1/1 proven. This is insufficient granular evidence and remains an acceptance-contract defect.
- The generated title includes request scaffolding (`Small Browser Based Library Checkout Board In A`) and the visual layout remains generic. Functioning form controls are not proof of visual originality or a polished domain-specific product.
- Installation incurred an offline-attempt timeout before the successful build. Investigate bounded dependency setup and avoid redundant work, without weakening quality gates.

No prompt advancement, commit, push, or success claim follows this run. Next repairs must address the observed internal browser verification and substantive evidence/acceptance gaps, then replay this same prompt. The internal Codex browser received the generated preview URL; screenshots were inspected directly because CUA control remained unavailable.

### Viewport Failure Isolation

Added `api/src/tests/manual/verify_browser_viewport_stream.ts`, which uses Joe's browser manager on an existing local preview, measures actual document width/height and CSS media queries before and during streaming, and optionally runs `auditBuiltApp`. It creates a dedicated test session and closes it; it does not rebuild the project or call a planner/model.

Normal viewport control passed eight initial transitions and the full audit scored 100. A forced no-op normal setter then reproduced the supported CDP fallback failure. This exposed two separate defects: the setter accepted matching width despite wrong height, and screenshots restored Playwright's cached metrics while the CDP session still remembered the requested metrics. Repeating those same metrics did not reapply them. Repeating a size after a screenshot reproduced this without streaming, ruling out streaming alone as the cause.

Local corrections: check both dimensions; clear a stale CDP override once only after a failed measurement while capture is paused; use native CDP capture for unmasked stream frames so they do not reset device emulation. Masked frames keep Playwright's mask path. Explicit session screenshots now also pass the configured session masks.

Evidence after the combined corrections: `api/data/tests/viewport-stream-proof-1789094583774/report.json`. Ten forced-CDP transitions passed, including repeated identical sizes and live streaming; the actual `auditBuiltApp` returned 100 with no findings. The masking check passed and `masked.jpg` was visually inspected, showing the input covered. `npm test -- src/__tests__/deep-self-qa.test.ts src/__tests__/browser-cleanup-behaviour.test.ts --runInBand --silent` passed 63 tests; `npx tsc --noEmit` exited 0.

Remaining limitations: the streaming event still derives dimensions from `page.viewportSize()`, which can remain stale under CDP ownership; fix frame metadata before claiming accurate in-panel responsive presentation. Masked capture still follows Playwright and may require viewport restoration before subsequent QA. This is a component-level reproduction and repair, not a passed full Joe prompt. API runtime has not yet been restarted with these browser changes. Empty ledger and compound acceptance defects from the live run remain open.

### Intrinsic Stream Frame Dimensions

Moved the existing `imageSize` header reader into lightweight `shared/image-size.ts`, retaining its original export from `core/design/images.ts`. Streaming now labels each captured JPEG with dimensions from that same buffer, rather than stale Playwright viewport state; an unreadable frame is not broadcast with invented dimensions.

`image-size.test.ts` and `deep-self-qa.test.ts` passed 66 tests, including baseline/progressive JPEG markers, PNG/GIF behavior, and incomplete headers. Real forced-CDP testing passed all ten viewport transitions and published-frame dimension assertions (`data/tests/viewport-stream-proof-1789095100293/report.json`). The normal setter remained deliberately ineffective and Playwright still reported 1280x720, while the real document and emitted frame metadata correctly reflected requested sizes.

Follow-up static review found two open risks: masked streaming still uses Playwright screenshots and can undo CDP viewport emulation; the capture timeout does not yet bound CDP session attachment or awaited detach. The existing mask test only proves mask forwarding and actual obscuring, not stability during masked streaming. These must be repaired/tested before approval. No full prompt pass, API restart, commit, or push is claimed for this follow-up.

### Bounded Native Capture Connection

Native capture now lives in `modules/browser/native-capture.ts`. One deadline includes CDP attachment and image capture. Cleanup does not await a potentially stalled detach while holding the manager's capture lock. A connection arriving after timeout is detached and never cached or used for a frame; failed connections are evicted so subsequent capture can attach afresh.

`npm test -- src/__tests__/native-capture.test.ts src/__tests__/browser-cleanup-behaviour.test.ts --runInBand --silent` passed four tests, including stalled attachment, late arrival, never-resolving capture/cleanup, healthy reuse, and existing session cleanup behavior. Real-browser verification passed ten forced-CDP size transitions and published-frame assertions with exit 0; evidence: `data/tests/viewport-stream-proof-1789095381037/report.json`. Diff checks passed.

The masked-streaming viewport defect is still open. This change bounds the native capture operation but cannot forcibly cancel a browser protocol promise that never settles; it prevents that promise from holding up the caller and prevents late publication. No complete Joe prompt rerun or GitHub publication is claimed.

### Masked Streaming Without Page Mutation

The combined forced-CDP/masked-stream test reproduced the failure (`viewport-stream-proof-1789095543565`): streaming changed actual height from 900 to 720. Restoring metrics after a Playwright masked screenshot was also insufficient because measurements could occur during the temporary resize. That approach was removed.

`masked-capture.ts` now captures natively and overwrites masked regions in the JPEG pixels using pinned `jpeg-js@0.4.4`. It does not modify DOM, focus, or viewport for redaction. All matching locator boxes are resolved with a bounded region count; geometry, viewport, or URL changes across capture reject the frame. Malformed images/masks also fail closed. Decoding is bounded to 8 megapixels/128 MB; synchronous image processing cannot be preempted, but expired results are rejected using an elapsed-time check before return. Attachment, capture, and mask observation share the existing deadline.

Evidence: `api/data/tests/viewport-stream-proof-1789095936477/report.json` passed all ten forced-CDP transitions with masking enabled, including live streaming and published-frame dimensions. The test checks actual magenta pixels at the masked field center. `stream-390.jpg` was visually inspected: the input is obscured and the image remains 390 pixels wide. The test preview contains no real secrets.

Verification: masked/native capture plus deep-self-QA suites passed 68 tests; after adding the elapsed-processing deadline assertion, native/masked suites passed seven tests. TypeScript checking exited 0 before the small deadline assertion addition. Package metadata changed only to add jpeg-js (one package line, seven lockfile lines). No application generation or model call was needed for these component tests.

Still pending: final static review, remaining substantive-ledger/compound-acceptance defects, loading the browser changes in the API, and the same full Joe UI prompt replay. Pixel redaction costs CPU on masked frames; frames over the configured limits are rejected rather than exposing unmasked content. No main-branch publication or prompt advancement is claimed.

### Redact Before Lossy Compression

Follow-up review identified two separate redaction defects: JPEG compression before masking can mix sensitive pixels into neighboring pixels, and matching geometry before/after capture does not detect a target moving A-to-B-to-A during capture. The second defect remains open; endpoint equality is not proof of capture-consistent masking.

Addressed the compression defect by capturing native PNG, decoding losslessly with pinned `pngjs@7.0.0`, overwriting masked pixels, and only then encoding JPEG. Added `@types/pngjs@6.0.5` for strict TypeScript checking. Input signature, header, 8-bit depth, compressed size (32 MiB), pixel count (8 million), and CRC are checked. Existing capture deadlines remain in effect. Dependencies were installed with lifecycle scripts disabled.

`npm test -- src/__tests__/masked-capture.test.ts src/__tests__/native-capture.test.ts --runInBand --silent` passed nine tests. A new regression requires byte-identical redacted JPEG output from two PNGs differing only inside an unaligned mask. An initial oversized-image test used dimensions below the pixel limit; corrected its fixture to exceed the actual limit, then reran both suites. `npx tsc --noEmit` passed.

Real-browser component verification with `JOE_VIEWPORT_FORCE_CDP=1` and `JOE_VIEWPORT_MASK=1` passed all ten viewport transitions, stream frame-size checks, and masked-pixel assertions against the existing local preview. Evidence: `api/data/tests/viewport-stream-proof-1789096761195/report.json`. No model generation calls or full Joe prompt reruns were needed for this check.

This is partial local repair, not completion: moving-target capture consistency, substantive verification ledger, compound acceptance, full UI replay, and GitHub publication remain pending. The API was not restarted and no production action was performed.

### Compound Declaration Evidence

The live checkout-board request's declarative inventory was truncated at validation during fallback extraction, while the deterministic source judge could certify the whole sentence from its first matching capability. Added one shared declaration splitter for `needs/requires/has/must include/should provide` inventories, preserving later behavior items. Deterministic judgment now checks every item from both text and source quote before certifying the parent inventory; unresolved items fall through to the existing model judge instead of an early success.

The eight-item checkout inventory is retained in full. Regression cases reject validation-only source for a mixed inventory and for a narrow summary paired with a broader quote; a positive case proves all three requested capabilities without calling a provider. `npm test -- src/__tests__/records-requirements-without-a-model.test.ts src/__tests__/an-instruction-to-joe-is-not-a-requirement.test.ts src/__tests__/nothing-is-done-until-it-is-proven.test.ts --runInBand --silent` passed 80 tests across three suites. Diff checks passed.

This fixes the observed declarative-list path, not arbitrary natural-language decomposition or runtime acceptance. Non-list compound grammar still relies on existing judgment. Full Joe replay, substantive ledger receipts, moving-mask safety, and GitHub publication remain outstanding; the prompt sequence has not advanced.

### Reject Empty Verification Summaries In UI Acceptance

Extracted the real-UI runner's evidence predicate into `src/tests/manual/verification-evidence.ts`. It now requires valid nonnegative integer counters, substantive completed checks, consistent receipt totals, no failed/cancelled/timed-out/incomplete checks, inspectable check identities/tools, and a passed final check. Only the latest cumulative summary is inspected; earlier passing snapshots cannot hide a later empty one. Report JSON records rejection reasons rather than treating the event envelope as proof.

`npm test -- src/__tests__/ui-verification-evidence.test.ts --runInBand --silent` passed nine cases. Re-evaluating the saved real UI report `2026-09-11T02-14-50-975Z/report.json` rejected its zero-count summary with `verification_no_checks` and `verification_check_evidence_missing`. This evidence replay required no provider call or regenerated application.

This is a stronger evaluator, not a repaired execution ledger. The actual run still needs its checks connected to canonical verification receipts; no checks were fabricated, no live prompt was advanced, and no GitHub or production publication occurred.

### Final Quality Gate For Constrained Frontend Plans

The constrained frontend planner omitted a `verificationTask`, leaving its internal builder checks outside the canonical phase ledger. It now requires final verification and schedules `quality_run` with lint/typecheck/test/build after construction. No project path is guessed by the planner: PhaseExecutor inherits the accepted runtime project root. The final check uses the existing ToolService path and ledger, not a competing execution route.

`QualityRunTool` now returns `incomplete` with `ok: false` when all requested checks are unavailable. When at least one check executes, missing optional scripts remain explicitly skipped in results; failures remain failures. A successful result therefore proves available checks, not nonexistent test coverage.

Focused planner/quality/phase suites passed 22 cases. An additional phase integration test passed with the complete seven-case phase suite, proving that the actual constrained plan's final checker inherits the runtime root and records `frontend:final-quality` as a passed final receipt. That integration mocks tool execution; it is not the live generated-app test. `npx tsc --noEmit` and diff checks passed before the final test-only addition.

Required gates all exited 0 for this planner change: `npm run guard:architecture`, `npm run guard:package-scripts`, `npm run test:joe:engineer-flow`, `npm run test:self-fix:build-context`, `npm run test:self-fix:execution-safety`, `npm run test:self-fix:typescript-repair`, `npm run test:self-fix:typescript-missing-name`, `npm run test:self-fix:typescript-number-to-string`, `npm run test:self-healing:failure`, and `npm run test:self-healing:success`. The deterministic engineer-flow test demonstrated reuse of the unchanged smoke check and one distinct final gate after repair. Existing websocket-without-server and child-process deprecation warnings remain visible; no test failed.

The browser moving-mask defect and full real-UI replay remain pending. No commit or GitHub publication is claimed for this local batch.

### Same-Prompt Replay: Runtime Improved, Ledger Still Missing

Restarted the owned local API after verifying the previous test identity had no active runs. Ran `npx tsx src/tests/manual/verify_joe_prompt_ui_acceptance.ts` with the unchanged checkout-board prompt. Run `run-1789098287755`, session `6aa3792d14b388dd32787d6d`, finished after 255824 ms (previous run: 475468 ms). Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T03-44-32-779Z/report.json`. Joe returned `done` and `run_finished.ok=true`, but the independent runner exited 1 because the final verification summary still had zero selected/reused/passed checks and no check evidence. The sequence does not advance.

All independent form/preview checks passed: invalid input rejection, row creation, reload persistence, filtering, switch/date controls, and document-width mobile fit. Page errors: 0; one console/network error remains recorded. Browser QA reported 100/100 without the prior viewport-emulation finding. Compound acceptance now explains evidence for all eight declared items, though the UI still groups them as one criterion. The repeated prompt used model planning (`Application` phase), not the constrained frontend plan changed above. The final-check guarantee must therefore cover the general plan normalization path rather than only one planner branch. No results may be fabricated to fill the ledger.

Inspected `05-preview-mobile.png`: the long request-derived title is visibly clipped and the interface remains a generic purple records layout. Passing document scroll-width checks did not prove text fit or visual originality. These remain defects despite Joe's 100/100 score. Offline dependency installation again timed out before fallback; this is another bounded-performance gap, not a reason to repeat generation blindly.

Internal Codex browser control was retried and failed with `failed to write kernel assets` / missing path. Opening a panel returned only `queued`. The test used the real Joe UI through headless Playwright and saved screenshots; no visible in-app interaction is claimed. API remains the local dev server, with no production deployment or GitHub publication.

### Normalize Final Checks Across Planner Sources

Moved the frontend final-check guarantee into the shared `validatePlan` path as well as the constrained plan. A plan containing the registered `react_project` builder now requires final verification regardless of provider or request wording. The last phase receives a quality checker when it had only a note or no executable check; an existing explicit checker retains its tool, arguments, and ID and is marked final. Original notes are retained, blocked/unrelated plans are unchanged, and normalization is idempotent. The default checker definition is shared rather than copied between branches.

`npm test -- src/__tests__/planner-final-verification.test.ts src/__tests__/change-aware-phase-verification.test.ts src/__tests__/public-api-discovery.test.ts --runInBand --silent` passed 24 cases. `npx tsc --noEmit` passed. For this batch, required runtime gates use process-local `TS_NODE_TRANSPILE_ONLY=1` with the separate full TypeScript check, avoiding repeated type analysis while retaining all runtime assertions; no package script was weakened or changed. Full gate outcomes are recorded below when complete.

This repairs the observed plan-normalization gap; arbitrary builder types still need their own evidenced verification contracts. API has not been restarted with this latest change, so no new live-prompt pass or GitHub publication is claimed. Clipped mobile title, generic design, masked-capture motion safety, and dependency-install delay remain open.

Gate results: all ten required commands exited 0: `guard:architecture`, `guard:package-scripts`, `test:joe:engineer-flow`, `test:self-fix:build-context`, `test:self-fix:execution-safety`, `test:self-fix:typescript-repair`, `test:self-fix:typescript-missing-name`, `test:self-fix:typescript-number-to-string`, `test:self-healing:failure`, `test:self-healing:success` (each invoked with `npm run`). Diff checks also passed. These gates prove the controlled pipeline and repair invariants, not a successful replay of the live checkout request.

### Generated Header Text Must Remain Visible

The phone stylesheet deliberately applied nowrap/hidden/ellipsis to `.app-name`, masking the long-title defect from document-width checks. Updated the reusable generator stylesheet to wrap names and unbroken words. Initial browser measurements then exposed a second issue: 23px line boxes contained 27px glyph rectangles, causing vertical overflow. Increased title line-height to 1.4 rather than relaxing the assertion.

New permanent component browser test: `npx tsx src/tests/manual/verify_generated_header_fit.ts`. Eight cases passed: long sentence and unbroken name at 320, 390, 820, and 1280px. It measures text rectangles, scroll/client dimensions, parent containment, button overlap, and document width. Evidence: `api/data/tests/header-fit-1789099240351/report.json`; `390-0.png` visually inspected and shows the full title on three lines. This uses generated CSS in a local browser fixture, not a successful new Joe prompt or an edit to the generated project.

`npm test -- src/__tests__/shell-visible.test.ts --runInBand --silent` passed 25 cases. The real Joe UAT runner now also requires visible heading text to fit and not be clipped by ancestors; page width alone no longer suffices. This assertion will be exercised in the next live replay. No new provider generation, API restart, GitHub commit, or deployment occurred in this component repair.

### Avoid Duplicate Cold Dependency Installation

Both real checkout replays first timed out `npm install --offline`, then retried network installation. Fresh generated workspaces now run one bounded `npm install --prefer-offline --no-audit --no-fund`, allowing cached entries and missing registry entries in the same resolution/extraction pass. Verified existing/reused dependency trees still skip installation. Native executable checks, scoped esbuild quarantine repair, and one clean reinstall for a demonstrably incomplete successful installation remain in place.

Removed the obsolete intermediate deletion/retry block; the existing `cleanReinstallReactDependencies` owns cleanup when its verified-incomplete condition is reached. Its behavior test still checks that the partial tree/lock are absent before execution, that timeouts are passed, and that only one reinstall is called. No new destructive operation or script approval was added.

Initial React/terminal regression run: 110 passed and one static source-location assertion failed because it expected deletion in the removed retry block. Updated that assertion to require the existing clean-reinstall call; the behavior test remains intact. `npx tsc --noEmit` passed. The React suite is rerun below; the already-passing terminal suite is not rerun for this test-only assertion correction. Actual speed improvement remains unmeasured until the next full Joe UI replay; no new generation or publication is claimed.

Final focused result: `npm test -- src/__tests__/react-project.test.ts --runInBand --silent` passed all 84 cases. Together with the unchanged terminal suite's 27 passing cases, all 111 targeted cases now pass.

### Faster Replay Exposes Post-Planner Rescue Gap

Loaded the prior batch into the owned local API and repeated the unchanged prompt through Joe UI. Run `run-1789099844409`, session `6aa37f416e8f68fab6fd7583`, completed after 169869 ms. Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T04-10-30-329Z/report.json`. All independent interaction checks and the new heading-text-fit assertion passed; `05-preview-mobile.png` was inspected and the complete generated name is visible. Four real commands all exited cleanly, with one cache-preferred install and no failed cache-only attempt. This is a measured improvement from 255824 ms, not a general performance guarantee.

The independent runner still exited 1: Joe reported `done` but its verification summary contained zero checks. Root cause is now traced to `ProjectPipelineTool.deterministicPhasesFor`: a rescue branch replaces planner output after `ProjectPlannerTool.validatePlan`. The prior normalization fix therefore did not reach the plan actually executed. Earlier assumption that the `Application` phase was directly model-produced was incorrect; this run supplied the evidence to identify the replacement.

Extracted the existing final-check policy into shared `core/quality/plan-verification.ts` and apply it in the pipeline after all rescue replacements, immediately before accepting phases for execution. The planner uses the same helper. No execution path or duplicate checker is introduced. The regression uses the full real request with `deterministicPhasesFor`; its initial shortened request classified as a page, so the fixture was corrected to retain the actual behavioral requirements. Planner tests passed five cases, canonical phase tests passed seven, and full TypeScript checking passed. Mandatory runtime gates are being run with separate type checking, as above.

The latest shared-policy change is not yet loaded into the API or validated by a further full UI replay. Generic visual design, request-derived naming, and moving-mask safety remain unresolved. No prompt advancement, commit, GitHub publication, or production action is claimed.

Shared-policy gate results: all ten required `npm run` commands passed: `guard:architecture`, `guard:package-scripts`, `test:joe:engineer-flow`, `test:self-fix:build-context`, `test:self-fix:execution-safety`, `test:self-fix:typescript-repair`, `test:self-fix:typescript-missing-name`, `test:self-fix:typescript-number-to-string`, `test:self-healing:failure`, and `test:self-healing:success`. Diff checks passed. Runtime assertions used process-local transpile-only mode after a separate successful full TypeScript check, without changing permanent scripts.

### Live Final Receipt And Independent UI Checks Pass

After verifying the previous test identity had no active runs, loaded the shared-policy fix into the local API and replayed the unchanged checkout prompt. Run `run-1789100649901`, session `6aa382679be9ed1de810b2af`, completed in 162593 ms. Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T04-23-55-467Z/report.json`. The independent runner exited 0: terminal success, receipt `done`, substantive final evidence, input rejection, row creation, reload persistence, filtering, returned switch, date field, mobile width, and heading text fit all passed. Page errors: zero.

The actual `quality_run` tool-start event binds to the generated root `data/projects/92bd4bdab0c20254c93745dd/react-small-browser-based-library-chec`. The ledger records one selected/passed `frontend:final-quality` check in final mode, duration 14541 ms, with zero failures and zero reused checks. This fresh build does not prove reuse; controlled engineer-flow tests separately cover that. The generated manifest contains build/test scripts but no lint/typecheck scripts. Its test checks scaffold files/entrypoints, not domain behavior; the independent UI interactions provide the separate behavioral evidence.

`05-preview-mobile.png` visually inspected: heading is readable but the records surface remains generic purple styling with request-derived naming. One failed network request is Joe's Gravatar avatar lookup (404), not generated-app execution. Do not call this full mission acceptance: visual originality and masked-capture motion safety remain open, and no GitHub commit/main synchronization exists for the batch. No production deployment occurred. This is the first full runner pass for the functional/verification repair, not completion of Issue 89 or the 500-prompt objective.

### Real Browser Reproduces Mask Motion Exposure

Added permanent adversarial browser regression `src/tests/manual/verify_mask_capture_motion.ts`. It captures real Chromium pixels from a local fixture containing only `TEST ONLY`, moves the masked element from A to B immediately before native capture, and restores A before the second geometry observation. Only screenshot scheduling is controlled; the actual image is produced by Chromium and decoded independently.

`npx tsx src/tests/manual/verify_mask_capture_motion.ts` exited 1 with `blocked:false`, `exposed:true`, `safe:false`. Evidence: `api/data/tests/mask-motion-1789101010467/report.json` and `captured.jpg`, visually inspected. The original location is magenta while the moved black field and its test text remain visible. This turns the prior static review finding into a reproducible real-browser failure. Endpoint equality is insufficient, even after lossless capture fixed compression leakage.

This test intentionally remains failing until capture-consistent masking is implemented. No production code or actual secret was changed/exposed, and no full Joe generation was needed. Do not publish the new masked-capture implementation or weaken the regression to make the batch green. A geometry-only patch, including another endpoint check, is not a complete fix.

### Hide Source Pixels Before Capture

Masked capture now temporarily applies inline `filter:opacity(0)!important` and disables transitions on matched elements before native lossless capture. It restores only owned style properties in finally, preserving unrelated page changes. DOM mutations during capture reject the frame. Observation covers the target's shadow root and every enclosing shadow/document root; missing targets reject before screenshot. Acquired element/guard handles are disposed on success and error paths. Pixel redaction remains a second layer and JPEG encoding still happens last.

Native capture now holds a per-page cleanup barrier for mutating captures. A timeout can return to the caller without allowing later native/masked capture to race unfinished restoration. Restoration failure taints that Page, and further capture requires page recovery rather than silently clearing the failure. A permanently stalled restoration suppresses later frames until a fresh page is created; this is an explicit fail-closed availability limitation.

Real browser evidence: style-mutation A-to-B-to-A is blocked (`mask-motion-1789101258928`); CSSOM-only movement produces no exposed pixels and restores styles (`mask-motion-1789101349167`); shadow-root mutation that temporarily removes hiding is blocked with restoration verified (`mask-motion-1789101710962`). These tests use only synthetic test content. Forced-CDP masked streaming passed all ten viewport transitions and actual redacted-pixel checks (`viewport-stream-proof-1789101381318`), before the final shadow/handle/taint hardening.

Verification: native/masked/deep-self-QA suites passed 72 cases during implementation; final native/masked subset passed 12 cases including missing targets, timeout cleanup barriers, and tainted-page rejection. Full TypeScript checking passed after correcting DOM node typing; diff checks passed. Focused security review found the shadow-root, missing-target, handle cleanup, and failed-restoration issues above, now addressed locally; follow-up review remains pending. No full Joe UI replay, API restart, GitHub publication, or production action is claimed for this browser change.

Follow-up static review completed: no additional blocker found in those fixes, and the three preceding review findings are closed at code-review level. The reviewer did not independently rerun tests and did not grant full UAT approval. Runtime/UI revalidation after the final hardening remains required.

### Final Masked Stream Recheck And Subject Extraction

Reran `verify_browser_viewport_stream.ts` with forced CDP and masking enabled after the final shadow-root, missing-target, handle-cleanup, and taint changes. All ten transitions, stream dimensions, and actual masked-pixel assertions passed. Evidence: `api/data/tests/viewport-stream-proof-1789101847409/report.json`. No fresh model-generated application was required; full API UI replay remains separate.

Investigating repeated visual identity found that structured records/ledger engines intentionally bypass free-form model authoring to preserve behavioral contracts. Their fixed presentation still needs a separate request-driven design solution; recoloring the same form is not sufficient. The immediate naming defect was independent: `englishBriefSubject` included execution-location phrases such as `in a new local project`, and direct English/Arabic branches truncated mid-word despite the fallback branch using word boundaries.

Subject extraction now excludes explicitly qualified project/workspace/directory/folder execution locations, preserves genuine locations such as New York, and bounds direct subjects at word boundaries. Explicit named-subject behavior remains unchanged. `npm test -- src/__tests__/subject-execution-location.test.ts src/__tests__/his-brief-is-not-the-headline.test.ts src/__tests__/the-thing-he-named-not-the-sentence.test.ts --runInBand --silent` passed 29 cases. This is a general identity repair, not a claim that layout originality is solved. No API restart, provider generation, commit, or publication occurred for this naming change.

### Layout Grid Is Not A Records Table

Inspected the actual records selection path: `ReactProjectTool` selects the structured records/ledger engine before free-form authoring, and the records presentation contract currently exposes only `asTable`. `heAskedForATable` treated any English `grid` mention as a table request, including CSS layout, card grids, and form-field grids. A new focused regression reproduced five false table selections before the fix; five genuine table/data-grid cases already passed.

The selector now classifies each grid mention locally, excluding explicit layout/card/image/form meanings while preserving data grids and independently requested tables. Integration assertions check the resulting generated `content.js`, not just the predicate. `npx jest --runInBand --runTestsByPath src/__tests__/layout-grid-is-not-a-data-table.test.ts src/__tests__/the-shape-he-named-was-the-one-thing-not-read.test.ts --silent` passed 16 tests after the fix. Diff whitespace checks passed.

This is a narrow presentation-selection repair, not implementation of arbitrary request-driven layout. Negated table requests and broader natural-language ambiguities are not covered by this change. The existing records engine still lacks a separate request-derived presentation model, and preserving input/persistence contracts must not be mistaken for visual originality. No provider generation, API restart, live UI replay, commit, GitHub publication, or benchmark advancement occurred in this step.

The separate `npx tsc --noEmit` check caught an invalid test fixture (`records` is an engine, not an AppKind). Corrected the fixture to the real `generic` kind and asserted that it resolves to the records engine. Reran the changed suite: 10 passed; reran TypeScript checking: exit 0. The six unchanged original shape tests were not repeated after this test-only correction.

### Issue 89 Efficiency Evidence Uses Real Checks

Re-read open GitHub Issue #89. Its efficiency comparison requires fewer actual checks with the same correctness verdict, not merely fewer selection decisions. The existing extension of `verify_joe_prompt_benchmark_50.ts` only recorded a fabricated passing result/duration and counted selected actions. It therefore did not establish that requirement.

Replaced that comparison in the same benchmark with a real isolated Node assertion against a small CommonJS fixture. Both naive and change-aware policies replay initial, unchanged, deliberately broken, repaired, and final states. Each actual child exit/result/output and measured duration is retained in `api/data/tests/verification-efficiency.json`. The gate compares every stage's verdict against the expected sequence, requires the defect and repair to execute, requires final verification to execute independently of focused receipts, and asserts a strictly lower execution count. No wall-clock speed assertion is used. The fixture check is a benchmark of the ledger policy, not execution through ToolService or full system acceptance.

Results: naive 5 executions; change-aware 4 executions plus 1 reused passing check; both verdict sequences passed, passed, failed, passed, passed. Latest measured child durations total 1000.097 ms naive and 703.375 ms change-aware. These measurements are not a general cost or performance claim. The existing 50 routing cases passed in 2277 ms (excluding process/module startup and the efficiency comparison).

Commands: `npx tsx src/tests/manual/verify_joe_prompt_benchmark_50.ts --verification-only` passed; `npx tsc --noEmit` passed. The full benchmark under `tsx` failed before cases because its ESM export getters cannot be monkeypatched by the existing provider stub. The canonical `npm run test:joe:prompt-benchmark`, with process-only `TS_NODE_TRANSPILE_ONLY=1` after separate type checking, passed all 50 cases and the actual efficiency comparison, exit 0. No provider generation, production action, new live UI evaluation, commit, or publication occurred. Canonical-pipeline fault/repair acceptance, the final matrix, supervisor review, and GitHub evidence remain separate requirements; this does not close #89.

### Canonical Flow Counts Real Check Executions

Strengthened the existing `verify_joe_full_engineer_flow.ts`, not a new orchestrator/harness. Its generated smoke/build checks append an independent exit/duration trace under the fixture's ignored `.joe` directory. The final verification now executes both fixture checks instead of the build check alone. The test asserts the exact sequence: smoke pass, build fail, repaired build pass, final smoke pass, final build pass. A duplicate focused smoke check or missing final check fails this independent assertion even if ledger/log summaries say otherwise.

Each run now uses a unique directory beneath `data/tests/full_engineer_flow`; prior test evidence is preserved instead of recursively deleting the shared test root. `npm run test:joe:engineer-flow` with process-only `TS_NODE_TRANSPILE_ONLY=1` passed, followed by a successful separate `npx tsc --noEmit` and diff whitespace check. Evidence: `api/data/tests/full_engineer_flow/run-3LP3aM/verification-evidence.json`, session `test-session-e2e-1789103429982`. Pipeline completed two phases, controlled self-fix succeeded once, and the five actual check exits match the required sequence. The fixture still uses a controlled planner and synthetic TypeScript diagnostic; its final gate is not the repository AGENTS matrix or live UI UAT.

New accounting defect confirmed while inspecting that evidence: `recordVerification` replaces older receipts for a check id, while `summarizeVerificationLedger.executedDurationMs` sums only retained latest receipts. Failed-attempt time therefore disappears after a successful repair. The saved-duration estimate likewise looks up the latest receipt instead of the duration at the reuse decision. These aggregates must not be presented as full-run cost measurements until cumulative accounting is repaired and survives compaction. Independent process traces remain valid evidence of the execution sequence. This is an open Issue #89 telemetry gap, not a reason to weaken reuse safety or rerun unrelated UI prompts.

### Cumulative Verification Accounting Repair

`verification-ledger.ts` now separates fixed-size cumulative accounting from its bounded latest-receipt cache. Every recorded execution contributes duration, including failed/cancelled/incomplete attempts; fingerprint time is accumulated at selection, including reuse checks; estimated savings capture the matching receipt's duration at the actual reuse decision. Receipt replacement and eviction no longer erase those totals. Numeric overflow/invalid counters mark accounting incomplete. Old ledgers without counters retain only reconstructible lower-bound evidence and explicitly report `accountingComplete:false` when prior history exists.

Focused command: `npx jest --runInBand --runTestsByPath src/__tests__/verification-ledger.test.ts src/__tests__/change-aware-phase-verification.test.ts --silent`, 33 passed. New coverage proves failed-attempt retention, savings unchanged by a later slower check, serialization/compaction, 110 executions beyond the 96-receipt limit, and incomplete legacy/malformed accounting. Separate `npx tsc --noEmit` and scoped diff whitespace checks passed.

Canonical flow rerun passed: `api/data/tests/full_engineer_flow/run-TJB88a/verification-evidence.json`. It preserves the independently measured five child-check sequence; ledger metrics correctly report four verification tool invocations (the final invocation runs two child checks), 2359 ms cumulative execution time, and 750 ms estimated saved time, with complete accounting. These are fixture measurements, not a claim about general product cost or full UI acceptance.

Verification environment correction: architecture/package guards and engineer-flow passed with process-only `TS_NODE_TRANSPILE_ONLY=1`. Several subsequent repair scripts refused startup because no test JWT value was provided. Stopped that batch, supplied an explicit process-only dummy test value plus offline mode, and resumed only the seven affected/not-yet-run repair gates. No real secret or persistent configuration was read or changed. Final results are recorded below when the remaining gates terminate.

All ten required commands finished successfully: `guard:architecture`, `guard:package-scripts`, `test:joe:engineer-flow`, `test:self-fix:build-context`, `test:self-fix:execution-safety`, `test:self-fix:typescript-repair`, `test:self-fix:typescript-missing-name`, `test:self-fix:typescript-number-to-string`, `test:self-healing:failure`, and `test:self-healing:success` (each via `npm run`, exit 0). Failure-loop coverage confirms progression still stops after the one permitted repair fails. No API restart, live UI replay, commit, or GitHub publication occurred in this accounting step; overall #89 and the broader objective remain incomplete.

### Independent Accounting Review Follow-Up

A narrow read-only review found one P2 inconsistency: selected/invalidated/reused counts still covered only the last 192 decisions while durations/savings covered the whole run. Added those three cumulative counters, updated them before decision eviction, and made the summary consume the same lifetime scope. Legacy persisted counters missing the new fields are explicitly incomplete. A new regression records 200 reuse decisions and verifies 192 retained detail entries versus 200 cumulative reuses and 2000 ms estimated savings after serialization.

Verification: focused ledger/phase suites passed 34 cases; separate `npx tsc --noEmit` passed; `npm run test:joe:engineer-flow` passed with the same process-only test settings. Independent canonical trace: `api/data/tests/full_engineer_flow/run-0ZaGvO/verification-evidence.json`. The other nine matrix commands above were not repeated for this reporting-only follow-up; their results describe the preceding accounting version, not a new complete matrix run. The reviewer confirmed closure of the specific P2 finding by static inspection, with no additional tests or broader acceptance claim.

Environment observation: `/api/health` remained healthy, but retrying visible in-app browser creation failed before navigation with `failed to write kernel assets ... os error 3`. No visible in-app UAT is claimed, and Chrome was not opened as an unrequested substitute. The running API has not been restarted to load the latest accounting changes. No publication or #89 completion claim is made.

### Live Replay Exposes History Send/Stop Race

Confirmed the previous test account had no active run, then restarted only the owned local API to load the accumulated changes. Health passed. Direct in-app opening returned queued, not proof of a visible tab; the browser-control runtime still could not initialize. Used the existing real-UI Playwright acceptance runner without opening external Chrome.

First replay evidence: `joe-ui-acceptance/2026-09-11T05-30-44-867Z/02-prompt.png`. The composer showed Stop before submission, the API received a stop request, no run was created, and the test account's active-run endpoint returned 200 with an empty list. Stopped only the acceptance runner after confirming this state rather than waiting out its 20-minute deadline. Root cause: `loadHistory` set the shared execution status to thinking; Enter interpreted that as Stop. Its finally block could also clear a genuine run's status.

Removed execution-status mutations from history loading. Added a regression protecting that separation and changed the UAT no-start deadline to depend on actual run evidence, not a transient busy icon. Focused queue-auth/evidence suites passed 11 cases; `npm run type-check` in web passed. Verified Vite served the changed component. No new API restart was needed for this frontend change.

### Same Prompt Now Runs But Acceptance Is Blocked

Replay `run-1789105095443`, session `6aa393c3a8e63123a28e51dc`, genuinely started after the fix and terminated failed. Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T05-37-51-439Z/report.json`; elapsed 439216 ms. Independent UI checks passed invalid-value rejection, record creation, persistence, filtering, switch/date controls, phone width, and readable heading; no page errors. Viewed the mobile screenshot: the execution-location suffix is gone, but the generic purple records presentation remains.

Joe's actual failure is `acceptance_criteria_unmet`, repeated after its bounded acceptance rerun. The request-derived denominator is now nine criteria, with five proven and four unprovable. Delivery correctly stops because unresolved criteria remain; no final verification receipt ran, and the independent runner correctly exits 1. However, the diagnostic says zero requested criteria not proven because it prints only `acceptance.unmet`, omitting `unprovable`. Exact criterion-level diagnosis and acceptance-evidence generalization remain required; do not reinterpret the UI checks or 100/100 browser score as full acceptance. No benchmark advancement, commit, or publication occurred. Local API now runs the latest backend code; its dev session is intentionally left running.

### Diagnose And Repair The Four Unprovable Criteria

Inspected the failed run's requirement-reading and final report events. The actual nine extracted items were `a title`, `a borrower`, `a due date`, `a returned toggle`, `filtering`, `validation`, `local persistence`, `a responsive layout`, and `focused checks while editing`. The first three failed exact-label matching due to grammatical articles. The last describes Joe's verification process, not a capability the artifact should render; the noun-phrase wording bypassed the existing imperative-instruction filter.

Added regressions reproducing the article and instruction failures, then fixed the general readers. Field proof prefers an exact literal label and only then removes a leading English article. Date evidence must come from the same field object and cannot fall through to label-only proof or borrow another field's date type. Explicit focused/targeted/incremental/change-aware checks during editing/development stay out of artifact criteria; a requested UI panel for those checks remains judgeable. Execution-verification obligations still belong to the planner/phase/ledger gates and are not waived.

Also corrected ReactProjectTool's blocked-delivery report: include unmet plus unprovable counts and criterion ids/verdicts; render acceptance as blocked whenever the actual delivery acceptance gate rejects it. This does not change gate eligibility. It removes the contradictory `Accepted with gaps` message on a rejected delivery.

Verification: article/records/instruction suites passed 29 cases. React-project plus acceptance regression run passed 140 cases and failed one old source-wiring assertion that required passing the raw accepted flag directly to the renderer. Updated that assertion to require the blocked override and complete unresolved count, then reran only the affected case: 1 passed, 56 intentionally skipped. Separate type checking initially caught missing callback type annotations in the new diagnostic, corrected with `JudgedCriterion`; final `npx tsc --noEmit` and diff checks passed.

Applied `verifyNamed` to the actual failed project's existing `src/content.js`, `RecordsApp.jsx`, store, and CSS, using the nine observed extracted phrases filtered by `isJudgeable`: all eight artifact requirements returned met, with zero model calls. Project root: `data/projects/d40bc1803a63e070fb932979/react-small-browser-based-library-chec`. This is source-contract re-evaluation, not a fresh UI pass. The failed live receipt is unchanged, API restart/live replay remains required, visual originality remains unresolved, and no commit/publication or benchmark advancement occurred.

### Acceptance Replay Passes But Browser Navigation Has A Server Error

Confirmed the previous test account's active-run endpoint returned 200 with no runs, then restarted only the owned local API. Health passed. Replayed the unchanged library prompt once without concurrent heavy tests. Run `run-1789106796288`, session `6aa39a6907f1e942cf0a7ad9`, terminated done; original runner exit 0, elapsed 352504 ms. Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T06-06-13-746Z/report.json`.

The provider extracted one compound inventory this time, not the prior nine separate phrases. Acceptance proved its eight declared artifact items, and one `frontend:final-quality` check ran on the correct new project root and passed: 24357 ms, fingerprint 365 ms, one execution, complete accounting, no reused result. Independent preview checks passed invalid-value rejection, creation, reload persistence, filtering, switch/date controls, mobile width, and heading fit; zero page exceptions. Viewed the mobile screenshot: the title is readable, but generic purple records styling remains. This run proves recovery of the acceptance path, not a new live exercise of every article-normalization branch.

Inspected the two failed HTTP responses rather than trusting runner exit 0: Gravatar 404 and `/api/browser/nav/goto` 502. The navigation request took about 41 seconds; the original harness did not preserve its error body. A single authenticated navigation to the existing preview through the same browser session subsequently returned 200/ok; this does not establish the original cause or prove a fix. No new project was generated for that diagnostic.

Found a harness defect: server errors were recorded but ignored by the exit gate. The runner now records `serverFailures` and rejects captured 5xx responses. Applied the new predicate to the retained report: it rejects the one navigation 502, while leaving the historical report unchanged. Thus this replay is functional/acceptance progress, not a fully healthy UAT pass. Detailed navigation-failure diagnosis, visual originality, final publication, and the broader evaluation remain open. No benchmark advancement or GitHub completion claim is made.

Final verification for the harness-only adjustment: `npx tsc --noEmit` passed; scoped diff whitespace checks passed. No extra full generation was run just to exercise this reporting gate.

### Issue 89 Environment Identity Audit (2026-09-17)

### Current-Version UI Replay (2026-09-17)

After confirming the previous test account had no active runs, restarted only owned local API handle 54240 with the current code as handle 27263 (approved local network access). Waited for the same handle to report listening; the early health failure during startup did not trigger a restart. Existing UI on 5002 was available. Replayed the unchanged library prompt once. Run `run-1789644903897`, session `6aabd064cf2107bbe3bcf1a7`, runner evidence `api/data/tests/joe-ui-acceptance/2026-09-17T11-33-25-702Z/report.json`, elapsed 391017 ms.

Joe reported done with one selected/passed final verification and zero reused checks. Independent creation, invalid-input rejection, persistence, filtering, toggle/date controls, mobile width, and heading fit passed with zero page exceptions. Inspected desktop screenshot: the generated board uses the new side composer and state lanes; generic wording and purple styling remain. Runner correctly exited 1 because `/api/browser/nav/goto` returned 502 with retained diagnostic `kind:timeout`, `attempts:2`; Gravatar 404 was the other failed response. API log shows the initial artifact-preview navigation succeeded in 1533 ms, followed later by another preview-ready event and a navigation failure after 41307 ms. This is not the previously repaired ERR_ABORTED supersession case.

A subsequent authenticated active-run read returned 200/runs:[], then one navigation of that same owned browser session to `http://127.0.0.1:4301/` returned 200/ok. The failure therefore remains intermittent during the task; its cause is not established and no timeout was increased or failure gate relaxed. UAT acceptance remains blocked on diagnosing it, not on repeating unrelated passing tests. No publication or completion claim.

Required-matrix follow-up: all ten AGENTS.md commands completed with exit 0, once each, in `api/data/tests/final-matrix-20260917-142057/report.json`; per-command full output is retained beside the report. Durations in milliseconds: architecture 19817; package scripts 15649; engineer flow 65991; build context 22115; execution safety 51958; TypeScript repair 58090; missing name 42423; number-to-string 39709; self-healing failure 51633; self-healing success 41830. Total measured command time 409215 ms. Used process-only TS_NODE_TRANSPILE_ONLY=1 with the separately passing API type check from this same code version, and dummy verification JWT/OFFLINE_MODE for the controlled tests.

Canonical evidence `api/data/tests/full_engineer_flow/run-wFWSq3/verification-evidence.json` proves the failed build, one successful repair rerun, reused unchanged smoke check, and distinct final gate. `npm run test:joe:prompt-benchmark -- --verification-only` then passed the efficiency comparison after the environment-fingerprint change: naive=5 executions, aware=4, reuse=1, same verdicts=true. Exact child durations and outputs are in `api/data/tests/verification-efficiency.json`. This comparison covers the ledger policy; the canonical scenario uses a controlled planner. Current-version real Joe UI acceptance and reviewable GitHub publication remain outstanding. These passes do not establish unrestricted autonomy or close the larger goal.

The requirements audit found that verification fingerprints covered files and runtime versions but omitted inherited process environment. Added a process-local HMAC of the complete environment to the toolchain fingerprint. Neither environment names nor values are persisted. Changing or removing an environment variable invalidates a passing receipt; process restart also conservatively invalidates it because the HMAC key is not persisted. Unchanged environment still permits reuse. This can over-invalidate after irrelevant environment changes; correctness takes priority until trusted relevant-environment declarations exist.

Exact checks: `npx jest --runInBand src/__tests__/verification-ledger.test.ts src/__tests__/change-aware-phase-verification.test.ts` passed 35 tests across two suites in 197.145 seconds; `npx tsc --noEmit` passed. Tests include mutation/removal of a synthetic environment variable, unchanged-input reuse, no raw environment retention, and existing AgentLoop/PhaseExecutor integration. No runtime server restart or new UI prompt occurred in this audit. Final required matrix, current-version real UI acceptance, reviewable commit/PR and publication remain outstanding. Previously pending short-field board browser test and type-check handles were also retrieved as terminal exit 0; the board report now derives its schema from the request, replacing the earlier explicit-schema limitation for that case only.

### Full UI Replay Environment Failure

Rechecked GitHub Issue #89 as open, including its final-matrix, system acceptance, measured efficiency, and supervisor review requirements. Started the unchanged library prompt using the permanent UI runner. Run `run-1789110254701`, session `6aa3a7e9f3d822d048e48855`, ended failed after 378316 ms runner elapsed; evidence `api/data/tests/joe-ui-acceptance/2026-09-11T07-03-46-617Z/report.json`. No preview and no final verification receipts existed. Provider connections failed and dependency installation waited under the API process's inherited sandbox restrictions. Read-only comparison of the same npm registry HEAD request returned `fetch failed EACCES` inside the sandbox and HTTP 200 outside it. This establishes an environment restriction, not a reason to weaken Joe's acceptance gate.

Requested owned-run cancellation, but the run had already finished: stop returned 200 with `stopped:false`, the runner exited 1, and an authenticated active-run read returned 200 with no runs. Only then stopped API handle 63729 and started approved local API handle 54240 outside the network-restricted sandbox. It reported listening on 5000 with auto-deploy disabled; health returned 200. Started one replay of the identical prompt after readiness. No production action, secret modification, or implementation change was used to address this environment failure. The prior runtime-start choice was an evaluator environment error and the failed run is retained, not counted as a passed prompt.

### Full UI Replay After Environment Correction

### Request-Driven Record Board Work In Progress

Inspected the actual authoring path: records/ledger engines deliberately preserve derived field contracts instead of letting a weak model replace them, but presentation remained fixed. Added `requestedBoardField` to the existing generator: explicit board/kanban requests can group records by a declared status field or the sole unambiguous select field. Table requests retain priority; ambiguous grouping is not invented. `content.boardField` now drives state lanes using the existing row renderer, edit flow, and storage. Unknown persisted values remain visible rather than disappearing. The board workspace uses a side composer on desktop and a single column on phones, with unframed sections. This is one reusable presentation capability, not completion of visual originality.

Focused verification: requested-record-board and layout-grid suites passed 21 tests; API type check passed before the last layout-only changes. Permanent `verify_record_board_ui.ts` proved create, edit to another state lane, reload persistence, and mobile width in real Chromium. Desktop/mobile images were inspected; screenshot capture settles animations. The browser fixture supplies an explicit schema and real generated store, not the full planner/API path. An initial short request (`Build an equipment return board with title and a returned toggle.`) produced fallback Title/Details/Value/Date/Status fields instead of its named fields; this parser gap is retained as open, not hidden by claiming the explicit-schema test proves language understanding. Generic entity labels and contextual titles also remain unresolved. Existing duplicate `kind` output warning remains. The running Joe API has not yet loaded this generator change; same-prompt UAT, broader regression, final gates, and publication are pending.

Run `run-1789110814545`, session `6aa3aa1acef06d5d4acc3998`, completed the identical library prompt after correcting local API networking. Runner exit 0; elapsed 332987 ms. Report: `api/data/tests/joe-ui-acceptance/2026-09-11T07-13-16-016Z/report.json`. Final receipt done; one selected/passed final verification, zero reused receipts. Independent checks passed invalid-input rejection, record creation, reload persistence, filtering, returned switch, native date input, mobile width, and heading fit. Zero page exceptions and zero captured server failures. The sole failed HTTP response/console error was Gravatar 404, not browser navigation. Active preview is `http://127.0.0.1:4301/`, generated root `data/projects/6efef59df3525e370602267e/react-small-browser-based-library-chec`.

Inspected both desktop and mobile screenshots. Readable headings and functional controls are proven, but generic purple Records/Add record panels persist. Joe's 100/100 browser score and the runner's exit 0 do not establish visual originality or domain-appropriate interaction design. The same prompt remains open for that capability gap; do not advance the 500-prompt sequence or claim Issue #89 publication/completion. Request-driven presentation and a visual quality gate that can detect repeated generic scaffolds are the next implementation investigation, not another blind run of the same prompt.

### Navigation Supersession Follow-Up

Component interaction follow-up: added permanent `api/src/tests/manual/verify_browser_navigation_ui.ts`. It bundles the real React `EmbeddedBrowser` in Chromium while replacing only stream/translation/configuration boundaries and controlling HTTP response order. Initial execution could not resolve runtime files inside the filesystem sandbox; approved local execution succeeded. The first test assertion incorrectly expected an animation class; inspection showed the production component uses inline animation, and the test now checks that actual style. It then exposed a real defect: `browser:session_status` applied another session's URL before checking ownership. Added an early exact-session guard and reset `isConnected` on session change.

`npx tsx src/tests/manual/verify_browser_navigation_ui.ts` now passes six interaction scenarios: stale success, stale failure with newer loading preserved, explicit supersession, genuine current failure, old-session response, and foreign-session status. The foreign-session assertion failed before the product patch and passed afterward. Evidence: `api/data/tests/browser-navigation-ui/report.json` and `component.png`; inspected the screenshot, which intentionally contains only the isolated toolbar and blank stubbed stream, not a generated application or full visual UAT. API `npx tsc --noEmit` and web `npm run type-check` both passed after all edits. No model requests or generated-project rebuilds were made by this test. Full Joe prompt replay, visual originality, final batch verification, and GitHub publication remain outstanding.

Live follow-up: verified the owned test account returned HTTP 200 with `runs:[]`, stopped only local API handle 67589, and started updated API handle 63729. An initial health probe arrived before startup finished; the same live handle was observed until it reported listening, without another restart. The real Chromium/API race then returned older=409/nav_superseded/attempts:1 and latest=200 with the latest target URL. The cleanup navigation to port 4300 failed with a genuine 502/connection-refused because the prior preview was no longer listening. Restarted the existing generated project's Vite server on 4300 (handle 44374); the initial sandboxed launch was denied filesystem access, and the approved elevated local launch succeeded. A separate restoration then returned 200/ok at 4300, and Joe UI 5002 returned 200. The combined probe exited 1 due to cleanup failure and did not write its success-only report; preserve that distinction rather than calling the entire probe green. The race behavior itself passed its real-route assertions. No application generation was performed. The in-app browser tool again failed to initialize with `failed to write kernel assets ... os error 3`; visible in-app QA is not established.

Controlled same-session real API race reproduced an older navigation returning 502/ERR_ABORTED while the newer navigation returned 200. The temporary loopback target delayed the first response; the owned browser was restored to its existing preview afterward. This demonstrates a concurrency defect, not the cause of the earlier unexplained 41-second UAT failure.

Added page-scoped navigation ownership in `navigation-order.ts` and `/nav/goto`: superseded requests return 409/nav_superseded without retrying over the newer target. Older cleanup cannot clear the newer ownership. `EmbeddedBrowser.tsx` ignores stale asynchronous navigation results, invalidates them on session cleanup, and does not classify explicit supersession as degraded quality. Genuine unsuperseded failures remain failures.

Verification: `npx jest --runInBand src/__tests__/navigation-order.test.ts src/__tests__/browser-nav-supersession.test.ts src/__tests__/browser-nav-retry.test.ts` passed nine tests across three suites, including route-level interruption, genuine failure, and access rejection. Route tests use controlled page doubles, not Chromium. API `npx tsc --noEmit`, web `npm run type-check`, and `git diff --check` passed. Type checks preceded addition of the route test; Jest compiled that test successfully. No new model generation was used. The running API has not yet been restarted with this fix. Real-browser post-fix verification, frontend asynchronous interaction coverage, full UAT, final batch gates, and GitHub publication remain pending.

### Bounded Navigation-Failure Evidence

#### September 17: Navigation Timeout Investigation

Live history/navigation follow-up: loaded the ownership patch in the owned local API (handle 87012) after authenticated active-run status was empty. Added permanent `verify_live_navigation_history.ts`, using an owned test session, authenticated real routes, an ephemeral loopback fixture and guaranteed preview restoration. The first fixture held response headers and Back returned false; it was not a valid committed-history scenario. Changed the fixture to commit its document and hold a module instead. Final command exited 0: newer Back and Refresh returned 200 while each older goto returned 409; ordinary Back/Forward history passed; saved preview restoration returned 200. A new committed goto truncates forward history, so forward-overlap remains controlled route-test coverage, not real-browser overlap evidence. No generation or production operation occurred. API type checking passed before the final fixture-only timing adjustment, which executed successfully via tsx. Requested independent static closure of the navigation finding; whole-task acceptance/publication remain pending.

Latest-version UI acceptance after loading the eligibility/fingerprint fixes: run `run-1789650211953`, session `6aabe5211684772b342194a7`, report `api/data/tests/joe-ui-acceptance/2026-09-17T13-03-17-263Z/report.json`. Same library prompt, runner exit 0, elapsed 156371 ms. One final check selected/passed, zero reused; all independent validation/create/persistence/filter/toggle/date/mobile/heading checks passed, no page exceptions or server failures. Only Gravatar returned 404. Inspected desktop/mobile screenshots: generic purple presentation and literal/general labels remain; no visual-originality claim. The visible in-app browser limitation remains. Authenticated active-run query was empty before restarting the owned local API; current API handle is 67591.

The independent remaining-diff review found a concrete P2: Back/Forward/Refresh did not supersede a pending goto retry. Patched all three authenticated route handlers to use the same page ownership token, suppress obsolete responses, and release their token in finally. Three new route interleaving tests prove a newer history/reload action prevents the older goto retry from overwriting its destination. Browser supersession/order/retry suites passed 12 tests. These are controlled route tests, not a real-browser replay. The preceding UAT predates this narrow navigation patch; the running API has not loaded it. Review covered navigation, masking-related diffs, acceptance honesty, and image-size changes but did not exhaustively audit generated templates/blueprints. GitHub publication and #89 acceptance remain pending.

#### September 17: Verification Fingerprint Boundary Repair

Post-eligibility final regression (2026-09-17): all ten required AGENTS.md commands passed on the current source, with no intervening source edits. Report and per-command logs: `api/data/tests/final-matrix-20260917-155745/report.json`; total measured command duration 138671 ms. Current canonical trace: `api/data/tests/full_engineer_flow/run-fEKguv/verification-evidence.json`. The verification-only benchmark also passed again: naive 5 versus aware 4 executions, one reuse, same verdicts; durations remain in `api/data/tests/verification-efficiency.json`. The independent reviewer confirmed static closure of the four reported non-check option modes and found no additional bypass within that specifically bounded review; it did not run duplicate tests or grant full #89 acceptance. Current-version live UI acceptance, broader accumulated-diff review, GitHub commit/PR proof, and publication remain outstanding. Do not confuse this checkpoint with completion of #89 or the 500-prompt objective.

Shell verification eligibility follow-up: 30 permanent command-contract cases exposed 16 false positives before repair, including `echo jest`, command chains, redirections, non-testing help/list modes, and stateful/fix modes. Eligibility now recognizes one expansion-free invocation with an anchored executable/subcommand contract; ambiguous shell syntax is not reusable or final verification. Ordinary execution still goes through ToolService. Two new phase tests prove `echo jest` and a test chained with a stateful action execute twice as ordinary work instead of being cached, then are rejected as phase verification. Four focused suites passed 80 tests; after adding those integration cases, all 13 phase tests passed (82 unique cases across the four suites).

Reviewer follow-up found additional non-check modes (`tsc --init`, `tsc --build --clean`, `jest --clearCache`, `eslint --print-config`). Replaced broad checker argument acceptance with a small positive option contract: unknown options are ineligible, TypeScript requires exactly `--noEmit`, ESLint permits path operands only, and Node test mode rejects preload/eval options. Seven additional negative cases cover these and unknown/UI/preload modes. All four focused suites now pass 89 tests. This is intentionally conservative: unsupported valid commands may still run as ordinary uncached actions but cannot silently serve as final verification. The preceding canonical pass predates only this option restriction; final regression/review remains pending.

Canonical fixture migration retained its assertions and independent execution trace. Running its build-error script under Node's TAP reporter changed the error shape and correctly failed self-fix; switching to npm scripts without discovery was also correctly refused by the planner. The final fixture creates its manifest through ToolService, runs real engineering discovery before planning, and invokes declared `npm run build` / `npm run check`. `test:joe:engineer-flow` then passed, with evidence `api/data/tests/full_engineer_flow/run-Hv2CV4/verification-evidence.json`: one failed build, one repair rerun, unchanged smoke reused, final smoke/build each once. The mocked LLM plan remains documented. This command eligibility policy is not proof of arbitrary package-script purity; executable verification scripts remain project contracts governed by ToolService. Requested another bounded static review; no final acceptance, live reload/UAT, or publication is claimed.

Verification-tool eligibility repair: eight new planner/phase regressions failed against the old behavior for `read_file`, `write_file`, `project_detect`, and an unregistered checker. Planning now retains only supported verification tools, and phase execution rejects unsupported requested/resolved contracts before executing them or selecting a receipt. Removed the `project_detect` fallback and the arbitrary checker-name/metadata eligibility escape. One test fixture initially let its preliminary quality check inherit final mode; explicitly marking that preliminary check focused isolates the intended no-bogus-final-receipt assertion. All 50 ledger/phase/planner tests and API type checking now pass. Shell-command substring eligibility remains OPEN, so neither reviewer P1 is claimed fully closed. Existing canonical fixture commands such as `node check.js` need an explicit executable verification contract as part of that next repair; do not relax the new gate to restore arbitrary-tool success. No runtime reload, new UAT, publication, or full-matrix rerun was performed after this patch.

Final matrix after the target-identity patch: all ten AGENTS.md commands exited 0; exact durations and per-command logs are in `api/data/tests/final-matrix-20260917-153522/report.json`. No source edits occurred while this matrix ran. This is regression evidence, not acceptance of #89.

Independent bounded static review from the existing development task identified two P1 blockers, confirmed by reading the current implementation: (1) `ensurePlanFinalVerification` promotes any nonempty tool name to final verification; the PhaseExecutor verification-task path accepts arbitrary successful tools and falls back to `project_detect` for unresolved names. Thus a read/detection result can masquerade as final quality evidence. (2) `isVerificationTool` recognizes arbitrary shell commands by substrings, including `echo jest` and a test followed by a stateful command; reusing such a receipt can skip non-verification work. Neither finding is fixed by the passing matrix. Next action is permanent reproductions and fail-closed executable verification eligibility, including rejection of mixed/ambiguous shell commands. Do not publish or close #89 before resolving these findings and revalidating affected gates. The reviewer made no edits and ran no duplicate tests; its review is not full acceptance.

Runtime-target identity follow-up: a permanent regression demonstrated `reuse` after changing a target's query or fragment with the same build revision and empty explicit tool arguments. The display-safe target intentionally drops those components, but had also been used as its complete identity. The fingerprint now additionally includes a process-local HMAC of the complete supplied runtime target. Receipts still store the display-safe target only. The regression proves unchanged-target reuse, query/fragment invalidation, and non-retention of a fixture token. It failed before the change and passed afterward; ledger/phase suites passed 37 tests. This closes a target-state stale-proof case without storing raw URL credentials or weakening invalidation. Final batch verification and current-version live acceptance remain pending.

Review of #89 found an actual stale-proof defect: file paths and contents were concatenated without framing. A regression with `src/a=Xsrc/b, src/b=Y` followed by `src/a=X, src/b=src/bY` produced identical fingerprints despite changing both files. The new permanent test failed before the patch. `verification-ledger.ts` now hashes each file's JSON-encoded relative path and byte length before its content, preventing this ambiguous concatenation. No successful receipt may survive that content redistribution.

Verification after the repair: ledger and phase-integration Jest suites passed 36 tests; `npx tsc --noEmit` passed. `TS_NODE_TRANSPILE_ONLY=1 npm run test:joe:prompt-benchmark -- --verification-only` passed with five naive executions versus four aware executions, one reuse, and identical verdicts. Measured aware execution times were 246.32, 241.92, 218.07, and 236.73 ms, not a wall-clock correctness assertion. `npm run test:joe:engineer-flow` with process-local test/offline environment passed; independent evidence is `api/data/tests/full_engineer_flow/run-4NO5kj/verification-evidence.json`. It proves one unchanged smoke execution during editing, the failed build plus one repaired rerun, and both final fixture checks once. This uses the documented mocked planner and controlled failure, not unrestricted autonomy. The current running API and earlier UAT predate this latest ledger patch. Final applicable regression gates, runtime acceptance, independent review, and GitHub publication remain pending.

Same-prompt UAT after loading diagnostics: `npx tsx src/tests/manual/verify_joe_prompt_ui_acceptance.ts` exited 0. Evidence directory `api/data/tests/joe-ui-acceptance/2026-09-17T12-20-50-752Z`, run `run-1789647665959`, session `6aabdb2f8db583b51055ce84`, elapsed 194535 ms. Selected 1 / passed 1 / reused 0. Invalid-input rejection, creation, persistence after reload, filtering, returned control, date input, mobile width, and heading fit all passed. Zero page errors and server failures; the only failed HTTP response was Gravatar 404. The live preview navigation returned 200 in 21524 ms, not the prior 502. This is a passing replay, not proof that the intermittent timeout is repaired: this batch added diagnostics, not a navigation behavior fix.

Inspected both saved screenshots. The board layout and controls render, but purple generic record presentation, literal request-like title, generic entity wording, and a checkbox-like returned control remain visible limitations. Desktop/mobile captures are headless evidence, not proof that the in-app browser was visible to the user. Do not advance the 500-prompt sequence or claim overall design originality from the passing runner. Issue #89 was reread on GitHub and remains OPEN; no commit, PR, publication, or supervisor approval is established for this batch.

Live-route diagnostic verification: after authenticated `/api/runs/active` returned 200 with an empty list, restarted only the owned local API. New API handle 78739 loaded the diagnostic changes; health returned OK. Added `verify_live_navigation_diagnostics.ts`, which requires an explicitly owned local account/session and restore URL, checks for active tasks, serves an unfinished module on an ephemeral loopback listener, calls Joe's real authenticated navigation route, closes its listener, and restores the preview.

The first probe exceeded its 65-second client deadline and its original live-port restore target returned 502. The saved preview remained available and was restored with HTTP 200. A warm-session retry also exceeded that deadline, disproving the initial assumption that browser startup alone explained it. Inspection found the browser manager default is 45 seconds per navigation, while `app-audit.ts` sets a borrowed page's default to its audit timeout; the client budget did not cover the manager's two attempts. Corrected only the diagnostic runner's deadline to 110 seconds and verified the restore target before measuring. No product timeout was increased. Final probe exited 0: HTTP 502, `{kind:timeout, attempts:2, documentState:interactive}`, followed by restore HTTP 200 to `/project-preview/6aabd064cf2107bbe3bcf1a7/index.html` on port 5002. This proves real-route collection, not the cause or repair of the original UAT failure. API type checking passed after the initial runner addition; the later runner-only warmup/deadline changes executed successfully via tsx.

The visible in-app browser control still fails to initialize (`failed to write kernel assets`, OS error 3). The separate app panel-open request returned queued, not confirmed visible. No new prompt generation, GitHub publication, or production operation was performed. The existing API startup still writes its configured git safe-directory entries; do not describe the restart as having no host-side effects.

Follow-up controlled Chromium experiment: `npx tsx src/tests/manual/verify_shared_page_navigation.ts` exited 0. Concurrent same-target navigation produced `interrupted/completed`, whereas a deliberately delayed module produced two `timeout` results with `document.readyState=interactive`; navigation to a ready page then recovered. The fixture uses an ephemeral loopback server and its own headless browser, closes both, and makes no model calls. This distinguishes two failure signatures but does not reproduce the UAT root cause or constitute visible in-app acceptance.

Added a bounded document-state diagnostic to failed `/nav/goto` responses: only `loading`, `interactive`, `complete`, or `unavailable`, with a 300 ms maximum diagnostic wait. Supersession is checked again after collecting it. The UAT evidence helper preserves only that allowlisted value; old reports remain supported. This state describes the page at observation time, not proof that the requested document committed, and does not establish which resource or controller caused a timeout. Failed navigation remains HTTP 502. Focused Jest command covering `navigation-diagnostics`, `ui-verification-evidence`, `browser-nav-supersession`, and `browser-nav-retry` passed 29 tests in four suites. `git diff --check` passed. Runtime reload, actual failing-route observation, same-prompt acceptance, and GitHub publication are still pending; do not describe the original timeout as fixed.

Re-read the persisted evidence for `run-1789644903897`, rather than regenerating the application. The live-preview event (sequence 168, timestamp 1789645172057) precedes the delivery audit's `watching` event (sequence 173, timestamp 1789645172373) by 316 ms. The audit finishes at timestamp 1789645261359, about 89 seconds later. `app-audit.ts` emits `watching` after borrowing `getBrowserSession(watchSessionId).page`; the pipeline supplies the same panel session used by the preview. Its navigation calls are not coordinated by the route's `claimNavigation` ownership. Thus two controllers can navigate the shared page during this interval. This is a demonstrated coordination gap, not proof that it caused this particular timeout.

The earlier builder audit finished at 11:38:45 UTC, before the live-preview event at 11:39:32 UTC; do not confuse it with the overlapping delivery audit. The acceptance report still fails on HTTP 502 with `navigation.kind=timeout` and two attempts, despite all independent application behavior checks passing. A subsequent real navigation returned 200 in approximately 244 ms. The owned API process remains live. No timeout was raised, failure gate weakened, generation rerun, runtime restarted, or product code changed for this investigation.

Next evidence needed: a bounded shared-page reproduction covering concurrent preview navigation and delivery audit navigation, including navigation lifecycle/request completion, before choosing coordination or readiness changes. Existing evidence does not distinguish interrupted navigation from a stalled document/module load. GitHub publication and full objective completion remain unproven.

The prior 502 cannot be diagnosed retrospectively from its status alone. Added `readNavigationFailureEvidence` to the existing UAT evidence helper and wired it to failed `/api/browser/nav/goto` responses. It retains only a classified cause (timeout, navigation interruption, closed page, connection, DNS, TLS, or unknown) and a validated attempt count, never the raw response detail or arbitrary page/credential fields. Body reading has a one-second deadline; at most 16 navigation diagnostics are collected per run; the report awaits those bounded reads before writing. Evidence-read failure is explicit and does not turn the server-error gate green.

`npx jest --runInBand --runTestsByPath src/__tests__/ui-verification-evidence.test.ts --silent` passed 17 cases, including stalled-body timeout without retry, classification, and non-retention of arbitrary response data. `npx tsc --noEmit` passed.

Controlled real-route probe, without generating an application: verified the existing test account had no active runs, allocated and closed an ephemeral loopback listener, navigated its owned browser session to that now-unavailable local target, and read the actual API failure through the new helper. Result: HTTP 502, `kind:connection`, `attempts:2`. A finally-path navigation restored `http://127.0.0.1:4300/` with HTTP 200/ok. This proves diagnostic collection and existing recovery in a controlled connection-failure case; it does not reproduce or repair the original unexplained 41-second navigation failure. No production action, full prompt replay, commit, or publication occurred.
