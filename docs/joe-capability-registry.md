# Joe Capability Registry

This registry records reusable capabilities proved by the live evaluation. It is updated when a test exposes a general gap; it is not a claim of universal autonomy.

| Capability | Implementation | Selection | Evidence | Known limitation |
| --- | --- | --- | --- | --- |
| Inspectable capability-route decisions | `decision-profiles.ts` + `decide_capability_route` through `IntentParser`, `PlanningEngine`, and `PhaseExecutorTool` | An explicit least-setup/local-vs-external decision for OCR, geocoding, storage, speech, or routing becomes one read-only tool step | Focused profile, tool, intent, planner, and phase-handoff tests prove setup/cost/privacy/reliability/rate/license/maintenance constraints, one required user action at most, rejection of forged handoff output, and no model call for explicit decisions. 2026-09-22 visible local UI UAT completed two unseen requests in 7 seconds each. | Profiles describe route classes rather than live provider discovery; an external connection, key, account, or paid route is never activated by a decision receipt |
| Persisted browser ownership and authorized live subscriptions | `wsHub.canAccessBrowserSession`, `ws.authorizedLiveSessions`, browser status route | Every browser request/stream and live chat subscription resolves its authenticated owner | 2026-09-07: 25 focused tests pass; `node scripts/verify-browser-account-isolation.cjs` proves real HTTP and WebSocket denial across two synthetic identities, owner access, invalid-token rejection and fixture cleanup; guest Browser panel remains connected | Mongo lookup is unit-tested with a mocked query, not a live Mongo deployment. Artifact/production-preview authorization is a separate open boundary; this row does not certify complete tenant isolation |
| Read-only workspace inspection | `IntentParser` + `ProjectPipelineTool` | Explicit read-only contract | Live P01: root listing and README summary completed without writes | Broad unfamiliar requests still need deeper live coverage |
| Explicit file creation and read-back | `file-intent.ts` + `PlanningEngine` + `ToolService` | Named relative file, declared content, optional read-back | Live P02: exact three-line file written and read back | Only bounded explicit file contracts use this fast path |
| Nested file destinations | `file-intent.ts` preserves an explicit folder relationship | Named folder plus “inside it” / equivalent wording | Live P03: `joe-prompt-03/README.txt` created and read back; root remained untouched | More complex path language still belongs to the normal planner |
| Bounded terminal diagnostics | `isBoundedTerminalDiagnosticRequest` + `PlanningEngine` allowlist | Explicit local diagnostic/check request with read-only constraints | Live P04: real `node --version` and `pwd` output appeared in chat and Terminal | The allowlist is deliberately bounded; it does not interpret arbitrary shell text |
| Explicit multi-file reading and reporting | `parseExplicitReadFilesRequest` + `parseExpectedReadMarkers` + `composeReadFileReport` | Named safe relative paths in a read-only request | Live P05: both files read from the active workspace; chat reported exact contents and line counts. Live P06: two supplied markers returned `PASS` | Marker verification requires an explicit marker value; Joe reports that limit instead of guessing |
| Explicit directory inspection and entry confirmation | `parseExplicitDirectoryInspectionRequest` + `PlanningEngine` + `composeDirectoryReport` | Named folder/directory in a read-only request, with an optional expected entry | Live P08: `inspect_directory` opened `joe-prompt-03`, listed `README.txt`, and confirmed it in chat | The current contract is bounded to a named directory and depth-one listing; recursive and ambiguous targets remain on the normal planner path |
| GitHub credential lifecycle | `/github/connect`, `/github/disconnect`, encrypted user secrets | GitHub menu action | Static/API contracts pass; disconnect removes durable and offline credentials | Live connected-state click requires a user-provided valid token |
| GitHub sync telemetry separation | `broadcastTerminalLine` from repo connect route | Repository selection | A prompt session id is never inherited by manual GitHub selection; ownerless sync output is dropped by the privacy boundary rather than appearing in a chat or another user's panel | A dedicated GitHub-panel progress surface can be added later if operational progress needs to be shown without creating a run session |
| GitHub panel/session isolation | `Joe.handleSelectRepo` keeps panel sync independent from the active prompt | Pressing `Done` in the GitHub repository picker while another prompt is running | The repository connect call no longer receives `activeSessionId`; the sync cannot be attributed to the prompt's conversation (`github-connect.test.ts`) | A live connected-account retest still requires a valid user token |
| Self-healing execution boundary | `RepairTicketService` → `SelfFixService` → `SelfFixExecutionService` | Failed phase with trusted context | Permanent failure/success and TypeScript repair checks pass | One repair attempt is intentionally the hard limit |
| Request-derived application build with browser evidence | `ProjectPipelineTool` → `ReactProjectTool` → `ProjectRunTool` → `SelfQA` | A request with named fields and behaviors, followed by a real preview target | Fresh live P09 rerun: compiled `react-customer-6ce4f1b0`, served `localhost:4300`, measured 7 interactions across 3 viewports, and returned `89/100` with 2 findings | Generated controls and unverified behavior remain findings; a successful build is not a clean acceptance pass |
| Provider-outage fallback with honest retry policy | `AIGeneratorTool` + request-derived blueprint engine | Engineering pipeline with a bounded blueprint and provider failure | Live P09 recorded provider failures, used one bounded fallback path, and avoided a second long authoring retry before continuing to build and QA | QA analysis can still incur provider wait when the entire mesh is unavailable; local model/provider health remains an external dependency |
| Compact neural work surface | `NeuralThinkingIndicator` + `NeuralTraceView` + `neuralTrace` | Live session telemetry with translated stage mapping | Browser DOM and UI verification passed: one readable live line, opt-in details, no internal tool name in the Arabic primary line, and technical detail kept out of the default surface | Full technical history is still available when expanded or in Logs; this does not replace the underlying execution evidence |
| Semantic form-field QA | `semanticTypeForField` + browser form probes | Infer input semantics from the field's own tag/name/label, then challenge invalid values | P13 regression and live QA: `message` is no longer misread as `age`; email/telephone/number/date/time contracts are tested with valid and invalid inputs | Generated forms still depend on accessible labels and native constraints being present |
| Arabic semantic form-field QA and anchor evidence | `semanticTypeForField` Arabic vocabulary + `probeControls` structured anchor results + `ProjectEditTool` acceptance evidence | Arabic-only accessible labels identify email, telephone, date, time, and numeric fields; in-page CTAs are accepted only when their visible target exists | Live P18 run `run-1788846533077` returned Browser QA `100/100` and acceptance `6/6`: `احجز استشارة` resolved to `#contact`; `رقم الهاتف` was challenged as `tel`, rejected letters under `[0-9+ ]{7,20}`, accepted the valid fill, and produced zero semantic failures | Vocabulary coverage remains evidence-driven; fields without an accessible name or native constraint cannot be honestly certified |
| Stateful disclosure and accordion QA | `behaviour-audit.ts` disclosure probe + typed `AppAudit.disclosureEvidence` | Rediscover visible `aria-expanded`/`aria-controls` triggers, open each in sequence, require exactly one expanded item, then close the final item with Enter | Live P19 opened all three FAQ questions in the visible preview, confirmed single-open behavior, and closed the focused final trigger with Enter; the final Joe audit returned `100/100` and acceptance `5/5` | Requires semantic disclosure attributes and visible triggers; custom widgets without an accessible disclosure contract cannot be certified by this probe |
| Portable static Vite preview | `ReactProjectTool.portableViteBuildArgs()` | Every generated Vite production build receives a relative base so Joe's project-preview gateway can serve nested hashed assets | P19's repaired production bundle rendered through `/project-preview/<session>/index.html` in the independent visible Browser tab and Joe's phone preview | Applications that intentionally require an absolute deployment base must declare and verify that deployment contract separately |
| Responsive evidence ownership | `app-audit.ts` completed route/viewport evidence set | Preserve every in-page anchor in the dedicated phone pass and skip a later route/viewport sweep only after that exact context completed | P19 final audit measured desktop, tablet, and phone, retained the FAQ link in phone evidence, and finished without the prior duplicate-phone budget failure; focused QA suites passed `206/206` | The shared audit deadline remains bounded; genuinely unvisited routes or states still fail rather than borrowing evidence from another context |
| Responsive header inspection and repair | `ui-inspection.ts` + `ui-repair.ts` measured mobile-header gate | Detect multi-row/offscreen header controls at phone width and apply a bounded surgical repair | P13 live mobile preview showed a compact two-row header with brand/theme and reachable navigation; focused QA tests cover detection and idempotence | Wider visual quality still requires task-specific inspection of hierarchy and content |
| Measured heading hierarchy repair | `design-audit.ts` evidence + `ui-repair.ts` `repairFlatHierarchy` | When computed heading/body sizes fall below the measured ratio, append an escalating, idempotent CSS repair with sufficient precedence | Focused `ui-repair.test.ts` proves a `.hero h2` override is superseded; build passed; local commit `a7c43c5f` | A fresh generated-app rerun is still required to prove the repaired page's measured ratio moves in the live Browser panel |
| Session refresh recovery | `Joe.tsx` persisted-history rehydration + immediate title update | On `sessions:refresh`, update the affected chip and reload only the active conversation without clearing it | P13 first rerun reproduced the empty-chat-after-completion defect; reload and persisted message evidence confirmed the repaired session remains visible | Long-running background sessions still need broader multi-tab live coverage |
| Request-derived acceptance for contact forms | `acceptance.ts` `contact_form` criterion + delivery mapping | Treat a requested contact/Visit form as an explicit source-backed acceptance criterion | P13 final rerun proved `5/5`, including source form evidence and an actual 3-field browser submission | The criterion proves presence and source wiring; richer backend delivery requires a separate server requirement |
| State-driven exploratory browser QA | `behaviour-audit.ts` stateful control frontier | Re-catalogue after each interaction and re-test menus, tabs, filters, and disclosures even when they appeared in the baseline | Focused deep-self-QA guard proves stateful controls are eligible for exploration; the next live run will measure the new exploratory count | Exploration is bounded by the shared time budget and cannot prove paths hidden behind authentication or unavailable data |
| Shared persistent-browser viewport control | `ui-inspection.ts` `applyViewportSize` + `manager.ts` capture lock, used by behaviour, UI, and visual audits | Pause live JPEG capture for the full viewport transition, use Playwright first and CDP metrics only as a verified fallback, publish measured dimensions, then restore the borrowed page through the same helper | P17's exact manager diagnostic kept streaming active and proved `1280→390→820→390→1280` with `window.innerWidth` matching every transition; the real Joe rerun then measured all 3 viewports and returned `100/100` | Persistent-profile, headed, and remote-CDP browser modes still need their own live transition matrix; requested dimensions are never accepted as evidence without read-back |
| Form-shape inference from field semantics | `ReactProjectTool.ts` `SHAPE_ASKS` | A request naming an email field and submit action selects the Contact section even without the word “form” | Focused regression proves the section is selected and filler `Features` is excluded | Provider outage can still reduce the acceptance contract when the request reader cannot recover all named requirements |
| Generated-link invariant at serialization | `ReactProjectTool.ts` final rendered-anchor guard | Before writing a single-page bundle, compare every generated CTA target with the sections that will actually render | Live QA reproduced two dead `#features` links; focused regression covers the valid Contact fallback; the final guard repairs stale CTA state before serialization | A fresh end-to-end Browser rerun is still required to confirm the running service has no other late link producer |
| Browser-eye protection from internal terminal boot | `AutoOpenManager.ts` `normalizeAutoOpenToolName` | Normalize `execute:<tool>` and object-shaped tool names before auto-opening a visible panel; exclude `terminal_manager` after normalization | Live log reproduced BrowserWS disconnect immediately after `execute:terminal_manager`; web type-check passes and the wiring guard covers the exclusion | The interrupted live run must be rerun to prove the browser audit reaches its final report |
| Requirement-derived editorial acceptance | `named-requirements.ts` deterministic source contract | When a request asks for an article/blog page, inspect generated headline, article data/rendering, and search state before asking the provider judge | Focused React generation and acceptance-contract tests pass; article cards and search are now provable from source when the provider is unavailable | Visual polish and browser interaction still require live Browser QA evidence |
| Expanded shared browser exploration budget | `app-audit.ts` one shared adaptive deadline | Give route, state, and responsive discovery one bounded window of 90–180 seconds instead of truncating at the former 60-second floor | `deep-self-qa.test.ts` and the full engineer-flow verification pass after the change | A blocked browser session or unavailable preview remains a genuine QA blocker; more time cannot prove an unreachable target |
| Coverage-aware browser QA recovery | `ProjectPipelineTool` coverage retry before `project_repair` | When exploratory QA reports `qa_budget_exhausted`, rerun the same live target once with a larger bounded budget, then pass the refreshed findings to the normal repair gate | `pipeline-quality.test.ts` proves the retry is selected before source repair; API build and `/api/health` passed after the implementation | The retry is intentionally bounded to one pass; a still-incomplete or unreachable target remains an honest blocker |
| Durable evidence writes under Windows contention | `JsonStore` atomic rename retry + stale temp cleanup | Retry a briefly locked destination and remove only payload-sized temp files older than the startup grace period | `two-documents-in-one-file.test.ts` passes concurrent writes, stale-temp cleanup, parse recovery, and no-stray-temp checks; live store contains the restored session history | Cross-process read-modify-write contention can still require a single canonical API writer |
| Reserved semantic-form QA time | `probeControls` reserves a bounded slice of each browser walk for `probeForms` | Keep real email, telephone, number, date, and invalid-value checks from being starved by a large control surface | `app-is-used.test.ts`, `deep-self-qa.test.ts`, and `app-audit.test.ts` pass; the reservation is applied before control exploration | The reservation improves coverage but cannot prove a form hidden behind authentication or an unavailable preview |
| Evidence-bound semantic edit recovery | `SelfFixService` regenerates only the failed source file when an exact replacement misses the current source shape | A proven file target plus the failed task brief and rejected replacement evidence | Focused file-edit recovery suite passes 25/25; the live expense run exposed the original exact-match failure that this path addresses | One bounded regeneration is allowed; the phase still stops if the regenerated file or its rerun is not verified |
| Offline local-provider deadline | `IntelligentRouter` `OFFLINE_LOCAL_TIMEOUT_MS` | A local-only deployment with an unavailable or overloaded Ollama | One real Local (Auto) attempt is bounded to 45s by default, with a guarded 10-120s override; `offline-local-deadline.test.ts` passes | The local model must still be available and capable; this prevents hanging, not weak-model failure |
| Browser visibility lock during live QA | `Joe.tsx` `keepBrowserVisibleRef` | Preserve the Browser watcher when preview-ready, terminal, or build events arrive during `self_qa` | Real 5002 rerun recorded `watching=true`; web production build passed; GitHub main contains `e1c27fbf` | The watcher stayed attached, but the generated app still exposed independent coverage and visual-hierarchy findings |
| Domain-shaped interaction engines | `app-blueprints.ts` selects an interaction engine; `react-app-templates.ts` supplies the engine UI | The request's workflow and field contract select a distinct interaction surface rather than a generic CRUD grid | Expense tracking now selects the `ledger` engine, with native numeric input, live totals, delete controls, and durable local rows; generated-source parsing and a same-prompt live retest are required before this row may be treated as live-proven | The engine catalogue is growing; unrelated management domains still need their own interaction profiles where a records grid is not the natural workflow |
| Request-clause delivery reconciliation | `named-requirements.ts` ids + `ReactProjectTool.ts` semantic topic reconciliation | A source-judged request clause maps through the same shared capability topics used by delivery evidence | The Pocket Ledger live run exposed a completed build and clean QA that was stopped solely by an unknown `request-clause-*` delivery id; the id family and semantic topic mapping now have regression coverage | Requires the same live retest to prove the full delivery path no longer stops after a verified build |
| Same-project literal follow-up edit with measured repair | `ProjectEditTool.parseLiteralTextReplacement` + safe Vite build wrapper + `improveUntilItStops` | An explicit quoted old/new label change in the active built project | Live continuation changed `احجز موعدك` to `احجز جلستك` in `src/content.js`, built successfully, showed Browser QA, measured `85/100`, repaired `low_contrast`, rebuilt, and remeasured `100/100`; focused suites passed `57/57` and loop suites `114/114` | The first live repair duplicated CSS across two files; the repairer is now constrained to one primary stylesheet and no longer adds responsive rules without a responsive finding. The narrower source behavior has regression coverage but still needs a fresh measured app run |
| Same-project compound follow-up editing | `ProjectEditTool` clause-local intent reader + `AgentLoopService` active-project continuity | A short follow-up can rename a brand, add a linked section with an exact item count, and change the primary palette without scaffolding another project | Live P17 kept the existing `react-مشروعي-48eeedb0` project, produced the `بصيرة` brand, `#services` navigation, exactly three visible service cards before Contact, emerald primary controls, `8/8` acceptance, and Browser QA `100/100` | The deterministic compound editor is intentionally bounded to well-evidenced page edits; structural rewrites and ambiguous references still require the normal planner and may need clarification |
| Guest identity continuity across local API restart | `web/src/services/socket.ts` verifies policy closes through authenticated HTTP before clearing the token | A transient WebSocket `1008` or unauthorized-looking close while the local API restarts | Live proof created `SESSION_RESTART_PROBE`, observed `Sessions · 1`, restarted the API, retained the conversation, then opened Joe again and recovered the same message and session count; ownership/restart suite passed `26/26` and the web production build passed | An explicit HTTP `401`, an expired JWT, or deliberate logout still clears the identity as intended |
| Explicit URL navigation with visible-content reporting | `PlanningEngine` URL-first observation route + `BrowserOpenTool.readContent` + URL-safe file intent parsing | A bounded request to open an explicit URL and report/read/show its visible response, including a no-file-change constraint | Live P15 opened `http://127.0.0.1:5000/api/health` in Joe's visible Browser and reported the rendered `OK`/`LOCAL`/single-user response; focused routing suites passed `25/25` | This contract reads bounded visible text after navigation. Multi-step interaction, authentication, and exploratory application QA remain separate browser capabilities |
| Browser-frame session isolation | `WorkspacePanel` remounts `EmbeddedBrowser` on the owning browser/chat session key | Any chat/session switch, including New Chat, changes the browser owner key | Live new-chat retest cleared the prior `/api/health` canvas and showed `No page loaded`; `browser-session-frame-isolation.test.ts` protects the remount contract; web TypeScript/Vite build passed | This prevents stale pixels in the UI. Server-side browser ownership and preview authorization remain separate security boundaries |
| Deterministic existing-file append and verified delivery | `parseExplicitAppendFileRequest` + `PlanningEngine` + `WriteFileTool` append mode + `composeWriteAndReadReport` | A named safe relative file plus literal quoted line content; optional ordinal establishes the expected final line count | Live P16 continuation created three lines, appended `delta=4` in the same conversation, read back four lines, and displayed `Final line count: 4`; direct disk evidence matched; missing-file and retry/idempotency tests passed | The fast path intentionally handles bounded literal append requests. Semantic or multi-region code edits remain with project/file editing workflows |
| Non-recoverable filesystem preconditions | `WriteFileTool` structured `nonRecoverable` result + `AgentOrchestrator` stop boundary | Missing append target or stated final-line count inconsistent with current content | Live missing-file rerun stopped in 15s with the exact target and no provider call or file creation; mismatch and absent-parent tests leave disk unchanged | Joe stops rather than guessing. A user can create/select the intended file or issue a different explicit edit request |
| Arabic morphology and intent normalization | `core/language/arabic.ts` (`snowball-stemmers` + `Intl.Segmenter`) + `promptNormalizer.ts` + normalized `buildIntent` probe | Tokenize and stem Arabic where semantic readers need word boundaries, then normalize dialect/spelling and verbal-noun build phrases before routing | Direct reader probe recognizes `تصميم` as a word without falsely equating it to `صمم`; focused routing/title suites passed 111/111. The exact UI prompt `تصميم موقع حديث لشركة برمجيات بعنوان مدار...` entered the deterministic project pipeline and, after title-marker repair, generated `react-مدار` with `مدار` in the document metadata and content contract | Snowball is a morphology aid, not a complete Arabic semantic model. It is directly consumed by five production areas; remaining ad-hoc language readers still need gradual consolidation. The live run also exposed a separate blank post-run Browser canvas/final-verification inconsistency |
| Arabic and English title-marker extraction | `page-head.ts` explicit brand markers | Extract a requested name after `بعنوان`, `تحت عنوان`, or `titled` without swallowing the remainder of the brief | Focused title/routing suites passed 111/111; the repaired live rerun wrote `مدار` to `index.html`, Open Graph/Twitter metadata, package name, and `src/content.js` | Additional languages and punctuation shapes require evidence-backed additions rather than an unrestricted name guess |
| Intact-token-first Arabic normalization | `promptNormalizer.ts` token variants | Test the complete Arabic token before stripping a leading conjunction | Regression covers `واجهة` and related waw-prefixed words; the malformed live brief routed as a build request and produced a consulting interface | Dialect and spelling coverage grows from observed failures; the normalizer is not a free-form translator |
| Multilingual URL observation routing | normalized `describe` / `translate` in `PlanningEngine` browser fast paths | An explicit URL plus a description or translation request in any normalized language | Russian `Открой https://example.com и опиши страницу` now selects `browser_run`; `selftest-understand.ts` is fully green | Authentication and multi-step interaction remain separate browser workflows |
| Runtime-aware local session fallback | `/api/runs/start` shared `usesJsonRunStore()` decision | Mongo is configured but Mongoose is not connected | The first malformed-Arabic UI attempt reproduced the raw Mongo crash; after repair/restart the same guest session path started normally while preserving owner denial for foreign sessions | Local JSON persistence is a development fallback, not a substitute for production database availability |
| Arabic brief subject isolation | `subject-phrase.ts` `arabicBriefSubject()` | Extract the object after `موقع`, `تطبيق`, `منصة`, or `صفحة` and stop before requirement clauses | Exact live typo brief generated `شركة استشارات متكاملة`; source and preview contain no `بدي تصيم` prompt leakage; focused tests pass | A missing explicit brand still uses the current generic fallback |
| Acceptance-aware delivery summary | `engineeringReportSummary.ts` final-verdict precedence | Browser mechanics can score 100 while requested acceptance criteria remain unproven | Regression proves `Accepted with gaps` produces an incomplete-verification message and names unproven requested parts instead of claiming no findings | Full evidence remains in Logs; the chat summary intentionally stays concise |
| Provider-independent cost-calculator follow-up | `ProjectEditTool` `calculator_section` edit | A same-project Arabic or English request adds a service/rate selector, numeric quantity, live subtotal, requested tax, total, and Reset without waiting for a model | P20 exact Arabic replay kept the current project, built successfully, and proved `6/6` acceptance; the generated component uses native numeric constraints and renders the exact computed bindings | This bounded deterministic editor handles quote-style calculators; materially different formulas still use the planner and must earn their own evidence |
| State-aware calculator Browser QA | `behaviour-audit.ts` calculator probe + `AppAudit.calculatorEvidence` | Discover a semantic calculator, exercise every option, test `0`, a large value, negative validity, exact arithmetic, and Reset from a changed state across requested viewports | P20 final run recorded 3/3 choices, correct zero and `9999` totals, negative rejection, correct 16% tax, working Reset, and desktop/phone/tablet evidence at `100/100`; independent visible interaction confirmed `500 + 80 = 580` and Reset to `139.2` | Requires the generated calculator's semantic `data-qa-*` contract; arbitrary opaque third-party widgets remain outside this proof |
| Durable final-answer ordering | `AgentLoopService` synchronous local-store flush before `text` and `run_finished` | A history reload or session switch races the final WebSocket frames | P20 failure and success replies were present in `chat-messages.json` before the closing frame; refreshing the long-lived UI restored the exact prompt and final `100/100` delivery | A browser tab holding an older frontend bundle still needs one refresh to load newly deployed client behavior |
| Canonical same-project edit routing | One `[SURGICAL PROJECT EDIT]` branch in `PlanningEngine` plus request-bound `workspaceRoot` | Explicit same/current/existing-project language, including an explicit prohibition on creating a new project | P21 removed the older competing edit branch, selected the Explorer project over stale session memory, and routed the exact Arabic continuation directly to `project_edit`; no sibling project was created | Specialized routes such as import, undo, and image-backed edits remain separate by design because they have distinct contracts |
| Restart-safe workspace selection | `WorkspaceService` workspace-scoped root map persisted in `.joe-workspace-roots.json` | Authenticated workspace id accompanies Explorer selection and run start | P21 selected `react-مشروعي-48eeedb0`, restarted the API, reloaded Joe, and observed the same project path and files without selecting it again; isolation regressions prove another workspace does not inherit the choice | A request with no workspace identity still uses the explicit local fallback and is not treated as an account-scoped selection |
| Currency, promotion, and tax-order calculator QA | `ProjectEditTool` enhanced calculator contract + `behaviour-audit.ts` state probe | A calculator continuation asks for currencies, a promotion code, invalid feedback, discount-before-tax arithmetic, state preservation, or full reset | P21 final exact replay reported `100/100`, 13 interactions, and evidence on desktop, phone, and tablet: 2 currencies, round trip, preserved service/hours, invalid and valid promotion states, 10% discount before 16% tax, and full reset. Independent Browser interaction reproduced both messages and the USD total `1104.03` | Exchange rates are request-derived static configuration unless the user separately asks for a live rate source |

## Evaluation Rules

### UAT Navigation Diagnostics (Evaluation Infrastructure)

- Components: existing `verify_joe_prompt_ui_acceptance.ts` and `verification-evidence.ts`, not a new Joe execution path.
- Behavior: failed browser navigation retains a bounded cause category and attempt count; 5xx responses fail UAT. No raw navigation-error detail or arbitrary response content is persisted by the diagnostic helper.
- Evidence: 17 helper/evidence tests, successful type checking, and a controlled authenticated real-route connection failure (502, connection, two attempts) followed by successful preview restoration.
- Limit: the original live 502 was recorded without its body and remains unexplained. This is evaluator observability, not a claim of newly implemented autonomous navigation repair.

### Article-Aware Field Evidence (Local, Pending Live Replay)

- Purpose: prove request-derived field contracts despite grammatical articles introduced by requirement extraction, without spending a model call on explicit schema evidence.
- Components: `named-requirements.ts`, ReactProjectTool acceptance report; uses the existing acceptance path, not another judge.
- Selection: literal labels first, grammatical article normalization second; date type must be bound to the same schema object. Builder-process instructions remain separate from artifact capabilities.
- Evidence: failed UI run `run-1789105095443` identified three article-prefixed fields plus one misclassified execution instruction. Existing project source now proves all eight artifact items with zero model calls. Focused article/records/instruction coverage passed 29 cases, related regression coverage and type checking passed as recorded in the evaluation log.
- Limits: source-contract evidence is not a new UI acceptance result. The live failed receipt remains failed until replay. Generic records design and overall task completion remain open.
- Live follow-up: `run-1789106796288` passed acceptance and one final quality check, plus independent form/persistence/filter/mobile checks. Its extraction used the compound inventory shape. Browser navigation also returned a 502, so full UAT remains unresolved; the runner now rejects server errors instead of silently treating that trace as healthy.

### Request-Derived Records Presentation (Local, Incomplete)

- Components: `app-blueprints.ts` (`heAskedForATable`), generated `content.js`, and `RecordsApp`.
- Selection: explicit table/spreadsheet/data-grid requests choose a tabular view; explicit CSS/card/form layout-grid mentions alone do not. Mentions are classified independently.
- Evidence: five false table selections reproduced before repair; focused layout-grid and existing table-shape suites passed 16 tests. After correcting the new fixture's AppKind, its ten tests passed again and full TypeScript checking passed.
- Regression: `layout-grid-is-not-a-data-table.test.ts`, `the-shape-he-named-was-the-one-thing-not-read.test.ts`.
- Limitation: source/content integration evidence only for this change, no new live UAT. Negation and arbitrary natural-language layouts remain unsupported by this narrow selector. The structured engine still selects a fixed presentation family; this is not a visual-originality completion claim.

### Issue 89 Verification Evidence Hardening (2026-09-11, Local Only)

- Components: `verification-ledger.ts`, `PhaseExecutorTool`, `AgentLoopService`, and `AgentOrchestrator`.
- Executable-check eligibility follow-up (2026-09-17): planner/phase guards now reject arbitrary successful read/write/detection tools as verification; shell reuse requires a single supported verification invocation with known options. Permanent regressions cover echo-only, mixed/stateful commands and non-check CLI modes. Four suites pass 89 cases, type checking passes, and all ten final gates pass (`final-matrix-20260917-155745/report.json`). Current canonical trace is `full_engineer_flow/run-fEKguv/verification-evidence.json`. Reviewer statically closed its reported option-mode finding only. Package script contents remain project contracts, not sandboxed by this classifier. Live acceptance and GitHub evidence for this latest version are pending.
- Runtime target-state identity (2026-09-17): a red-to-green regression exposed query/fragment changes reusing old browser proof when explicit tool arguments were empty. The complete target now contributes a process-local HMAC to the fingerprint while receipts retain only the display-safe URL. Ledger/phase suites pass 37 tests; live acceptance and final publication are not yet proven for this revision.
- Fingerprint framing follow-up (2026-09-17): reproduced identical hashes for different multi-file contents caused by unframed path/content concatenation. Added per-file path/byte-length framing and a permanent red-to-green regression. Ledger/phase suites now pass 36 tests; canonical trace `full_engineer_flow/run-4NO5kj/verification-evidence.json` and the five-versus-four execution benchmark pass. Runtime reload, latest-version UAT, final applicable gates, review, and publication remain pending.
- Selection: verification tasks fingerprint their declared paths, transitive dependency boundaries, toolchain, and runtime revision. Explicit paths do not override dependency paths. Matching passing receipts may be reused; final verification covers the project.
- Safety: incomplete/truncated boundary metadata, missing dependencies, unsupported filesystem links, and unreadable inputs disable reuse. Scope and ancestor containment are checked before reading source or the package manifest. Tool and firewall contexts share the canonical run id.
- Evidence: ledger/phase suites passed 30 tests, including deep dependencies, metadata compaction, real directory junctions, and unreadable inputs. The full engineer-flow test proved unchanged-check reuse during a repair rerun and a separate final gate. All ten required architecture/self-healing commands passed.
- Related failures: missing verification events under the intended run id; stale-proof risks from omitted dependencies and linked paths; imperative product constraints misclassified as builder instructions.
- Canonical execution evidence: `full_engineer_flow/run-3LP3aM/verification-evidence.json` independently records five check executions (smoke pass, build fail, repaired build pass, final smoke pass, final build pass). Both final fixture checks ran and no successful focused check was duplicated during repair. The test now preserves prior runs in unique directories.
- Open accounting defect: replacing same-id receipts loses earlier failed-attempt durations from `executedDurationMs`; saved-time estimates also use the latest receipt rather than the reuse-time receipt. Those aggregates are not complete run-cost evidence until corrected. This does not invalidate the independent child-execution trace or prove unrestricted live planning.
- Accounting follow-up: fixed locally with cumulative counters independent of receipt retention and reuse-time savings capture. The focused ledger/phase suites passed 33 tests; canonical evidence `full_engineer_flow/run-TJB88a/verification-evidence.json` reports four verification invocations, including the failed attempt, with complete accounting. Persisted legacy histories that cannot be reconstructed are explicitly incomplete. This supersedes the implementation defect above, not the pending publication/UAT requirement.
- Independent review follow-up: made selected/invalidated/reused counts cumulative too, fixing a 192-decision retention mismatch with lifetime savings. Added a 200-reuse regression; 34 focused cases, type checking, and canonical flow `full_engineer_flow/run-0ZaGvO/verification-evidence.json` passed. Reviewer confirmed that specific finding closed by static inspection only.
- Efficiency evidence: the existing 50-prompt benchmark now runs real child-process assertions for naive and change-aware policies, including an injected defect, a repair, and an independent final gate. Both produce the same five expected verdicts; actual executions fall from 5 to 4 with one valid reuse. Counts, durations, reasons, exits, and output are retained in `api/data/tests/verification-efficiency.json`. This replaces the former synthetic selection-count comparison; it does not replace canonical-pipeline/UAT evidence.
- Limitations: static path checks are not an atomic filesystem sandbox against concurrent hostile path replacement. The engineer-flow planner is controlled, not unrestricted model planning. Fresh live UI UAT and GitHub verification remain pending; no completion claim is made.
- Live follow-up: `run-1789100649901` passed the independent Joe UI runner in 162593 ms with one real final `quality_run` receipt, all form/persistence/filtering checks, and readable phone headings. Shared `plan-verification.ts` now enforces final-check planning after deterministic rescue as well as normal planning. Evidence: `api/data/tests/joe-ui-acceptance/2026-09-11T04-23-55-467Z/report.json`. This supersedes the pending functional UAT note above only; generic visual design, masked-capture motion safety, and GitHub publication still prevent batch completion. Generated smoke tests currently prove scaffold structure, not domain semantics.

- A prompt is passed only when its requested artifact or behavior exists and is independently verified.
- A chat sentence, HTTP success, or Joe's own completion claim is not evidence by itself.
- A failed prompt is recorded before repair; the same prompt is rerun after a general fix.
- Generated runtime artifacts and logs are not source evidence and are not committed unless the test explicitly requires them.

### Concurrent Preview Navigation

### API Artifact Workspace Ownership (Local Verification)

- Observed failure: the offline API integration harness generated files under the shared explorer root despite supplying a workspace identity. React already used the execution workspace; the API builder did not.
- Implementation: `ApiProjectTool` now resolves its default artifact root with `workspaceService.getActiveRoot(contextWorkspaceId)`, matching the existing React builder. Explicit root compatibility is unchanged; this is not a comprehensive audit of root overrides.
- Regression evidence: the new two-workspace case failed before the fix because `getActiveRoot` was never called. The complete `api-project.test.ts` suite then passed 38 tests, including trusted context precedence over a conflicting input workspace.
- Integration evidence: `verify_build_without_a_brain.ts` no longer supplies an explicit root. Through ToolService it created its API inside `session-no-brain-1789652875305`, booted SQLite, and passed all 20 checks without a model. The test refuses to boot an artifact outside its expected workspace.
- Verification: all ten required AGENTS commands passed after the production-code change; canonical trace is `api/data/tests/full_engineer_flow/run-HAh5ju/verification-evidence.json`. API `npx tsc --noEmit` and `git diff --check` passed.
- Updated-runtime UI evidence: full-stack library request `run-1789653218558`, report `api/data/tests/joe-ui-acceptance/2026-09-17T13-53-16-200Z/report.json`, failed after 440435 ms. Both API and React artifacts were in workspace `fecb2ce0115ae95c8ce0893e`, confirming the corrected default root in the real UI pipeline. This is NOT full acceptance: only one of two phases completed, no final verification was selected, and the generated model included infrastructure phrases as tables.
- Follow-on failure: the first React content file streamed at event 112 contained `api: '/api/products'`; the failed interface phase reran, and event 323 wrote `api: ''`. The saved desktop screenshot and source confirm local-only storage afterward. ReactProjectTool derives API URL, runtime credentials, model, and packaging directory only from a previous registry entry with type `api`; its own first execution replaces that slot with type `react`. A bounded retry therefore loses the backend handoff. Preserve the same-run, workspace-owned backend contract across retries; do not patch only the generated app or weaken acceptance.
- Limits: local, uncommitted checkpoint; GitHub publication pending. Independent form checks passed, but reload persistence alone only proved local storage, not database integration. The integration harness still reports a PageStore persistence warning. In-app control initialization still fails with OS error 3; inspected headless screenshots do not prove user-visible browser control. No production deployment was performed.

### Backend Handoff Across Interface Retries (Local Verification)

- Observed failure: full-stack UI run `run-1789653218558` lost its API connection when the interface phase retried. See the workspace-ownership entry above for the original evidence.
- Implementation: ReactProjectTool restores the linked backend contract only for an explicit scaffold continuation or the same pipeline run. It retains the backend resource, model, app kind, runtime-only credentials, and packaging directory independently of the React registry slot. Continuations reject missing or out-of-workspace real paths and malformed API resource links. Unqualified new builds do not inherit the old React entry's backend.
- Regression: extended clinic handoff test reproduced the empty API URL before the fix. Four suites then passed 132 cases (API, React, checkpoint consumer, build checkpoint). A subsequent credential handoff assertion and the disk-persistence credential guard passed with the React suite: 85 cases, overlapping the preceding run, not 85 additional independent cases.
- Post-patch gates: `npx tsc --noEmit`, all ten required AGENTS npm scripts, and `git diff --check` passed. These checks do not replace the pending real UI replay.
- Limits: updated-runtime UI replay and publication pending. The original full-stack request still has demonstrated entity-extraction and acceptance failures; preserving a backend link does not prove those are fixed. The existing direct API-to-React handoff and explicit-root API are not comprehensively security-audited by this patch. Runtime credentials remain top-level runtimeAuth so the existing persistence boundary strips them.

### Requested Record Boards (In Progress)

- Local follow-up while visible replay approval remains pending: `inside the existing workspace` and `under my selected folder` reproduced incorrect record subjects (`inside existing` and `under my`). Record-subject extraction now shares the existing execution-location grammar with headline extraction. Focused Jest suites `record-board-entity-contract` and `subject-execution-location` passed all 13 cases, including preservation of real geographic subjects. This is parser evidence only, not live Joe acceptance; the earlier full-gate results predate this follow-up.

- Full-stack replay `run-1789655247818` remained failed after 548403 ms. Final generated content retained `/api/products` and the preview used the real server at port 4617, but the model still contained bogus infrastructure/instruction tables. The acceptance runner did not sign into the generated application, so its missing editable controls do not independently prove that authenticated CRUD is broken.
- Visible follow-up: the existing development task used Codex in-app browser tab 29 (not Chrome), visited three generated table tabs, and confirmed infrastructure and execution-instruction phrases were actual data entities. Joe's guest session remains open in tab 28. No duplicate generation or data entry was performed for this inspection.
- Root cause: column inference correctly read title/borrower/date/returned, but `recordedSubject` returned null because `board` was absent from the shared record-container grammar. Entity inference then treated unrelated clauses as candidate tables. The local patch recognizes boards and excludes location/implementation prepositions from the subject phrase; it does not add domain-specific noun exceptions.
- Focused evidence: four new regression cases failed before the patch. Seven parser/board suites passed 49 cases; API handoff and further subject/column suites then passed 70 cases (overlapping the new four cases). Scaffold-only integration deliberately retains `requested_features_not_proven` for unexecuted API/database capability claims, while independently verifying one resource and the frontend link. Live replay of this parser patch remains pending.
- Post-patch typecheck and all ten AGENTS gates passed. The local API was restarted with the parser patch. Visible replay in the other task was blocked by its approval reviewer before the prompt was entered or submitted, citing that task's Issue 86 scope. No run started. Do not retry through another mechanism; obtain user clarification/approval for the visible local library checkout test first.

- Purpose: derive a grouped working view from explicit board intent and schema, preserving existing record mutations and persistence.
- Implementation: `requestedBoardField`, serialized `content.boardField`, shared RecordsApp lane rendering and responsive board workspace styles.
- Evidence: 21 focused selection/syntax/regression cases; explicit-schema Chromium fixture proves creation, state change through editing, persistence, and phone width. No extra model call.
- Limits: not yet exercised through Joe after this change. Short natural-language schema extraction and generic entity/title labels remain deficient. Grouping ambiguity returns no board rather than guessing. This does not prove full visual originality or Issue #89 completion.

### Record Mutation Acknowledgement and Context Isolation (Local)

- Observed during follow-up to `run-1789659818049`: generated record handlers exposed temporary create IDs, removed rows despite failed deletes, and toggled status without awaiting the server. Reload alone was not proof of server persistence because local storage also retained changes.
- Implementation: the existing generated RecordsApp now serializes mutations with a ref lock and disabled controls, applies remote changes only after acknowledgement, retains failed input/rows, and distinguishes authentication, read-only, ownership and forbidden refusals without changing authorization policy. A collection key derived from storeKey/API isolates late responses when the backing context changes. No new tool or execution path was added.
- Permanent evidence: `records-mutation-lifecycle.test.ts` covers delayed/failed CRUD, direct handler contention, malformed create acknowledgements, numeric guards and local-only operation. `records-mutation-dom.test.ts` with `fixtures/records-mutation-dom.cjs` renders the emitted component and store using existing React/ReactDOM/jsdom dependencies and mocked transport; it checks disabled clicks, delayed acknowledgements, failed-input retention, same-context rerender and late success/failure across collection/endpoint switches.
- Executed on 2026-09-17: ten affected Jest suites passed 139/139 tests; API TypeScript checking, API build and diff whitespace checking passed. The first build attempt hit sandbox parent-directory access denial; the identical build passed with the approved execution permission. No runtime restart occurred.
- Regression baseline: three additional suites have 19 failures (wiring-policy 16, admin-screens 2, request-shapes 1). An isolated source copy with pre-mutation template/assertions from `248a29b6360105f61f2bfb5e651aa6edb985defb` and otherwise identical current sources produced the same 19 failed/197 passed results as the current tree. This proves no new failures in that comparison, not that these failures are harmless or present on GitHub main.
- Limits: this is local component evidence, not visual QA or post-fix Joe UAT. Context switching remounts the collection; preserving unsaved drafts across leaving and revisiting a collection is not implemented. An already-sent server request can still finish after switching, but its response cannot mutate the new collection's state. Parent-record mutation behavior is outside this batch. Full-stack acceptance evidence, live replay and publication remain pending; there is no published commit for this batch.
- Acceptance follow-up: the existing confirmed-delete recognizer missed functional state updates and accepted a filter that retained every row. Two new tests reproduced both failures. It now recognizes direct/functional selected-ID removal and requires a cancellation guard; eight negative variants cover missing/ignored confirmation and absent/ineffective state mutation. The focused records suite passed 22/22. Four affected verification suites produced 104 passed/4 failed; the same four failures reproduce on the isolated pre-batch source (legacy batch fixtures omit `quote`, causing `declaredRequirementItems` to throw). Type checking passed. These additional failures remain unresolved; no runtime replay or publication followed this check.

### Local Backend Acceptance Evidence (In Progress)

- Failure: `run-1789659818049` reported Express/database as having no model verdict and persistence as unsupported source evidence. React acceptance inspected frontend source and public-API integration evidence, not the existing local backend proof. Terminal health/schema checks did not prove payload durability.
- Implementation: `local-backend-evidence.ts` reuses verification-ledger fingerprinting and receipts, plus the existing path-containment primitive. ApiProjectTool captures the source/config/lockfile fingerprint before boot, checks HTTP write/read payload equality, and sends a fixed independent SQLite/JSON reader through ToolService's shell tool. Valid receipts travel through existing tool output and joeProjects to React acceptance; there is no new registry or execution path.
- Safety: trusted workspace/session/run/resource and canonical backend ownership are required. Changed source during verification, later source/config changes, missing storage, linked source/storage, mismatched payloads, invalid identities and tampered receipts are refused. A process-local signature authenticates the receipt; Joe restart invalidates it with no unsigned fallback. No token, account data or probe payload is stored in the receipt. Shell arguments encode JSON data only, never code; no mutable project script is executed.
- Acceptance distinguishes owned Express source, measured SQLite storage and measured JSON persistence. JSON is not called SQL or a real SQLite database. Missing required local runtime proof remains unprovable without asking a model to invent evidence. The primary records/catalogue backend branch issues receipts; feed/workflow and arbitrary backend layouts remain unsupported rather than receiving fabricated proof.
- Evidence: local-backend tests passed 25/25, including real independent SQLite reads with a keyword table name, JSON fallback, payload mismatch, signature tampering, foreign ownership, missing DB, source/config/lockfile changes and linked source/storage. ToolService integration used the actual shell in orchestrator context on Windows with spaces/quotes in paths and shell-like input data; both SQLite and JSON success/failure contracts passed. API/React builder suites plus the earlier backend tests passed 145 cases before the final path/timing hardening. Build and TypeScript checks passed. No post-fix Joe UI replay has run.
- Regression accounting: the four legacy missing-quote acceptance crashes are now guarded without coercing objects into evidence; the three focused acceptance suites passed 55/55. The original 19 baseline failures remain unresolved. All ten AGENTS architecture/self-healing gates passed during this batch; the build-context invocation first needed the same test-only environment used by the engineer-flow fixture. Canonical trace: `api/data/tests/full_engineer_flow/run-SsZPi7/verification-evidence.json`.
- Limits: one-hour receipt lifetime and same-process signing intentionally require renewed proof after restart. Reading data from an independent process is storage durability evidence, not a crash/recovery or concurrent-writer guarantee. Flat generated backend layouts only are supported. Source-path guards are not an atomic OS filesystem sandbox against a hostile concurrent file replacement. Runtime reload, visible UAT, independent review and publication remain pending.

### Library UI Replay After Backend Evidence (Not Accepted)

- One prompt was submitted through the visible Joe composer after the local API restart: `run-1789665409350`, session `6aac20742ff5112e36c11ff3`, 2026-09-17 17:16:49 UTC. API PID 17952 used this worktree; Vite was not restarted and authentication bypass was not enabled.
- Outcome: failed, 72 steps in 9:20, one of two phases completed. Initial preview 4734, final preview 4751. Acceptance was 10/11, not full acceptance. `a real database` fell through to a model with no verdict. Persistence explicitly used the new independent SQLite evidence; Express was accepted with a weak source-mention explanation, not the intended owned-source explanation.
- The persisted project has a SQLite receipt and the correct pipeline run ID. Exact extracted `quote` and final `preJudged` objects are not present in the inspected logs, so an overly strict text-plus-quote matcher remains a hypothesis, not a confirmed diagnosis. Do not broaden matching to accept compound requirements without proof.
- Independent final-preview check: QA row 3 (`Joe QA mu5sv1de-cbmcz`) existed in public GET with returned `Yes`. Signed-out Undo displayed `Change not saved` and GET remained unchanged. Signed-in Undo changed returned to `No`; independent HTTP GET, a read-only SQLite query, and a browser reload all confirmed `No`. No user rows or accounts were deleted.
- Empty-title submission was blocked by native required-field validation. Its browser-generated validation message was Arabic in an English application. One Export CSV click produced no observable download event within ten seconds; this does not establish the complete cause or prove that every browser blocks the export.
- Remaining QA failures included `form_dead_submit` and `form_persistence_unproven`. Persisted `lastAudit.findings` lost their text and identifiers, keeping only severity plus an empty message. Screenshots also showed generic purple styling, repeated theme/export exploration, and unnecessary account-management surface. These are open findings, not accepted output.
- No additional full prompt, production deployment, push, or source-code patch occurred during this replay. Earlier automated test results above remain separate from this failed live UAT. Nineteen baseline test failures remain unresolved; publication is pending.

### Backend Criterion Grounding Follow-up (Local Tests Only)

- Three synthetic regressions failed before the patch: a short database label with a full-sentence quote missed valid evidence, a source mention incorrectly certified Express, and a compound backend requirement was accepted without proof of its additional obligation. These fixtures do not reconstruct the unrecorded live-run quote.
- `verifyNamed` now classifies the atomic label while using the existing grounding check for its quote. Existing inventory decomposition retains every compound item; unknown or unsupported items prevent full acceptance. This applies only with a local backend context and the supported English backend contracts, not arbitrary requirements or every language.
- Existing React tool output now includes bounded `acceptanceTrace`: hashed references and lengths, decision route, evidence kind and reason codes. Request text, source, explanations, credentials and payloads are omitted. The existing secret redactor runs before hashing; unknown routes are explicitly unclassified. This is diagnostic output, not an independent completion proof.
- Verification: initial three failures reproduced, then 60/60 focused tests passed; API/React builder suites passed 123/123. A subsequent builder-output assertion passed 1/1 (83 skipped). TypeScript and build passed; the first sandboxed build failed on ancestor-directory access, followed by a successful approved build. No updated-runtime UAT or publication has occurred.
- Remaining investigation: browser form QA treats a changed HTML fingerprint as submitted and checks reload persistence after a fixed 300ms. Download events are observed, but the fallback anchor-click counter is installed in the current document only. Delayed-ACK and post-reload download fixtures are still needed before attributing the live failures to these paths. No browser-audit patch was made in this follow-up.
- Final checks for this batch: all ten required AGENTS gates exited 0; canonical deterministic trace is `api/data/tests/full_engineer_flow/run-bgaLbN/verification-evidence.json`. Final TypeScript, build and builder-output check exited 0 after adding the same bounded trace to the existing terminal/session log. These synthetic checks do not supersede the failed live UAT or resolve the nineteen separately recorded baseline failures.

### Async Browser QA Observation (Local Diagnostic Verification)

- Observed class: `probeForms` treated a busy-only HTML change as submission and checked restored data after a fixed 300ms; `probeControls` stopped listening for downloads after 320ms. The isolated real-Chromium fixture reproduced three failures: delayed successful storage was unproven, a request with no response was called submitted, and an actual delayed CSV download was called unresponsive.
- Fix: pending form indicators prevent submission certification; observation and fresh-record checks poll within the existing total budget. A failed, cancelled or out-of-budget reload cannot certify the previous DOM. Persistence requires the QA anchor inside a record/list/table container outside forms and status messages. Unknown layouts remain coverage gaps. Download success requires a real completed download with no failure, with a bounded listener lifetime and cleanup around the entire action. The anchor-click counter is no longer accepted as file delivery proof.
- Permanent diagnostic: `api/src/tests/manual/verify_async_browser_qa.ts`, run with `npx ts-node --transpile-only src/tests/manual/verify_async_browser_qa.ts`; `--case=success` and `--case=reload-failed` allow focused repeats. It uses temporary headless Chromium and a loopback fixture, not Joe's live UI or user browser profile, and closes its browser/server afterward.
- Evidence: initial six checks had three failures. Expanded twelve-check run passed in 29431ms, including 1100ms ACK, 900ms read after reload, failed/no response, cancellation, actual delayed blob download, cancelled incomplete download, silent button and shared control budget (4092ms measured for 4000ms budget). An incomplete HTTP attachment did not reliably emit a failure event in the first fixture; the final negative uses an explicitly cancelled fixture-only transfer.
- After the record-only and reload guard follow-up, the targeted successful-save check passed in 4847ms; two rejected-reload checks passed in 3717ms while retaining the old DOM through an injected navigation rejection. Four affected Jest suites passed 120/120. Final TypeScript, build and diff checks passed. Earlier architecture gates are recorded separately; they were not rerun for this browser-observation-only batch.
- Limits: these fixtures establish the failure class, not the exact unseen timing in the earlier live run. Form handling versus business success is still distinct, and this is not independent server durability proof for arbitrary apps. Downloads beyond the bounded observation window remain unproven. Updated Joe UAT, full delivery acceptance and publication are still pending; the nineteen baseline failures remain open.

### Updated-runtime UAT Hold

- The next visible library replay is not started. The last preflight found the API healthy with normal authentication (`singleUser: false`), but still running the source loaded before the latest acceptance and browser-QA patches.
- The original environment-setting launcher no longer exists. The current launcher inherited those settings but does not contain the assignments needed to reproduce them. No process memory or environment dump was taken; no secret was printed, replaced or persisted, and no authentication bypass was enabled.
- The reviewer confirmed that there is no approved, verified environment-preserving restart route currently available. A narrowly scoped user decision about local authentication configuration is being handled in the review task. Leave API, Vite and generated previews running; do not label tests on the old runtime as verification of the new code.
- This blocks updated-runtime UAT and publication, not the already recorded local test evidence. No duplicate prompt or repeat of successful verification was performed for this hold. Overall delivery remains incomplete.

### Records Presentation Separation (Local Wiring, Not Live Verified)

- The library run `run-1789665409350` violates the visual-originality requirement: its persisted log selects the built-in records engine without authoring twice. Successful mutation checks do not make that presentation original.
- Generated projects now separate `records-controller.js`, `RecordsView.jsx`, and the trusted `RecordsApp.jsx` wrapper. The wrapper, not authored code, owns the collection/endpoint key and supplies the controller to the view. The bundled source helper composes those same emitters for existing source-contract tests; it does not duplicate behavior.
- For the single records surface, the existing `AIGeneratorTool` authors only `RecordsView.jsx`. Default engine selection and provider/build/runtime engine fallbacks no longer replace that surface silently. Ledger and unified-table policy are outside this batch.
- Source ownership checks reject changes to the wrapper, controller, store, shell and emitted accounts component, including at final delivery after repair. They report violations rather than silently restoring protected files. Exact unchanged/default-source detection is negative evidence only; `presentation.originality` remains `unverified`, even for model-authored output or a comment-only edit.
- The real React DOM fixture loads the actual emitted modules and trusted wrapper for the default view and two test-only presentations (list and table). It checks pending acknowledgement, denied creation, failed deletion and delayed responses across collection/endpoint switches. Numeric filter inputs now preserve decimal input with `step="any"`.
- Red baseline: default view passed; both alternative views failed because the hook did not exist. After extraction: four focused suites passed, 54/54 tests. These are local tests, not live Joe UAT or proof of visual originality.
- Additional red baselines: two different requests never reached authoring; an author returning success without changing the file was incorrectly accepted. Local tool-path tests now cover those failures, protected-file mutations, and default restoration/protected-file mutation during a controlled later repair. Author responses and the repair-gap trigger are mocked; these do not prove real provider performance.
- Wider regression exposed a synchronous missing-evidence scan: unanchored whole-source lookaheads repeatedly rescanned a 123286-character source. Two start anchors preserve the predicates without repeated starting positions. Permanent subprocess tests enforce a 15-second termination bound and check all missing terms plus positive evidence in both orders.
- Three media-contract failures remain (classification, title/field derivation, missing tag-filter evidence). An isolated module reconstructed by removing only the two new anchors reproduces `generic`, `where users`, and empty filters with current dependencies. This excludes the anchors as the cause, but does not establish a complete pre-wiring or pristine GitHub/main baseline. Two reading-log acceptance failures also remain and have not yet had a baseline comparison. Expectations were not weakened.
- Final focused regression: 14 suites, 199 passed and 5 failed tests (204 total), 83.294 seconds. The later focused numeric/DOM rerun passed 7/7 in 7.454 seconds, including native rejection of letters/infinite values and acceptance of decimals. API TypeScript and focused ESLint passed. Final API bundle passed in 440 ms after sandbox ancestor-read permission was granted; no service was restarted.
- All ten required architecture/self-healing commands passed. The first `test:self-fix:build-context` attempt failed because the child test environment lacked JWT configuration; the remaining seven commands were rerun with the existing `engineer-flow-only` fixture value and offline mode in that child shell only. Live API credentials/configuration were not changed. Full-engineer-flow evidence: `api/data/tests/full_engineer_flow/run-QoonJV/verification-evidence.json`; its planner remains controlled/mocked.
- No runtime restart, live Joe UAT, visual-originality proof, commit or publication accompanies this wiring. The existing runtime hold remains in force.

#### Five-failure Baseline Investigation

- An existing isolated reference was found at `C:/Users/home/AppData/Local/Temp/joe-mutation-baseline-ba85f548fdb4492b9a7928b904b6dc0b`. Its `api/node_modules` junction targets the current verification dependencies. Template, acceptance and the two relevant test files match HEAD `248a29b6360105f61f2bfb5e651aa6edb985defb`; its blueprint differs from that HEAD only by the earlier `board` record-container change. This is an older hybrid reference, not a complete exact pre-wiring snapshot.
- Four selected failing assertions reproduce exactly there: 4 failed, 11 skipped, 9.651 seconds. Media classification returns `generic` instead of `media`; its title is `where users` instead of `Media Review Board`. Reading-log acceptance omits both per-field filter criteria, returning `title`, `filtering`, `progress_metric`.
- The fifth assertion, missing media-filter evidence, was attempted in a separately bounded Jest process and terminated after 15 seconds (`ETIMEDOUT`), because that reference still has the unanchored evidence scan. This is a timeout, not an asserted functional failure. Independent reference emission confirms only title/tags/notes text fields and `filterFields: []`, with no separated presentation module; current emission has the same missing field/filter contract.
- Media cause: explicit-schema precedence bypasses domain classification; field-list replacement loses the separately requested upload-image input; subject extraction mistakes the relative clause after `board` for the entity name; exact filter token matching misses `tag` versus `tags`. These are request-contract defects, not justification for weakening the tests or restoring canned schemas.
- Reading-log cause: the canonical schema reader finds seven fields and both requested filter keys, but `acceptanceFor` clears generic columns unless the request literally contains a schema keyword. The user did not need to say `fields` or `table`. This loses acceptance coverage even though the blueprint still carries the filters.
- This investigation made no further production changes. Proposed fixes require shared contract derivation and negative widget/non-schema cases, not prompt-specific IDs or templates. No live UAT or new Joe prompt was run.

#### Natural-language Filter Acceptance Follow-up

- `acceptanceFor` now consults the existing `hasExplicitRecordSchema` predicate before discarding generic columns. The independent widget-only guard remains in place; no media/domain/schema generator behavior changed in this follow-up.
- Red: reading-log coverage and its new negative-case setup failed because acceptance returned zero per-field filter criteria (3 failed, 7 passed). Green: reading-log, records requirements and booking-versus-form suites pass 43/43 in 11.776 seconds. Existing IDs were not rewritten to fit output.
- Two domains, reading and maintenance, are exercised without requiring the user to say `fields` or `table`. The source-acceptance test emits the real content contract and records view, verifies both filters, then removes each key from the filter list that drives the actual rendered controls. Each omission yields one unmet and one met filter criterion. Two counter/widget-only requests still produce no invented record-field/filter criteria.
- This is local source-contract verification, not browser UAT or proof that arbitrary authored layouts bind every filter correctly. Media classification/field/filter defects remain open. Focused ESLint passed; no restart, deployment or live prompt was performed.

#### Explicit Upload Alongside Metadata Fields

- The existing field parser now retains an explicitly requested image-upload capability when the same request lists metadata fields. It reuses the existing image type mapping, appends the missing field without changing metadata order or primary selection, and avoids duplicate image fields. Negated upload requests and explanatory text do not introduce an upload field.
- For records source using `fields.map`, upload evidence now requires both the file/image input and a configured image field. A dormant conditional file-input branch is insufficient. Independent non-record upload evidence retains its existing behavior.
- Red baselines: three domain requests failed due to missing image fields; after that correction, a separate source-evidence test failed because removing the configured image still counted as covered. Final focused run: upload schema, presentation authoring, evidence budget and reading-log contracts passed 32/32 across four suites in 29.3 seconds.
- Review follow-up reproduced three further evidence errors (3 failed, 10 passed): an action label bypassed executable checks, an unused image schema counted as the active schema, and an unrelated fields renderer invalidated independent upload evidence. Explicit action rules now precede label evidence. The narrow source check reads the direct `content.fields` literal and checks the upload input's enclosing map, rather than accepting any fields array or rejecting every source containing a map. Unknown mapped renderers remain unproven; independent inputs do not require a records schema. The focused upload suite now passes 13/13 in 3.378 seconds. This is not arbitrary data-flow analysis or runtime proof.
- These are bounded local source/tool tests, not live browser UAT or visual-originality proof. Media classification, title extraction and singular/plural tag-filter matching remain unresolved. No runtime restart or publication was performed.

### Local Runtime Handoff (2026-09-17)

- Follow-up after the desktop restart: the current task's in-app browser controller initialized successfully. Both local listeners were absent and their worktree processes were verified absent before starting API and Vite again. API PID 15776 runs the worktree source; Vite serves loopback port 5002. Health was OK with LOCAL persistence and singleUser false. No work was delegated to the stopped task.
- One visible replay was submitted through guest UI tab 2: `run-1789678285142`, session `6aac52b5ead5d762287d4674`, started 20:51:25 UTC. It ended failed after the UI reported 41 steps / 4:07, with one of two phases completed. Preview 4690 is blank. A separate read-only in-app preview tab confirmed `TypeError: Cannot read properties of undefined (reading 'create')` in the generated bundle. No second prompt was submitted.
- The run log proves RecordsView authoring was called at 20:52:23 and an authored export accepted at 20:52:47. This closes only the observed authoring-bypass diagnostic, not visual originality or delivery. Browser QA reported empty_page, page_errors, console_errors, h1_count, a11y_no_main and auth_failed; a transient browser quality badge was not accepted as proof.
- Generated RecordsView reads nonexistent `content.actions.create`, treats the fields array as named properties, calls `controller.submit()` without its required event, passes IDs where mutation methods expect rows, and invents an object sort contract. The generic next repair must supply and verify the actual presentation/controller contract. Do not patch this generated library alone or claim the request succeeded. No runtime source edits were made during this replay.

- The user explicitly approved resetting the local session key and asked this task to take over while the other task stops. The existing API identity and terminal run states were checked before stopping only API PID 17952. No project data was deleted and no production action was performed.
- A new random session key was passed only through the local launch environment, not written into repository files or printed. API PID 28412 started from this worktree using `npm run dev`; the listener became ready at 23:22:32 +03. `/api/health` returned `status: OK`, `database: LOCAL`, `singleUser: false` at 20:22:59 UTC. Earlier sessions may require login again. Process-local backend proof signatures are invalidated by this restart.
- This removes the local authentication-restart blocker, not the visual acceptance blocker. The current task's in-app browser controller still fails during initialization with `failed to write kernel assets: The system cannot find the path specified. (os error 3)`, including after a controller reset. An open-browser request returned `queued`, which is not proof that the page was displayed or tested.
- No new library prompt or hidden replacement UAT was started. The latest completed live run remains `run-1789665409350`, failed, and predates the loaded source changes. Do not label its preview evidence as verification of the new runtime. The other task is stopped; do not dispatch duplicate work to it.

### Records Author Context (2026-09-18)

- Observed failure: live run `run-1789678285142` produced a blank authored view with invented content properties and incorrect controller arguments.
- Root cause: `AIGeneratorTool` sends a single artifact-generation prompt; naming files and asking it to inspect them does not supply their contents or give it a file-reading tool.
- General correction: `ReactProjectTool` now supplies the actual generated content and controller source to the records presentation author and the existing capability-gap repair. Explicit guidance preserves event/row arguments, field keys, filter keys and string sort modes. The default presentation source is deliberately excluded; ownership, persistence and authentication boundaries are unchanged.
- Regression evidence: the focused records presentation suite caught missing context in initial authoring for library/inventory requests and in later repair (3 failures, 7 passes before the correction); all 10 passed after the correction. These are mocked-author boundary checks, not proof that a real model will obey the contract.
- Validation: `npm test -- --runInBand src/__tests__/records-presentation-authoring.test.ts --silent` passed 10/10 on the final test source (18.745 seconds); `npx tsc --noEmit --pretty false` exited 0. No additional model calls were used for these checks.
- The in-app Joe tab was inspected and retained visibly. It still displays the previous failed 41-step run, not a new success. API PID 15776 has not been restarted for this correction; no new live prompt was submitted. Full visual acceptance and GitHub publication remain pending.

### Visible Replay and Repair Routing (2026-09-18)

- Previous turn classification: progress (actual author-context correction and 10 passing focused tests). Reloaded only the verified local API after checking the preceding run was terminal; health returned OK/LOCAL. A fresh local session key was held only in the launch environment under the existing local-reset authorization. No production or GitHub mutation.
- Submitted the unchanged library-board request once through visible in-app Joe tab 2. Run `run-1789679651874`, session `6aac580fc665ce7d6e9946a2`, workspace `ddc2bf8125143a0f6ce11cbf`, started 21:14:11 UTC and ended failed around 21:18:52 UTC. Preview `http://127.0.0.1:4855/` was opened and inspected in visible in-app tab 3.
- Authoring ran 21:14:47-21:14:56. The view now uses the supplied field keys and event/row arguments, and renders instead of crashing on `content.actions.create`. This proves progress on that observed failure, not full correctness. Layout is still weak; the generated cancel handler also references unimported `blank`, which remains an unverified interaction defect to address.
- Browser findings started at 60/100 with form_dead_submit, missing labels and visual defects. The improvement loop sent functional/semantic findings to a CSS-only model when no handler target could be identified. The retained CSS included `select{display:none}` and `[placeholder="Search..."]{display:none}`. Direct visible inspection confirmed search/filter controls disappeared. A higher audit score is therefore not accepted as improvement. Final delivery also reported a trusted-file ownership violation and insufficient current backend acceptance evidence.
- General repair-routing correction: `model-round.ts` now admits only known stylesheet-repairable findings before calling the model. ReactProjectTool preserves unresolved functional/semantic findings and skips CSS requests when none are appropriate. The existing CSS gate rejects visibility/interaction-changing declarations and escaped tokens, including the exact observed hiding output. No generated application was manually patched to manufacture a passing replay.
- Evidence: 3 focused suites passed 93 tests (`css-repair-boundary`, `it-keeps-improving`, `a-dead-button-is-not-a-colour-problem`); type check and diff checks exited 0. Jest reported a delayed-exit/open-handle warning but terminated with exit 0.
- All 10 required gates exited 0: `guard:architecture`, `guard:package-scripts`, `test:joe:engineer-flow`, `test:self-fix:build-context`, `test:self-fix:execution-safety`, `test:self-fix:typescript-repair`, `test:self-fix:typescript-missing-name`, `test:self-fix:typescript-number-to-string`, `test:self-healing:failure`, `test:self-healing:success`. Logs are in `C:/Users/home/AppData/Local/Temp/joe-repair-gates-20260918-002416`. The initial build-context invocation exited 1 before testing because JWT_SECRET was absent; that check and the remaining seven-check sequence ran with a process-only random test key, without changing the running API. The three already-passing gates were not repeated.
- Limits: the CSS gate is a conservative restriction, not a complete proof against every possible layout-based concealment. Functional submit diagnosis, precise failed-control evidence, preservation of trusted files during visual repair, full UAT and GitHub publication remain open. This runtime replay predates the repair-routing patch; a new replay must load it first.

### Form Failure Evidence and Manual Diagnosis (2026-09-18)

- Previous turn classification: progress (visible replay plus CSS repair routing/gate changes with focused tests and required architecture/self-fix gates). No new prompt or API restart during this continuation.
- Direct in-app interaction with preview 4855 disproved the broad assumption that record saving itself is broken: anonymous submission displayed a sign-in error and retained data; after login with the generated local test account, `Library acceptance check` / `Local QA` saved visibly and survived a full page navigation. Returned status changed from No to Yes. Only this QA record was created/changed; no user record or account was deleted.
- The original generic `form_dead_submit` verdict remains unresolved: the authenticated page contains both the record form and account creation form, but the run report retained no offending form target. Manual success on one form does not prove the audit's entire verdict false. Do not rewrite the persistence controller based on that anonymous counter.
- General correction: `probeForms` now retains bounded finding evidence identifying the submit label, measured form/submit selectors and field/fill counts; `app-audit` preserves route context when merging probes; `judgeBehaviour` forwards this evidence to the existing repair-file selector. Entered values and credentials are excluded. No acceptance threshold or dead-form verdict was weakened.
- Verification: focused deep-self-QA and behavior-repair suites passed 95 tests; after adding the judgement-to-file-selection assertion, deep-self-QA passed all 62 again. The real isolated Chromium `verify_async_browser_qa.ts --case=silent` check passed both assertions in 5356 ms, proving an inert form yields its measured target and no entered values. Initial sandbox launch failed EPERM; the same diagnostic ran successfully with process-launch permission. Type check exited 0. This diagnostic fixture is not full Joe UAT.
- Additional visible defect confirmed: clicking Edit then Cancel on the QA record produced `ReferenceError: blank is not defined` in the generated bundle and left draft fields populated. The source calls an unimported helper; a general generated-code reference check is the next concrete capability gap. Do not patch only this generated project or claim the record-board request passed.
- Current UI result remains the failed run `run-1789679651874`; the Joe UI reports 42 steps / 4:57 (the earlier 4:41 note measured log activity, not full displayed duration). Form-target evidence and CSS safeguards still need to be loaded for the next full replay. No commit or GitHub publication of these changes has been verified.

### Generated Reference Validation (2026-09-18)

- Previous turn classification: progress (manual save/persistence/toggle evidence and structured failed-form targets). Re-read the objective and existing source contracts before changing the artifact author.
- Observed gap: generated JSX parsed and bundled even though its Cancel handler called unimported `blank`; the real browser threw ReferenceError on the interaction.
- Extended `source-contract.ts` with TypeScript lexical-reference checking for JSX/TSX. It reads only the candidate and TypeScript's standard libraries, does not read/execute project imports, understands lexical scopes, destructuring, imports and browser globals, and reports missing-reference diagnostics with source positions. Suppression directives are rejected rather than letting generated code bypass the gate.
- Integrated with AIGeneratorTool's existing pre-write validation and bounded retry: one reference correction gets the rejected candidate and precise diagnostics; an unresolved reference remains off disk and recoverable through the existing pipeline. No extra tool execution path or domain-specific helper is injected.
- Evidence: the actual failed RecordsView from workspace `ddc2bf8125143a0f6ce11cbf` is rejected at 64:14 with TS2304 for `blank`. Three focused suites passed 98 tests (35.748 seconds), including successful bounded correction, repeated-failure/no-write, lexical scope, browser globals and suppression guards. Two stale generated-app assertions originally failed because behavior moved from RecordsApp into records-controller/RecordsView; assertions now inspect the actual owning modules without removing their contracts.
- Limits: this is missing lexical-reference validation, not proof of arbitrary runtime behavior, imported export correctness, or object property contracts. It currently applies to JSX/TSX artifact generation; later editing paths and final browser QA still matter. The local API has not been restarted for these changes and no new full prompt was submitted in this continuation. GitHub publication and full UAT remain unverified.
- Final validation: type check and diff check exited 0. API build initially failed on sandbox directory access, then the unchanged command succeeded with local process/filesystem permission (5.5 MB bundle, 3004 ms). All 10 required architecture/package/pipeline/build-context/execution-safety/TypeScript/self-healing gates exited 0; individual logs are in `C:/Users/home/AppData/Local/Temp/joe-reference-gates-20260918-004648`. These checks used a process-only test session key, not the running API key.

### Incomplete Artifact Replay (2026-09-18)

- Previous turn classification: progress (generated-reference guard, 98 tests, build/type checks and 10 required gates). Verified API PID 2716 belonged to this worktree and runs were terminal before restarting only that local API. Launcher 16200, logs `C:/Users/home/AppData/Local/Temp/joe-api-20260918-005406.*.log`; health OK/LOCAL. Used the existing authorization for a process-only local session key; no production change.
- Submitted the unchanged request once through visible guest UI tab 2. Run `run-1789682122211`, session `6aac61ba1cc766dfc206bbf1`, workspace `e24fece421d18b6efda4661c`, began 21:55:22 UTC. RecordsView authoring began 21:55:57; generation was rejected at 21:56:15 for an incomplete Markdown fence after the existing format retry. Run status is failed. No fresh generated preview exists to validate cancel/save; older preview 4855 is not evidence for this replay.
- Inspected the author retry: it used the same 1200-token frontend output cap after an incomplete artifact. Provider finish reason was not available in the inspected evidence, so output truncation is a hypothesis, not an established sole cause.
- Narrow general correction: only an engineering frontend artifact rejected for an incomplete fence gets a doubled output cap for its existing single format retry, capped at 4800. Normal success/outage/syntax paths retain their budgets. Repeated incomplete output still fails without writing. Diagnostics record character count and cap change, not the rejected source or credentials, and explicitly mark the cause unconfirmed.
- Focused AIGeneratorTool suite passed all 51 tests (23.208 seconds), including recovery and repeated-failure paths that capture caps 1200 -> 2400 and prove exactly two calls/no failed write. This uses mocked model completions and does not establish live provider recovery. The API has not been restarted for the budget correction; the broader objective and full UAT remain open, with no new verified GitHub commit.

### Protected Repair Targets (2026-09-18)

- Previous turn classification: progress (incomplete-artifact retry budget plus focused checks). Verified the running API was worktree PID 4612 and prior run terminal, then restarted only that local API. Launcher 10692, log prefix `C:/Users/home/AppData/Local/Temp/joe-api-20260918-010053`; health OK/LOCAL. No production action.
- Submitted the unchanged request once via visible in-app guest UI. Run `run-1789682517757`, session `6aac6345f15397bdc62e393b`, workspace `f767d7a8769be4b018c65ef4`, began 22:01:57 UTC. RecordsView authoring ran 22:03:01-22:03:10 and completed; browser QA started at preview `http://127.0.0.1:4640/` at 22:04:04. Direct in-app screenshot showed the rendered form, search and filters, but weak generic layout. Successful generation alone does not prove the prior incomplete-fence failure was caused/fixed by the token cap.
- The new form evidence changed the next action: `form_dead_submit` selected `src/components/Accounts.jsx` and label `Create account`, not the record-save controller. The improvement loop rewrote that protected component twice (22:05:11 and 22:06:17) while the finding persisted. Round 2 reported no gain and source rollback; post-restore build verification was not established. A stop was requested through the visible UI to avoid further repeated repairs; authoritative run status was then failed, not a demonstrated cancelled success.
- Root boundary defect: records ownership was checked at final delivery, but deterministic and model browser-repair paths could already modify trusted files before that check.
- Generic prevention: repairRound now accepts protected relative paths, excludes them before planning and guards them again before writing. ReactProjectTool passes its existing trusted-records list to that deterministic boundary and refuses a selected protected handler target before making a model call. It keeps the original finding open rather than guessing another file or weakening acceptance. Existing generated projects were not manually rewritten to fake a passing result.
- Focused evidence: records presentation, behavior repair and improvement suites passed 89 tests (19.202 seconds); type check and diff check exited 0. New real-filesystem regression proves protected source remains byte-identical while editable CSS can still be repaired. Jest emitted a delayed-exit warning but terminated successfully. All 10 required architecture/package/pipeline/build-context/execution-safety/TypeScript/self-healing gates exited 0; individual logs are in `C:/Users/home/AppData/Local/Temp/joe-protected-gates-20260918-011031`. Tests used a process-only random key, not the running API's session key.
- Limits: the account-create finding still needs direct root-cause investigation. The change prevents unowned writes, not the original form failure. UI originality, labels, cancellation behavior, backend acceptance evidence, full UAT and GitHub publication remain open. The running API has not loaded this protected-target patch yet.

### Form Rediscovery After Reload (2026-09-18)

- Previous turn classification: progress (visible run identified Accounts/Create account and repair ownership enforcement). Continued from the failed run instead of launching another model request blindly.
- Reproduced a concrete QA defect in real isolated Chromium: probeForms snapshots every form, submits the first, then reloads to verify persistence. The next form's old data-joe handles no longer exist. The old code filled zero fields and still incremented formsDeadSubmit, potentially authorizing a rewrite of a working handler. A two-form fixture failed with first form persisted, second form filled=0, dead=1 (9293 ms).
- General correction: before each form attempt, rediscover the current document and match a unique identity from the form label and field names/types/tags, not stale element handles or positional guesses. Missing/ambiguous identities yield the major coverage finding forms_not_reached, preserved across route aggregation and explicitly excluded from source-handler repair. No threshold was lowered and genuinely silent forms remain failures.
- The same fixture then passed with both forms filled/submitted and dead=0 (5039 ms). Added a disappeared-second-form case proving that a coverage gap remains visible without falsely accusing the application. The complete isolated Chromium fixture passed all 18 checks in 39880 ms, including existing delay/failure/cancel/reload/download/budget checks. Focused QA/repair suites passed 96 tests; type check and diff check exited 0.
- This proves the stale-handle failure class, not that every original account-create defect is resolved. The live Accounts path must be replayed with the corrected probe. No application account or stored credential was created/modified for this investigation, no new full Joe prompt was sent, and the running API has not yet loaded this patch. Required gate results and live acceptance remain separate evidence; no GitHub publication has been verified.
- Required verification completed: all 10 architecture/package/pipeline/build-context/execution-safety/TypeScript/self-healing checks exited 0. Logs: `C:/Users/home/AppData/Local/Temp/joe-form-discovery-gates-20260918-012258`. A process-only random test key was used; the live API key was not read or changed. Full Joe UI replay remains the next acceptance check, not replaced by these fixtures.

### Presentation Surrounding Context (2026-09-18)

- Previous turn was a verified wait: the live Joe UI still showed run `run-1789684234448` executing. This continuation observed its terminal failure after 69 steps / 8:29. Session `6aac69f74d1d9aaa8c6af4ca`, workspace `ff51dd796fb674212d9da3a6`, final preview `http://127.0.0.1:4701/`. Do not confuse the earlier intermediate preview on 4817 with final output.
- Form rediscovery replay advanced beyond the old false `form_dead_submit` finding; final open findings were `h1_count`, `some_dead_controls`, `form_persistence_unproven`, and `a11y_placeholder_as_label`. The backend's independent HTTP/SQLite persistence evidence passed, but delivery remained blocked (11/15 acceptance). This is not a successful full task.
- Visible in-app inspection of the final preview from a fresh navigation showed three QA records already persisted, duplicate H1 headings, an unlabeled search box, oversized checkbox layout, and a generic poorly integrated presentation. The generated view used custom div-only record wrappers; the browser persistence locator recognizes semantic record containers, not those wrappers. A persistence coverage failure alone therefore does not establish that storage is broken.
- Root context gap: the single-call records author was instructed to reuse the shell/styles but received only content/controller source. The author now receives the actual App.jsx and app.css as read-only evidence too, without the default RecordsView layout. Guidance requires integration with existing landmarks/headings, accessible field labels, semantic collections, scoped styling for new classes, and compact checkbox/radio controls. No quality thresholds, storage checks, ownership gates, or protected sources were weakened. Context is larger; whether it reduces retry cost requires live measurement.
- Verification: records-presentation-authoring passed all 10 tests (12.747 s), including both distinct requests and later repair context; TypeScript and diff checks exited 0. Full safety gates and real same-prompt replay are pending at this entry. No new GitHub commit is claimed.
- Gate follow-up: all 10 required architecture/package/engineer-flow/build-context/execution-safety/TypeScript/self-healing checks exited 0; logs at `C:/Users/home/AppData/Local/Temp/joe-presentation-context-gates-20260918-014419`. Reloaded only the verified local API (old PID 16448; new launcher 14012, log stamp 20260918-014815), health OK. Reauthenticated the local guest in the visible Codex browser and submitted the unchanged library request once after the gates. Live acceptance remains pending.
- Live replay follow-up: terminal failure after 48 steps / 2:18, before frontend authoring; WebSocket target `6aac6e5d308430f368d772a3`, persisted chat `6aac6e5f308430f368d772a5`, workspace `22a484da48f63cef7335c5b7`. Backend write/read succeeded; DuckAI and Pollinations returned 429/empty responses. The orchestrator made its bounded 15-second retry and then emitted run_finished. This neither proves nor disproves the presentation-context patch. No further prompt was submitted. Investigate provider selection/runtime configuration before replay: LLM7 does not appear among attempted providers in this new launch despite earlier working runs. Do not yet conclude every provider is externally unavailable. Detailed local stdout/stderr: `C:/Users/home/AppData/Local/Temp/joe-api-20260918-014815.{out,err}.log` (may contain generated test credentials; inspect only targeted metadata). The new session journal was not found at the old api/data/runs location. Live API PID observed as 4884; command points to the intended worktree ts-node entrypoint. Source/type/gate checks above remain valid, full UAT remains unproven.

### Compact Presentation Context (2026-09-18)

- Previous goal turn was progress (source context correction, 10 focused tests, required gates, and a real failed replay). The next investigation corrected its incomplete provider diagnosis: the full API log shows LLM7 succeeded early, then returned `429 ... Retry after 59 seconds`; the last fallback-only excerpt did not represent all attempts. The session journal does exist under the persisted chat ID: `api/data/runs/6aac6e5f308430f368d772a5.log`. Do not diagnose missing configuration from a truncated provider-attempt list.
- Observed cost risk: passing the whole shell and stylesheet added 60,191 source characters, including unrelated domain styling and explanatory comments. Added `presentationShellContext`: TypeScript AST/printer preserves the shell with comments removed; CSS Tree retains global selectors, theme tokens, literal shell classes/ids and enclosing media/support rules, omitting unrelated classes, font-face and keyframes. The supplied entry explicitly labels this as a projection, not the full stylesheet. The default presentation is still never supplied to copy. No arbitrary truncation, token-budget expansion or weakened quality gate.
- On the actual previous generated project, the surrounding context becomes 18,956 characters (8,061 shell + 10,895 stylesheet), a 69% reduction. This measures only the added surrounding-context portion, not total request tokens, dollars, or proof that the preceding 429 was caused solely by prompt size. CSS Tree 3.1.0 was already installed transitively; declared it directly with offline lockfile update (one line in each manifest, no install scripts/network).
- Verification: authoring integration 10/10 passed (12.027 s); new projection tests 3/3 passed after correcting a whitespace expectation (CSS custom-property raw whitespace is preserved). They cover shell headings/imported view, conditional class names, global inputs, theme variables, media/support conditions, unrelated selectors, and nested functions/quoted braces. TypeScript and diff checks passed. API bundle succeeded (5.5 MB, 464 ms) after a sandbox directory-read denial required an escalated local retry. Required gate suite running separately; full Joe acceptance still pending. No GitHub publication claimed.
- Gate follow-up: all 10 required scripts exited 0; `C:/Users/home/AppData/Local/Temp/joe-compact-context-gates-20260918-020120`. Reloaded the verified local API only (old PID 4884; launcher 13632; logs `joe-api-20260918-020406.{out,err}.log`). Initial health connection refusal was startup latency, not terminal failure; recheck returned OK, uptime 78.57, without another restart. Reauthenticated the local guest and sent the unchanged library prompt once through the visible Codex in-app browser after gates. Do not edit/restart or duplicate the prompt while this replay remains active.
- Replay handle: UI confirmed active at 02:08:57 +03, workspace `747ba7c87ae640f9b22c2faf`, persisted chat/runtime binding `6aac7250beeebfecef972b8e`, WebSocket run target `6aac724ebeeebfecef972b8c`. Backend on port 4322 passed anonymous-write rejection, owner authentication, HTTP write/read and independent SQLite proof. API log confirms the next phase is attempting LLM7. Frontend creation and final acceptance are not yet observed; no completed-run claim.

### Avoid Unused Application Marketing Copy (2026-09-18)

- Previous continuation was progress: compact context implementation, tests/gates and a visible replay. That replay subsequently emitted run_finished for WebSocket target `6aac724ebeeebfecef972b8c`; journal `6aac7250beeebfecef972b8e.log` shows frontend authoring blocked at 23:09:36 UTC after provider unavailability. No additional prompt was submitted during this investigation.
- The journal exposed model calls for marketing copy and catalogue before the actual records view. Changed only the marketing-copy gate: when an application blueprint exists, its dedicated domain author owns visible copy; the unused brochure-copy request no longer consumes a model call. Non-application pages retain their existing copy author. Catalogue generation is intentionally unchanged because users can explicitly request initial records/products; a generic empty-records assumption would drop valid requirements.
- Integration regression disables the test-only rationing shortcut while mocking external model boundaries, verifies that an inventory application never calls authorCopy, and still reaches RecordsView authoring. Both authoring/context suites passed together: 14 tests, 9.786 s. TypeScript and scoped diff checks exited 0. An earlier combined run passed the 11 authoring cases but the second suite failed setup because its shared temporary root was missing; isolated and combined reruns passed. Cause of that transient filesystem failure is unproven and not claimed fixed.
- Runtime still contains the prior compact-context version (launcher 13632, log stamp 20260918-020406); this one-line authoring gate has not been loaded or evaluated through live Joe yet. Existing 10-gate results belong to the prior compact-context batch, not this edit. No new GitHub commit/publication or end-to-end success is claimed. Next: load the small authoring gate, verify provider recovery without duplicate active requests, then replay the same UI task and inspect the actual result.
- Live follow-up: loaded the tested gate into the verified local API only (old PID 10024, launcher 9072, logs `joe-api-20260918-021619.{out,err}.log`; health OK, uptime 16.97). Same prompt sent once at 02:17:05 +03 in visible Codex browser. WebSocket target `6aac74ddef2d6611ac8eb454`; chat/journal `6aac74ddef2d6611ac8eb456`; workspace `2291f2a45d7242f55553f5b8`; backend port 4223 passed auth/write/read/independent SQLite checks. Journal now goes from classification to API binding without a marketing-copy call, then reaches RecordsView authoring at 23:17:48 UTC. This verifies removal of the unused live step, not final UI quality. Authoring failed at 23:18:05 with providers unavailable; no run_finished had yet been observed and the bounded orchestrator retry may still be active. Do not restart/submit another prompt without checking this same handle. `ollama` was not found on the current shell PATH; that alone does not establish whether a local model service exists. Next investigation should establish provider readiness before another full project replay, avoiding repeated backend builds during an outage.

- Terminal follow-up: `run_finished` is now observed for `6aac74ddef2d6611ac8eb454`. The replay is no longer active. No next request has been submitted; provider readiness investigation is next, not another blind full build.

### Local Model Readiness Evidence (2026-09-18)

- Previous continuation was progress: visible replay proved the unused marketing-copy step is gone, but frontend authoring failed on external-provider availability. No full project replay, server restart or source-runtime change was made during this investigation.
- Corrected the PATH-only inference: Ollama is running on 127.0.0.1:11434, PID 6308, executable `C:/Users/home/AppData/Local/Programs/Ollama/ollama.exe`. Its read-only tags endpoint lists already-installed qwen2.5-coder:7b, llava:latest and moondream:latest. The main checkout's local-model configuration points to localhost:11434/v1 and qwen2.5-coder:7b (only non-secret fields inspected). No download, credentials, purchase or external model request was needed.
- Two bounded local probes: exact ready JSON succeeded in 6,581 ms, 6 output tokens; filterRows JavaScript completed in 31,365 ms, 60 output tokens / 23.591 s generation, 72 evaluated prompt tokens. The returned function passed four direct checks: case-insensitive filtering with missing titles, empty result, empty query, and unchanged input. It ignored the no-Markdown instruction by emitting a code fence. This is isolated model evidence, not Joe UI or general engineering acceptance.
- Read-only `/api/ps` reports 5,062,566,870 bytes loaded, size_vram=0 (CPU), context_length=4096. The current complete frontend-authoring context is not demonstrated to fit this window; therefore do not simply enable this model globally and silently truncate contracts. The local router can allow very long waits when LOCAL_BRAIN_FIRST is set. No routing flags were changed. A short local fallback may be useful, but broad frontend authorship requires measured capacity/latency and full UAT first.
- Next general efficiency issue to investigate: `api/src/orchestration/AgentOrchestrator.ts:936` retries the engineering node on provider drought. Earlier live evidence showed a second backend being created inside the same task despite the first backend being verified. Inspect whether this outer retry discards completed phase evidence; do not repeatedly rebuild a successful database because the frontend provider is unavailable. Goal remains active; no final capability parity or GitHub publication is claimed.

### Final Pipeline Evidence Before Provider Retry (2026-09-18)

- Previous continuation was progress: measured the existing local model rather than assuming PATH absence meant no model, established CPU/context/latency limits and identified the outer provider retry as a source of duplicated work.
- Confirmed control-flow defect in AgentOrchestrator: the provider-drought branch scheduled the entire engineering node again before reaching the existing `pipelineFinal`, `verificationFailed`, `nonRecoverable` and deterministic-run guards. A canonical pipeline that had already performed its bounded recovery could therefore be restarted from the beginning, rebuilding a successful backend when only frontend provider availability failed.
- Moved the existing terminal-outcome guard ahead of provider-drought handling, without changing its predicates or introducing another execution path. Canonical phase recovery remains responsible for its one attempt; ordinary nonterminal engineering provider failures retain one delayed retry. Final measured evidence is returned unchanged, not converted into success or an invented recovery plan.
- Added real orchestrator execution tests with a mocked ToolService boundary: terminal pipeline, verification failure and nonrecoverable failure each execute once; nonterminal provider failure executes twice at most; no generative recovery or replanning occurs; the exact output payload (including completed phase evidence) survives. Added ordering regression to existing pipeline suite. Three focused suites passed 44 tests in 14.356 s; the strengthened four behavior cases then passed in 6.116 s. TypeScript and scoped diff checks passed; API bundle succeeded (5.5 MB, 433 ms).
- Required 10-gate run is in progress at `C:/Users/home/AppData/Local/Temp/joe-terminal-retry-gates-20260918-022814`. Current live API is still the marketing-copy-gate version (launcher 9072, logs `joe-api-20260918-021619.{out,err}.log`); no restart, live prompt or GitHub publication was performed for this orchestration patch yet. Full UI acceptance and provider recovery remain unproven.
- Gate follow-up: all 10 required scripts completed with exit 0, including both successful and failed self-healing scenarios. No gate process remains active. Tracked diff for this batch is a guard reorder in AgentOrchestrator (19 insertions / 25 deletions), two ordering assertions in the existing pipeline test, plus the new four-case behavior test and registry evidence. Runtime/UI verification is still pending; do not attribute the new behavior to the currently running API until it is loaded.

### Live Terminal-Retry Verification (2026-09-18)

- Previous continuation was progress: final-evidence guard reordered, behavior tests and all 10 required gates passed. This continuation first used one bounded readiness request through the existing LLM7 provider (256 output-token cap, 15 s provider timeout, 20 s overall signal). It returned exactly READY in 1,336 ms. This established only short-request readiness, not capacity for frontend authoring.
- Loaded the tested orchestration patch into the verified local API only (old PID 17128, launcher 2392, logs `joe-api-20260918-023447.{out,err}.log`). Initial health refusal was startup latency; the same process later returned OK, uptime 84.55, without another restart. Used the visible Codex in-app browser, reauthenticated the local guest and sent the unchanged library prompt once.
- Replay identity: WebSocket target `6aac798864fa478138e7ce89`, chat/journal `6aac798c64fa478138e7ce8b`, workspace `89fc8ce5fc234d94ede62c4e`. The UI terminal report shows 25 steps / 1:18, one completed backend phase, one failed frontend phase and preserved backend proof. Backend source is at `api-small-browser-based-library-chec`, with no second suffixed rebuild reported. API stderr explicitly records `Final pipeline_final for project_pipeline`; stdout records run_finished and no provider-drought outer retry. Thus the no-whole-pipeline-restart behavior has live UI/log evidence, beyond the mocked ToolService tests.
- Overall request still FAILED: the frontend author hit `429 Daily token quota exceeded. Retry after 59 seconds.` despite short readiness succeeding. No new frontend preview exists, and no visual/design acceptance is claimed. Do not treat a tiny readiness probe as evidence the full authoring request fits provider capacity. For this actual generated context, content.js is 5,361 bytes and records-controller.js 14,761 bytes, in addition to the compact surrounding shell/styles and authoring instructions. Further diagnosis should measure the complete request budget/contract needs without another blind whole-project replay. No production action or GitHub publication was performed.

### Artifact Request Budget Evidence (2026-09-18)

- Previous continuation was progress: real UI replay verified terminal-pipeline handling while preserving the successful backend. Frontend authoring still failed on provider quota even though a short readiness request succeeded. No new replay or provider call was made in this continuation.
- Inspected the actual AIGenerator request construction. Destination instructions plus verified runtime/path/layout/import-repair guidance were duplicated verbatim in both the system message and user prompt. Retained the complete contracts/evidence in the task, with a system instruction to follow them and treat source content as data; removed only their duplicated system copy. Original project context, runtime validators, completion budgets and retry limits remain unchanged.
- Added numeric `artifact_request_budget` evidence at each actual AIGenerator call boundary, including retries: artifact kind, retry kind, system/user/context character counts and requested output-token cap. Records both in tool logs and the API console so internal author calls remain inspectable even if the caller ignores returned logs. No source, prompt text, credentials or entered values are included. Character counts are not tokenizer counts, provider billing, or a claim about quota limits; downstream routing may still transform the message.
- Verification: all 52 ai-write-file tests passed (29.215 s); after the logging adjustment, the two focused privacy/deduplication/runtime-contract cases passed (4.517 s). Tests compare metrics with the actual mocked call arguments, ensure a private context marker is not logged, verify the runtime contract occurs once, and still reject an undeclared React Native switch in a React/Vite project. TypeScript and scoped diff checks passed; API bundle succeeded (5.5 MB, 4625 ms).
- Required repair/architecture gate run is in progress at `C:/Users/home/AppData/Local/Temp/joe-artifact-budget-gates-20260918-024435`. Live API still runs the terminal-retry fix (launcher 2392, log stamp 20260918-023447); the budget/deduplication patch has not been loaded or measured in live Joe. No production change or GitHub commit/publication is claimed.
- Gate follow-up: all 10 required scripts exited 0; no verification process remains active. Live request-size measurement and UI acceptance are still pending, and no claim is made that this deduplication alone resolves the provider quota failure.

### Navigation Evidence Continuation

- Full replay evidence after local network correction: `run-1789110814545` passed the permanent Joe UI runner in 332987 ms with one final passing verification, all independent form/persistence/filter/mobile checks, zero page exceptions, and zero server failures. Gravatar 404 remained non-blocking. This supersedes the pending functional replay note below only. Desktop/mobile screenshots still show generic records presentation; visual originality and GitHub publication remain unproven.

- Component evidence: permanent `verify_browser_navigation_ui.ts` passes six real React/Chromium interactions with deterministic external boundaries. It caught and now prevents foreign-session status from overwriting the current URL; session switching also resets connection state. Genuine current errors still degrade quality. API and web type checks pass. This is isolated component evidence, not full Joe UAT or visual originality proof.

- Purpose: prevent superseded browser requests from retrying over newer destinations or degrading their UI state.
- Components: existing authenticated `/nav/goto`, page-scoped `navigation-order.ts`, and `EmbeddedBrowser.tsx`; no additional execution path or model call.
- Evidence: controlled real API race reproduced the old 502; nine focused ownership/route/recovery tests pass after the local patch, with API and web type checks passing.
- Limits: post-fix Chromium/UI replay and publication are pending. The original 41-second UAT error remains unexplained; this fix must not be presented as proof that it is resolved.
- Live follow-up: updated API/Chromium returned 409 for the superseded request and 200 for its successor. Initial preview restoration failed because port 4300 was down; restarting the same generated preview and separately restoring it returned 200. The combined probe therefore exited 1, not green end-to-end. Visible in-app QA, frontend stale-response interaction coverage, full prompt replay, and publication remain pending.
