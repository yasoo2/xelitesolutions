# dependencies
node_modules/

# build output
build/
dist/
dashboard-x/dist/
api/dist/
web/dist/
services/**/dist/

# env files
.env
api/.env
web/.env
web/.env.local
**/.env.local
**/tsconfig.tsbuildinfo

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
coverage/
.cache/
ms-playwright
ms-playwright/
**/ms-playwright/

# Generated artifacts
factory_books_api/
vivos/
.smart_reflex_cache.json

# Generated God Mode Artifacts
api/api/
api/crm-demo/
api/xelite-crm/
data/
.file-cache.json
archive/

# Deploy artifacts (auto-generated, should not trigger local changes)
api/logs/
web/src/logs/
**/logs/
api/last_stable_commit

# Joe local secrets (never commit)
joe-secrets.ps1
api/.playwright-chromium-installed
api/.dep-stamp
web/.dep-stamp

# USER UPLOADS ARE NOT SOURCE CODE.
# Every screenshot attached to a chat lands here. Without this line ten private
# screenshots were swept into a commit by a plain `git add -A`, by someone who
# only meant to save a code fix. They stay on disk; they leave the repository.
uploads/
api/uploads/
